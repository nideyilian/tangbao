## v0.8.20（2026-09-17）

### 🎨 优化

- **系列图批量生成大幅提速**：以前是「生成一条提示词 → 等一张图出图 → 再生成下一条」整条串行，现在一次模型请求直接出 3 组提示词，且首图出图不再阻塞后续组的提示词生成（参考图锚定改为后台等待，出图后自动补发同组其余画面）。以 10 组 × 3 张估算，整体耗时约从 17 分钟降到 2.5 分钟量级（生图尾部受中转站并发影响，以实际任务为准）。同组已有锚定图时重试补缺直接带图提交，不再为锁视觉重复等一张新首图。
- 文件夹导入图片不再构建巨型 base64 字符串：内容哈希与落盘直接吃原始字节，导入 4MB 的图不再产生 5.3MB 的中间字符串，批量导入更省内存也更快。
- 合成图导出（10MB+ 的 JPEG）编码改走异步通道，导出期间界面不再被同步编码卡住。
- 图片入库主路径的缩略图编码补齐为异步（上一版只覆盖了网格通道），「生成完成那一刻不再卡界面」现在对每张新图都成立。

### 🔧 修复与内部

- 更换素材库根目录的迁移更安全：文件收集改为完整递归（修掉遇非文件条目跳过后误删源目录的静默丢数据）、复制后校验文件大小再删源、失败按精确清单回滚；并去掉一个实际无效的「反向搬回」假回滚逻辑。新增 3 条回滚测试。
- 本地图片落盘失败不再静默吞掉：错误会记录日志并派发持久化失败事件，界面能提示「图片可能没保存上」。
- 六处写文件类 IPC 补齐真实路径校验，闭合符号链接逃逸（安全加固；对尚不存在的新文件按祖先目录校验，正常写入不受影响）。
- 历史设置迁移按版本分派：主题配色字段只在旧版本数据上转换，不再重复执行；保持对老数据的兼容行为。
- 收敛内部重复实现：文件名清洗 6 处合 1（保留各处语义差异并有测试锁定）、dataUrl/Blob 转换与 HTML 转义各收敛为单一实现。
- 补齐图片内容哈希（字节入口）、迁移回滚、导出编码、入库缩略图异步通道的回归测试；ESLint 打开未使用变量检查（存量告警 113 处，后续清理）。

## v0.8.19（2026-09-17）

### 🎨 优化

- 图库滚动读取量降到约 1/3：网格磁贴改用 512px 网格缩略图，不再每次拉 1024px 大图（实测单张 79.9KB → 26.7KB）。查看器、详情页与卡片封面仍用清晰大图，画质不受影响；老库的小图会按需补齐，不会作废已有缩略图，也不需要重建。
- 生成完成那一刻不再卡界面：缩略图的 WebP 编码从渲染主线程移到后台线程。实测连续 5 张的界面冻结从 553.8ms 降到 32ms，批量出图不再出现整段掉帧，画质与产物体积完全不变。
- 图片落盘去掉主进程的同步解码与同步写盘：字节改在渲染进程解码后直传，主进程只做一次异步写盘。合成图导出（常有 10MB+）与批量出图期间，主进程不再被单张大图卡住，拖窗、滚动与其它操作不跟着一起顿。
- 打开大图更快：查看器、详情页与悬停大图在有本地文件时直接读取，不再绕「读成 base64 → 转二进制 → 再解码」整条链路，内存峰值同步下降。
- 批量出图的任务记录改为一次提交，不再逐条写库。

### 🔧 修复与内部

- 图片记录与缩略图记录合并为同一次提交，不再存在「图有了、缩略图没写进去」的中间状态。
- 补齐缩略图双通道（完整 / 网格）、图片字节写入、跨命名空间批量写的回归测试；新增缩略图编码路径的测试，覆盖 `toBlob` 不可用时回退到原实现的分支。
- 新增性能诊断报告 `docs/performance-diagnosis.md`：本轮 9 项修复的定位依据、真机实测数字，以及已否决/降级项的理由（含一处自我纠错）。

## v0.8.18（2026-09-16）

### 🎨 优化

- 系列组图批量名额改为以「组」为单位分配：先给每个参考图分派完整组数，再换算成画面数，不再出现一组被拆到不同参考图、或落在参考图上只剩半组的情况。
- 系列图一致性面板讲清「两级固定」的生效范围：维度点亮但留空＝每组各自决定（组内共用、组间有差异）；点亮并填值＝所有组逐字统一；灰色＝每张都不同。填值项的输入提示与读屏标签同步说明范围，不再让人误以为填值只约束组内。
- 系列图固定维度被全部锁定时给出明确收尾规则：还有可变维度就点名由可变维度承担组间差异，连可变维度都没有则要求各组保持一致，避免模型自行编造差异。

### 🔧 修复与内部

- 修复后期处理预设图层 / Logo「无法加载图片」并报 `createObjectURL: Overload resolution failed` 的问题：复合资源的字节过去直接以 Blob 交给 Electron 应用数据存储，而该存储是 SQLite + 主进程 `JSON.stringify`，`JSON.stringify(Blob)` 恒为 `{}`，字节与 MIME 在落库时就被静默抹掉。现改为以 base64 data URL 落库（与缩略图同一套路），读回还原为 Blob。
- 补齐 IndexedDB → SQLite 迁移路径的复合资源转换：此前丢字节的现场正是这里，现已按新格式转换，历史损坏记录按「资源缺失」降级处理，不再把非 Blob 交给 `createObjectURL` 而中断整张预览。
- 应用数据存储新增二进制写入拦截：遇到 Blob / ArrayBuffer / TypedArray 直接报错回滚，宁可写入失败也不再静默丢数据。
- 新增复合资源定向修复脚本 `scripts/repair-composite-assets.mjs`（默认只预演，`--apply` 才写回，写回前自动备份数据库），按内容哈希从素材库原图中找回已损坏的记录。
- 修复素材库中右键图片或任务卡片时菜单「闪一下就消失」的问题：素材库打开期间存在持续的查询自循环（`queryCounts` 进了目录查询 effect 的依赖，而 effect 又会回写查询结果），`assets` 引用每秒变化上百次，网格的布局重测量每次都无条件关闭菜单，菜单刚挂上就被清掉。现已把 counts 移出依赖、让内容未变的回写保持引用稳定，并让布局重测量只在右键目标真的离开结果集时才关菜单。
- 修复系列图提示词请求把「组」当成「条」表述的问题：正文与 JSON 示例统一为「N 组系列图（每组 M 条）」，示例至少给出两组并显式声明组数与每组条数，避免模型把「N 组」读成「N 条提示词」而丢掉组内一致、组间差异的要求。
- 修复系列图组间差异规则表述不完整的问题：明确差异只能来自未锁定的固定维度与可变维度，用户已填值的固定项属全局锁定、不得为制造差异而改写或重新措辞。
- 系列结构化输出的 JSON schema 补齐「画面文字」字段并列为必填，避免走 schema 约束的模型漏掉固定文案。
- 补齐后期处理复合资源读写、素材库右键菜单存活、系列图固定块与全局锁定语义的回归测试。

## v0.8.17（2026-09-16）

### 🎨 优化

- 素材库「图片」视图的右键菜单按选中状态分套：单选时展示全部单张操作；多选时只展示能批量执行的操作（收藏、添加到项目、发送到后期处理、导出原图、移入回收站），并显示「已选 N 张素材」，不再出现「选了多张却只操作一张」的误导。
- 素材库右键菜单新增「打开文件位置」，可直接在资源管理器中定位该素材的本地文件。
- 系列图设置改为输入栏紧凑控件，不再占用整行空间，提示词与参数编辑更顺手。

### 🔧 修复与内部

- 修复素材库默认视图右键图片时菜单「闪一下就消失」的问题：任务卡片右键漏了阻止事件冒泡，导致全局图片右键菜单与素材菜单同时打开、层级互相遮盖后立刻被判定为「点击了外部」而关闭。
- 修复任务卡片上右键图片时一律操作卡片首张素材的问题（右键第 3 张却操作第 1 张），现在会精确定位到落点上的那张。
- 修复系列图一致性缺失「文案内容」维度的问题：文案改为走独立的画面文字通道，组内固定块仍逐字复用，避免文字内容被当作可变项而漂移。
- 修复外部控制工具（`--asset-mcp`）无法工作的问题：单实例锁导致进程启动即退出，且 Windows 下主进程读不到标准输入流。
- 外部 AI 工具现在可读写工作区状态：新增 `get_app_state` 获取当前提示词与参数，`set_prompt` / `set_params` 直接写入编辑区，并补齐了对应的回执信息。
- 设置页新增「复制 MCP 配置」按钮，一键取得外部工具接入所需的配置片段。
- 应用级读写逻辑抽成独立可测模块，并补齐素材库右键菜单、右键目标定位、外部工作区命令的回归测试。
- 发布流程新增发布后校验：自动检查更新描述文件可下载，安装包异常的坏发布会直接判定失败。

## v0.8.16（2026-09-15）

### 🎨 优化

- 系列组图组内一致性：每组只生成一份「组内固定视觉常量」，组内每张画面逐字复用同一段固定块，模型只写各张的变化部分，不再每张重新描述一遍画风。
- 系列组图新增「首图锚定」：同组第 1 张出图后自动作为其余张的参考图，锁定画风、构图骨架与排版结构；可在批次设置里关闭。
- SOP 管理中心支持手工配置系列图：可把 SOP 标记为系列图，并设置每组张数、固定维度与可变维度。

### 🔧 修复与内部

- 修复系列组图「生出来完全不像同一系列」的问题：此前 AI 生成的组内固定规则被整段丢弃，每张都是独立的文生图任务，风格、构图与排版必然漂移。
- 修复变量提示词 SOP 叠加系列模式时少生成画面（把组数当成条数展开）的问题。
- 修复非渐进生成路径的提示词缺少系列分组信息、导致首图锚定静默失效的问题。
- 系列模式下重新生成单条提示词改为只生成 1 张并沿用本组固定规范，不再生成整组后丢弃其余结果。
- 补充固定块拼装、首图锚定等待、系列配置编辑与变量提示词计数的回归测试。

## v0.8.15（2026-09-14）

### 🎨 优化

- 系列组图 SOP：AI 生成 SOP 时自动识别系列需求并写入组内固定维度与可变维度，SOP 库显示「系列 N 图」徽标；提示词管理弹窗与输入栏胶囊可在「普通 / 系列」之间切换，结果网格按系列分组展示。
- 新增参考图风格复刻与视觉 Skill：可从参考图创建可复用的视觉 Skill（视觉分析、中英文提示词模板、关键词表，支持编辑与删除），输入新主题后按主题批量出图。
- 设置页模型列表改为展示全部已发现模型并支持滚动，不再只显示前 8 个。
- Agent 参考图按请求体积上限自动压缩（总量 7MB、单张 4MB），超限图片以占位符告知模型，并针对 413 给出明确提示。

### 🔧 修复与内部

- 修复 SOP 相关文本模型请求没有超时导致界面永久卡在「正在生成提示词」的问题：SOP 生成、SOP 提示词批量生成、变量词条扩展、视觉档案分析、变量模板生成、风格复刻与视觉 Skill 创建全部接入统一超时保护，超时后给出包含秒数的明确错误，并跳过「再等一次也一样」的重复等待。
- 修复生图请求超时被误报为「请求已取消」的问题；主动取消与超时现在严格区分。
- 系列提示词解析放宽组数校验：多余组截断、缺口交由批次循环补齐、组内不完整的组直接丢弃，兼容不支持结构化输出的模型，不再整批失败。
- 修复系列模式下 SOP 批次进度单位错位（显示「6/2」）与恒为真的「提示词缺口」误报，进度与缺口统一按画面统计。

## v0.8.14（2026-09-10）

### 🎨 优化

- Agent 生图新增文案说明与四步执行进度（理解需求、规划图片方案、生成图片、保存结果），实时反馈提交、等待服务商、生成数量、保存、部分失败和停止状态，执行过程更透明。
- SOP 智能生成新增「分体生成说明」：可按视觉或通用场景套用预设，再分别配置目标、输入、输出、约束与排除项，支持自定义内容和发送前完整预览。
- 模型切换按服务商分组并显示来源信息，选择模型时更容易辨别服务商与能力。
- 优化选择框、蒙版编辑器、任务网格和弹层的 Esc 关闭、焦点与交互稳定性。

### 🔧 修复与内部

- 修复 Agent API 请求超时与主动取消被混淆的问题，超时提示更明确，用户停止操作不再误报为超时。
- Electron fuses 改由 electron-builder 在签名和制作安装包前自动加固，避免发布产物在打包完成后才修改可执行文件。
- 拒绝不支持的 ZIP64 备份文件，并完善项目导出、外链打开及本地文件读取的边界校验。
- 补充 Agent 执行进度、SOP 生成说明、模型切换、弹层交互和备份导入相关回归测试。

## v0.8.13（2026-09-04）

### 🔧 修复与内部

- 修复 SOP 批次任务卡在图片已成功进入素材库、但任务记录未及时同步时仍显示“生成失败”和 `0/1` 的问题；卡片现在会合并任务记录与素材库实际产出判断状态，真实的部分失败仍保持失败提示。
- 增加 SOP 批次卡与素材库分组视图回归测试，覆盖状态滞后、完整成功和部分失败场景。

## v0.8.12（2026-09-04）

### 🎨 优化

- SOP AI 对话支持直接拖拽图片、拖入素材库图片，以及使用 `Ctrl/Cmd + V` 粘贴剪贴板图片；图片会随消息保留，并在多轮对话中作为视觉上下文发送。
- SOP AI 对话发送前自动压缩过大的附件，兼容 Responses API 与 Chat Completions API。
- 素材库、任务卡片、批次视图与本地图片工作区继续优化，提升图片加载、整理、重试和导出流程的稳定性。

### 🔧 修复与内部

- 完善 Electron 本地图片存储、迁移、目录监控、API 密钥隔离与渲染进程安全校验。
- 修复图片尺寸参数、批量任务、任务重试、素材引用清理和本地文件同步相关边界问题。
- 增加 SOP AI 图片附件、多模态请求、素材引用保护及相关工作区回归测试。

## v0.8.11（2026-09-02）

### 🎨 优化

- Windows 安装包与运行窗口统一使用当前 `icon.ico`，新版本安装后不再回退显示 Electron 默认图标。
- 任务卡片模式下打开任务详情或 SOP 批次详情时，生成中的图片与重试后的最新任务状态会持续更新，无需关闭弹窗再打开。

### 🔧 修复与内部

- 修复 SOP 生图提示词管理弹窗后台保活后仍使用上一轮输入要求的问题；重新输入后，自动生成与手动生成都会使用最新内容。
- 补充任务详情与 SOP 批次详情的回归测试，覆盖任务重试、提示词同步和弹窗内结果刷新。

## v0.8.10（2026-08-30）

### 🎨 优化

- 后期处理「开始导出」改为任务发送制：点击后任务即刻入队并在后台按顺序执行，界面立即恢复，可马上重新配置并发送下一个导出，无需等当前导出完成；发送时捕获完整配置快照（排队期间修改预设/背景/分配规则不影响已发送任务）；执行中按钮变为「加入导出队列」，排队任务显示「后台队列：N 个任务待导出」并可一键取消排队（不影响正在执行的任务）。
- Eagle 式 Delete/Backspace 快捷键：删除选中的任务/收藏卡片（输入栏）、词条（词条库）、SOP（SOP 库）；大图查看器中直接删除当前素材（移入回收站、自动切换下一张，删完最后一张自动关闭）；素材库全局快捷键在模态弹窗/遮罩打开时让位，避免穿透误操作。
- 图库输入栏批次参数（提示词数量 / 每条图片数 / 自动生图）可直接在胶囊上编辑与切换，通过同步信号实时回写已挂载的「提示词管理」弹窗，两处保持一致；弹窗批次设置区默认展开。
- 全局 toast 位置跟随输入栏高度（`--input-bar-clearance`），始终悬浮在输入栏正上方，不再遮挡底部参数/操作区。
- Esc 交互修正：下拉选择（Select）按 Esc 只关闭下拉、不再连带关闭外层弹窗；素材库侧栏右键菜单 / 批量移动菜单支持 Esc 兜底关闭；SOP 封面选择弹窗支持 Esc 关闭；批量弹窗内右键菜单 Esc 优先于弹窗级关闭。
- 词条库弹窗 Esc 关闭带未保存保护：词条有未保存修改时先弹确认，避免直接丢失改动；SOP 正文复制成功时给出提示。

### 🔧 修复与内部

- 修复素材库 Ctrl+Z/Y 撤销被注册两次的问题：撤销快捷键统一由 `useAssetLibraryShortcuts`（capture 阶段）处理，移除工作区重复监听，避免一次按键触发两次撤销（撤销跳步、可撤销次数减半）。
- 素材库筛选 Tab 支持键盘聚焦（tabIndex=0）；查看器快捷键不再拦截输入框/文本域内的按键。
- 后期处理导出任务流/运行状态文案迁入 store（后台任务可驱动结果面板），暂停/继续/取消仍作用于当前执行任务，每任务完成后照常写入历史记录与完成提醒。

## v0.8.9（2026-08-29）

### 🎨 优化

- SOP 模块整体重构（功能收敛 + 界面治理）：
  - 策略编辑器 SOP 区改为「引用 + 选择/管理」：显示当前 SOP 摘要卡片，从 SOP 库选择预设或打开 SOP 库管理；删除了内嵌的名称/内容/说明编辑表单与重复的手写预设弹窗（SOP 预设选择器组件接线启用）。
  - SOP 管理中心在策略工作台支持「应用 SOP 到当前策略」与「取消应用」，并标记「使用中」；图库模式下「生成提示词」按钮直接跳转提示词管理弹窗并定位到该 SOP。
  - 正文编辑器界面收敛为单层工具栏：移除标题栏与全屏模式，空间全部留给正文；快速整理（自动分段/统一格式/清理粘贴）并入编辑工具条；AI 指令全部移入常驻对话侧栏的快捷指令行，删除冗余的「AI 对话」开关按钮（对话侧栏真正常驻）。
  - 对话侧栏新增快捷指令胶囊（普通 SOP：将具体词泛化 / 结构化重排 / 精简压缩 / 拆分步骤 / 补全缺失 / 统一术语；元素池 SOP：选项泛化 / 衍生选项 / 改写选项），点击即填入输入框、可编辑后发送；支持添加自定义快捷指令（弹窗管理、本机持久化、上限 20 条）。
  - 「插入可变项示例」从整段引导说明收敛为单个胶囊按钮，与快捷指令同排。
  - SOP 参数区补上「说明」字段编辑入口；SOP 列表行 hover 浮起、选中主题条、操作按钮悬停显现；编辑面板头部操作按钮分组收纳。
  - SOP 管理中心拆分为独立标签页组件（SopLibraryTab / SopMetaTab / SopGenerateTab）与自包含弹窗组件（封面选择 / 生成记录详情 / 提示词快照 / 版本历史），样式全面接入设计令牌（styles.css 硬编码清零）。

### 🔧 修复与内部

- 移除未接线的死代码（原 SOP 预设选择器组件从未被使用，现已启用）与「AI 结构化/精简」有标签无按钮的残留；删除旧「SOP 预设」类型迁移链与重复的种子生成元指令；对话面板移除冗余的 externalInput 注入链路。
- 图库输入栏与提示词管理弹窗的批次参数（条数/每条张数/自动生图/二次参考）改为单一数据源，弹窗内修改实时回写，胶囊行只读展示状态。

## v0.8.8（2026-08-27）

### 🔧 修复与内部

- 修复画廊提示词输入框的缓存串用问题：输入内容按素材库文件夹隔离保存/恢复，切换文件夹后生图不再读取到其他文件夹残留的提示词（草稿持久化，重启后按当前文件夹恢复）。

## v0.8.7（2026-08-27）

### 🎨 优化

- SOP 正文编辑器新增「AI 指令」工具栏（将具体词泛化 / 精简压缩 / 拆分步骤 / 补全缺失 / 统一术语）：点击后就地生成修订预览（变更摘要 + 全文对比），可先测试生图再确认替换，支持撤销，全程无需离开正文。
- AI 对话面板默认常驻 SOP 库「参数与正文」模块右侧，打开 SOP 即可使用，无需再点按钮弹出；「AI 对话」按钮只负责开关，全屏编辑与对话相互独立（Escape 仅退出全屏）。
- 移除与 AI 对话重复的「AI 结构化 / AI 精简」入口及预览条替换逻辑，保留「AI 检查」（只出审查报告、不改写正文）；对话面板移除常用指令 chips，由正文工具栏统一承接。

### 🔧 修复与内部

- 修复 SOP 库分组侧栏被隐藏、SOP 列表被压缩的问题：过时的 `:has(.sop-ai-chat)` 布局规则在 AI 对话常驻后误触发，现已移除，SOP 库恢复完整三栏（分组侧栏 / SOP 列表 / 编辑面板），并适度收窄常驻对话列宽。
- SOP 批量生图的运行状态按「工作区标签页 + 素材库文件夹」双重隔离：同一标签页内在不同文件夹选择 SOP 生成，各自持有独立的草稿与运行状态，互不打断（已提交的生图任务仍全局后台执行，不受影响）；非文件夹视图退化为按标签页隔离，旧数据兼容。

## v0.8.6（2026-08-26）

### 🔧 修复与内部

- **修复安装版带参考图的图生图/局部重绘全部失败（`Failed to fetch`）的问题**：参考图与遮罩经 data URL 转 Blob 时需要 `fetch(data:)`，而打包版注入的 CSP `connect-src` 未包含 `data:`，请求被 Content Security Policy 直接拦截（开发版无 CSP 所以正常）。现已在 `connect-src` 中加入 `data:`。
- 主进程 API 通道改用 Electron `net.fetch`（Chromium 网络栈）替代 Node/undici：undici 默认 300 秒无响应即断开，且对慢速长请求的过期 keep-alive 连接不会自动重连；改用后与浏览器开发版行为一致，长请求与并发连接更稳健（不影响请求参数与流式解析）。

## v0.8.5（2026-08-26）

### 🎨 优化

- 设置弹窗尺寸调整为界面大小的 50%（50vw × 50vh），为配置表格提供更充裕的空间。
- 「生图 API」与「Agent API」的服务连接改为表格形式：每行一个配置/服务，含「启用」单选列与行内「添加服务」入口，顶部实时显示配置数量；点「启用」或行即可切换当前生效的配置。
- 服务配置字段整合进表格内联编辑：名称、服务商（下拉切换、可拖拽排序）、API URL、API Key（可显隐）、模型（带已检测模型候选列表）均在表格内直接编辑，删除表格下方的重复表单字段。
- 表格操作按钮改为纯图标（复制/删除），并收窄名称、服务商列宽，节省横向空间。
- 新增 `.ds-table--dense` 密集表格样式，供设置页内联编辑表格使用。

### 🔧 修复与内部

- 修复 3 处既有 lint 报错（`no-extra-boolean-cast` / `no-useless-escape` / `preserve-caught-error`），行为不变。
- eslint 忽略本地构建与开发脚本（`electron-builder.cjs`、`scripts/**/*.cjs`），与既有 `scripts/**/*.mjs` 忽略规则保持一致。

## v0.8.4（2026-08-25）

### 🎨 优化

- 后期处理（合成）导出的文件夹命名改为完全跟随「文件名模板」，仅去掉 `{index}` 序号字段：同一预设/尺寸的一组图片聚合到同一文件夹，文件用含序号的文件名区分。
- Agent API 支持添加多个独立的 Agent 服务，可自由切换：新增「添加 Agent 服务」按钮与「服务连接」下拉（含复制/删除），各服务独立保存 API URL、密钥与模型。

### 🔧 修复与内部

- 移除已废弃的 `subfolderTemplate` 字段（文件夹命名现由文件名模板派生），旧持久化数据中的该字段会在加载时安全丢弃。
- 修复 `preload.cjs` 构建产物损坏导致的 Electron 启动报错：因 `type: module` 下 `formats` 被合并成 `["es","cjs"]` 两种格式竞争写同一文件，现改为显式输出单一 CJS 格式。
- 修复 Agent 模型切换在切换器（ModelSwitcher）中不生效的问题，与多服务列表结构保持一致。

## v0.8.3（2026-08-25）

### 🔧 修复

- 修复任务卡片视图下「批次详情弹窗」在生图过程中每生成一张图就被自动关闭的问题：素材新增时不再关闭弹窗，仅在批次真正消失（整批删除/素材清理）时才关闭。

### 🎨 优化

- 统一工具提示（Tooltip）视觉为设计系统深色气泡（复用 `ds-tooltip` token、无边框），并将 `useHintTooltip` 并入 `useTooltip` 单一 hook。
- 新增根目录 `AGENTS.md`，固化发布流程与代码规范，便于后续自动执行发布。

## v0.8.1（2026-08-24）

### 🎨 优化

- **素材库快速预览弹窗对齐设计系统规范**：容器圆角改用 `--ds-radius-xl` 语义令牌、角标字号改用 DS 字号体系（`text-xs`），与其他弹窗观感统一，为皮肤切换提供一致的视觉基线。

### 🔧 修复与内部

- 修复设计系统合规回归测试暴露的 2 处旧工具类残留（新文件禁用 `rounded-xl` 与 `text-[11px]` 裸像素字号）。
- 本版为 0.8.x 系列首个基线版本：后续功能（批量变量提示词等）将基于此版本迭代，请留意后续更新。
