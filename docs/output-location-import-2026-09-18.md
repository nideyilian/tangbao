# 《输出位置明细-独立版》导入记录（2026-09-18）

源表：`C:/Users/tt/Downloads/输出位置明细-独立版(1).xlsx`（数据行 202）。
导入内容：**每个方向的共享盘导出位置 + 合规水印文案**。创量目录与交付方式按 2026-09-18 定案**不做**。

## 写到哪里

| 内容                | 落点                                                                          | 编码                                                  |
| ------------------- | ----------------------------------------------------------------------------- | ----------------------------------------------------- |
| 10 个纯文案水印预设 | localStorage 键 `tangbao-composite-v2-workspace-storage`（persist version 5） | zustand persist 快照                                  |
| 58 个节点参数       | SQLite `app_data_records` → namespace `projectTreeParams` / record `state`    | **双重编码**（`JSON.stringify(JSON.stringify(...))`） |

导入前备份：`%APPDATA%\tangbao\backup-before-import-20260918-163254\`（含 `local-saves/`、
`Local Storage/`、`Session Storage/`）。
写入方式见 `docs/tangbao-ops-runbook.md` 第九节。

## 导入结果（写后回读校验）

- 水印预设：**1 → 11 个**（原有 `preset-default 默认产品预设` 保留，未覆盖；新增 10 个）。
  新进程回读确认：10 个预设各 1 个 text 图层、字号 32、锚点 `bottom-center`、白字带阴影。
- 节点参数：**58 个节点**（产品 13 + 方向 45）。
  - 节点级 `watermarkPresetIds`：30 个节点（各渠道一致的水印写在节点级，少一层结构）
  - 按渠道 `byMedia.watermarkPresetIds`：6 处
  - 按渠道 `byMedia.outputDir`：**80 处**；**节点级 `outputDir` 为 0** ——
    表格里每个方向的目录都按渠道分开给，没有「各渠道共用一个目录」的情况。
  - 出现的渠道：`baidu` `toutiao` `gdt` `vendor`
- 覆盖的 58 个节点 id 全部存在于 `collections`（77 条，0 缺失）；表格覆盖的 61 个方向 = 树里的 61 个方向。
- 唯一被覆盖的旧值是 `builtin-direction-9`（原 `preset-default`，测试留下的占位），
  已按表格换成正式规格。

## 水印预设清单

| id                     | 名称（取最长行）          | 引用行数 | 对应图标               |
| ---------------------- | ------------------------- | -------- | ---------------------- |
| `preset-compliance-01` | 本故事纯属虚构            | 38       | 图标114+118+312+315 等 |
| `preset-compliance-02` | 所展示金额为广告创意实际… | 22       | 图标100+109 等         |
| `preset-compliance-03` | 活动期间奖品数量有限先到… | 8        | 图标188 等             |
| `preset-compliance-04` | 具体以APP内实际活动为…    | 8        | 图标210 等             |
| `preset-compliance-05` | 砍价至1分钱可购买活动时…  | 7        | 图标112+161 等         |
| `preset-compliance-06` | 详情见活动规则以实际售卖… | 6        | 图标167+170 等         |
| `preset-compliance-07` | 本素材纯属广告创意具体理… | 4        | —                      |
| `preset-compliance-08` | 赔付限额600万元保费预…    | 4        | —                      |
| `preset-compliance-09` | 该活动存在时效性具体优惠… | 3        | —                      |
| `preset-compliance-10` | 具体价格以APP内活动为…    | 2        | 图标334、337           |

**图标图层一律留空**（2026-09-18 定案「先建纯文案水印」）：表格里给了图标编号，
但图标图片在本机找不到（素材库 0 条、内置资源只有 `app-icon.png`/`icon.ico`、NAS 浅层也没搜到）。
图标素材到位后在 WebP 覆盖层里补进同一批预设即可。

## 需要人工确认的两处（导入刻意不改）

导入只填**表格里有的东西**；下面两项是**全局启用范围**，属于人的决策，没有被自动改写：

1. **媒体范围** `postprocessMedia.selectedMediaIds` 当前是 `["clean", "gdt"]` ——
   表格覆盖了 **厂商 / 百度 / 头条 / 广点通** 四个渠道，只开广点通的话，
   按渠道配好的目录与尺寸都不会展开。
2. **方向范围** `postprocessMedia.selectedCollectionIds` 当前只有 `builtin-direction-10` ——
   归属方向自身或任一祖先被勾才启用，**没勾任何就完全不跑**。

## 本轮未覆盖

- **无文案的 100 行**（表格里仅图标 / 空白水印）：没有文案可建，图标也缺。分布在 28 个方向上。

## 已知量的两处不精确

- 表格里 178 行标「产品级继承」，其中**共享盘目录**按 (产品, 渠道) 分组有 **2 组**内部不一致
  → 这 2 组已降级为逐方向写，其余上提到产品节点。
- 水印按**方向**去重（同一方向各渠道若写同一文案则落在节点级），
  所以「节点级 30 + 按渠道 6」不等于方向数 61 —— 未出现的方向表示该方向无水印。
