# 糖包 风险登记册（RISK）

> **本文件是唯一的风险清单。** 回答"哪些坑不能踩"。
> 从 `.workbuddy/memory/MEMORY.md` 的叙述式陷阱升级为**带分级、触发条件、检测手段、缓解动作**的登记册。
> 具体操作配方见 `docs/tangbao-ops-runbook.md`；本文件只登记风险与对策，不写配方。

## 等级定义

| 等级  | 含义                                           | 要求                                    |
| ----- | ---------------------------------------------- | --------------------------------------- |
| **R** | 数据安全级：会**不可逆地**丢数据 / 密钥 / 历史 | 动手前必须备份 + 停应用；改动必须有守卫 |
| **P** | 返工级：会导致静默失效、误判、诊断成本极高     | 必须有检测手段或测试守卫                |
| **Q** | 质量级：影响可维护性与改动速度                 | 有意识控制，不阻断交付                  |

**状态**：`OPEN`（现行有效） / `MITIGATED`（已有代码或流程守卫） / `CLOSED`（根因已消除）

---

## R 级 · 数据安全

| ID   | 风险                                                                                                                         | 触发条件                                         | 检测                                                                 | 缓解                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 状态                                               |
| ---- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| R-01 | **并行写线互相破坏**：HMR 把改动热更新进另一条正在使用的窗口；41731（`strictPort`）与单实例锁互抢；leveldb 被应用独占 `LOCK` | 同一仓库同时有 ≥2 个会话/进程在动代码或跑 dev    | `netstat` 查 41731；确认 dev 进程父链（`bash → npm run dev → vite`） | **单写线原则**；真并行用 `git worktree` + 独立端口/userData，见 `docs/work-protocol.md`                                                                                                                                                                                                                                                                                                                                                                                    | OPEN · 24h 内触发 3 次                             |
| R-02 | **`git rm` 会大面积删文件**（曾让 `src/` 576 个文件消失）                                                                    | 用 `git rm` 删单个文件                           | 操作后立即 `git status` 看待删清单                                   | 一律用 `rm` + `git add -A`                                                                                                                                                                                                                                                                                                                                                                                                                                                 | MITIGATED                                          |
| R-03 | **配置重置会连真 Key 一起抹掉**（`scheduleApiSecretsPersist` 随 settings 重写 `api-secrets.bin`，不可逆）                    | settings 被重置为该 namespace 的默认值           | `api-secrets.bin` **字节数**（正常约 302B；被抹后 282B → 205B）      | 改 settings 前先备份该文件；`apiKey` 恒空串是正常的，**判据是字节数**                                                                                                                                                                                                                                                                                                                                                                                                      | MITIGATED（`desktopJsonStorage` 读失败已拒绝写盘） |
| R-04 | **手改 `app_data_records` 覆盖其他记录**：`WHERE namespace=...` 不带 `record_id` 会一次写坏同 namespace 的全部记录           | 手工 UPDATE 未带 `record_id` 条件                | 改后逐条回读校验                                                     | ① 停应用 + 备份；② 写库必须带 `record_id`；③ 改完全库体检                                                                                                                                                                                                                                                                                                                                                                                                                  | MITIGATED（已写进 runbook）                        |
| R-05 | **编码分两种**：zustand 型是**双重编码**、记录型（`appDataPut`）是**单层**；用错则数据变字符串 → 下游 `.filter` 崩溃         | 手工写库 / 新增读库代码                          | 写前 `typeof JSON.parse(row.json)` 判一次                            | 按 namespace 分别处理；读取侧做兼容解析（`decodeSopBatchSnapshotRecord` 已是范例）                                                                                                                                                                                                                                                                                                                                                                                         | MITIGATED                                          |
| R-06 | **读带残留 WAL 的 SQLite 可能读到 0 条**（未 checkpoint 的帧不可见）→ 会把"合并"做成"覆盖"                                   | 用 read-write 连接打开正在使用的库取基线         | 基线读到 0 而备份里有数据                                            | **基线一律用 `readOnly: true` 读**                                                                                                                                                                                                                                                                                                                                                                                                                                         | MITIGATED（runbook 第九节）                        |
| R-07 | **新增 store 忘配 namespace 白名单 = 完全存不住且 UI 不报错**                                                                | 新增持久化 store 未改 `electron/asset-kernel.ts` | 重启后回读该 namespace                                               | 新增即登记白名单；用 `appDataNamespaceContract.test.ts` 守住                                                                                                                                                                                                                                                                                                                                                                                                               | MITIGATED（有契约测试）                            |
| R-08 | **`.workbuddy/` 被 gitignore → 记忆永不入库**，换机/故障即全部沉淀归零                                                       | 一直存在（原 `.gitignore:29-30`）                | `git ls-files --others --exclude-standard .workbuddy/`               | ✅ **已修（2026-09-18）**：`.gitignore` 改为白名单，**只入库 `MEMORY.md`**。范围收敛的原因：实测仓库是 **public**，过程日志含本机路径与服务商域名 → 日志留本地；硬结论已分层到 RISK / runbook / ADR（全部入库）                                                                                                                                                                                                                                                            | **MITIGATED**                                      |
| R-09 | **工作区长期挂着未提交改动，与并行改动混在一起**                                                                             | 多会话同时改同一仓库                             | 每次提交前 `git status` 逐文件确认归属                               | **禁止 `git add -A`**；提交只 add 本轮文件。⚠️ 2026-09-18 提交前实测：工作区同时挂着**两条线**的改动（未推送的功能修复 + 新建的 docs），已按线拆成两个提交。<br>⚠️ **2026-09-21 补充（自己被反咬一次）**：混合同一文件时（你与对方改**同一个**文件）「只 add 本轮文件」还不够 —— `git add <该文件>` 会把对方那份一起收进去。正解是**只把自己的 hunk 入索引**（`git apply --cached`，配方见 runbook §八），且**入完索引后不能再对该文件 `git add`**（整份覆盖，筛选白做）。 | MITIGATED                                          |

## P 级 · 返工 / 静默失效

| ID   | 风险                                                                                                                                                          | 触发条件                                                        | 检测                                                                                                                | 缓解                                                                                                                                                                                                                                                                                                                              | 状态                             |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| R-10 | **门禁放行"声称改了但没落地"**：`noUnusedLocals/Parameters=false`（`tsconfig.json:19-20`）+ `no-unused-vars: warn`（`eslint.config.js:48`），死 import 零告警 | 只加 import 没换调用点                                          | `npm run lint` 无输出（漏检）                                                                                       | 打开 `error`（TB-021）；✅ **已修（2026-09-18）**：存量 114 处**已全部清零**，`no-unused-vars` 与 `no-explicit-any` 在 `src/**` + `electron/**` 上从 `warn` 收紧为 **`error`**（`eslint.config.js`）→ CI 的 Lint 步骤即守卫，**新增即红**。过渡用的棘轮脚本已按计划退役（规则本身已是硬门禁）。对关键改动仍应补**结构性断言**测试 | **RESOLVED**                     |
| R-11 | **记忆入库可能连带本机隐私**：记忆文件含本机绝对路径、密钥字节数、凭据获取方式                                                                                | TB-025 执行时                                                   | 入库前全文检索 `token` / `pat` / `sk-` / `C:\Users` / 主机名                                                        | ✅ **已执行（2026-09-18）**：扫描确认**无完整凭据**（命中的只有 `github_pat_…` / `gho_…` 这类前缀示意）；仓库实测为 **public** → 进一步收敛为**只入库 `MEMORY.md`**，过程日志留本地                                                                                                                                               | **MITIGATED**                    |
| R-12 | **`store.setAppMode` 兜底分支把非白名单值改写成 `agent`** → 顶栏切换"点了没反应、零报错"                                                                      | 新增 `AppMode` 值时未补分支                                     | 点 tab 后查 `appMode` 实际值                                                                                        | 新增工作区必须补分支；已有测试守住                                                                                                                                                                                                                                                                                                | MITIGATED                        |
| R-13 | **`InputBar` 的 prompt 是双写**（store + contentEditable）：程序性改写未置 `isUserInputRef.current = false` 会被 effect 吞掉，下次从 DOM 回读把改动整个抹掉   | 新增任何"程序性改写 prompt"的入口                               | 改完后在输入框多敲一个字，看改动是否还在                                                                            | 改写前必须复位标志；effect 已收紧为「DOM 纯文本 === prompt 才跳过」                                                                                                                                                                                                                                                               | MITIGATED                        |
| R-14 | **`read` 不能用「返回值是否字符串」判有效性**（单层编码会返回对象 → 判为"首次运行" → 用默认值回写覆盖）                                                       | 持久化读取实现                                                  | 用单层编码记录做人工验证                                                                                            | `desktopJsonStorage` 已三种编码都认 + **读失败进降级态拒绝写盘**                                                                                                                                                                                                                                                                  | MITIGATED                        |
| R-15 | **`byMedia` / 参数解析必须 `??` 不能 `\|\|`**：`''` 与 `[]` 都是**有效值**（显式覆盖）                                                                        | 写合并/回退逻辑                                                 | 用例覆盖「空数组 = 明确不加水印」                                                                                   | 判据一律 `=== undefined`；`distribution` 语义是**整份替换**不是逐字段继承                                                                                                                                                                                                                                                         | MITIGATED                        |
| R-16 | **调 API 配置前先看它调哪个解析层**：图片配置改动**动不了** SOP 文本链路（`getAgentApiProfile` 独立），凭直觉判断会诊断到错的层                               | 修改 API 配置后判断影响面                                       | 按调用链定位（`getActiveApiProfile` / `getAgentApiProfile` / `getAgentTextApiProfile` / `getAgentImageApiProfile`） | `apiProfiles.ts` 三层解析已写进 runbook                                                                                                                                                                                                                                                                                           | MITIGATED                        |
| R-17 | **同一文件多条 Edit 必须串行**（并发只最后一条生效）；**整块删除时尤其危险** —— 把待删块误当 `new_string` 会变成"反向追加"（实测把 1 处复制成 4 处）          | 对同一文件连续发多个编辑                                        | 改后回读关键行                                                                                                      | 串行执行；**删多行块先 Read 拿行号 → 精确整块替换 → 立刻回读**（2026-09-19 复现）                                                                                                                                                                                                                                                 | OPEN                             |
| R-18 | **删文件必须三查**：① grep import ② grep 字符串字面量 ③ grep `readFileSync`                                                                                   | 判定为死代码后删除                                              | 三查缺一即可能漏                                                                                                    | 按三查流程执行                                                                                                                                                                                                                                                                                                                    | MITIGATED（进 `AGENTS.md` 语境） |
| R-19 | **Bash 工具会吃 `\\` 与 `${}`、改写 `/c/...`** → 脚本静默写错路径                                                                                             | 用 Bash heredoc 传含 Windows 路径的脚本                         | 脚本里路径是否完整                                                                                                  | **脚本一律用 Write 落盘，再 `node "C:/…"`（正斜杠）执行**                                                                                                                                                                                                                                                                         | MITIGATED                        |
| R-20 | **`npm run verify` 期间删临时文件会让 ESLint 崩**（`ENOENT ... .tmp-*.cjs`，扫描队列与删除并发）                                                              | verify 与清理并发                                               | verify 报 ENOENT                                                                                                    | 清理临时文件等 verify 跑完，或放到项目外                                                                                                                                                                                                                                                                                          | MITIGATED                        |
| R-21 | **`.git/refs/remotes/` 有环境级写保护** → `git status -sb` **恒报 `[gone]`**，会误判成"推送失败"                                                              | 任何 fetch / update-ref                                         | `git for-each-ref refs/remotes/` 恒空                                                                               | 推送自检只能用 `ls-remote` 与 `rev-parse HEAD` 比 SHA（runbook 第八节）                                                                                                                                                                                                                                                           | OPEN（环境问题，无法消除）       |
| R-22 | **看门狗超时会把任务提前标终态但请求还在飞** → 迟到结果是孤儿（既不进卡片也不进素材库）                                                                       | 生成超时后又返回了结果                                          | 检查三处 `status === 'running'` 守卫                                                                                | 已用 `canSettleTaskOutputs` 放行（`watchdogTimedOutAt` 标记）                                                                                                                                                                                                                                                                     | MITIGATED                        |
| R-23 | **收尾写入可以让 `outputImages` 变短** → 卡片数量少于素材库                                                                                                   | 槽位快照与 `outputImages` 写盘不一致；或 fire-and-forget 写失败 | 对比卡片数与素材库数                                                                                                | `pickMoreCompleteOutputIds` + `persistTaskWithRetry`；**两者不一致即落盘不完整，别去查加载链**                                                                                                                                                                                                                                    | MITIGATED                        |

## Q 级 · 质量 / 效率

| ID   | 风险                                                                                                                                                                                                                                                       | 缓解                                                                           | 状态                                                   |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------ |
| R-24 | **组件巨型化**：`store.ts` 13346 / `InputBar.tsx` 5565 / `SettingsModal.tsx` 5414 / `GallerySopBatchModal.tsx` 4216 行                                                                                                                                     | `AGENTS.md` 已写「勿继续膨胀」；拆分随 TB-023 顺带做，不单独立项               | OPEN                                                   |
| R-25 | **大提交不可回滚**：最大 `8b26160` 为 54 文件 / +338 −8978 行                                                                                                                                                                                              | 按"可独立验收的最小单元"提交；大重构按「删消费方 → 剥字段 → 清死代码」分次提交 | OPEN                                                   |
| R-26 | **落盘版本 bump 必须配丢弃式 migrate**，否则旧字段被静默丢弃、用户配置消失                                                                                                                                                                                 | 已固化为惯例（`storeV2` v4→v5、v3→4 均为丢弃式）                               | MITIGATED                                              |
| R-27 | **`CompositeV2Preset.sampleBackgroundPath` 只有读没有生产者**（暂留）                                                                                                                                                                                      | 明确标注，避免被当死代码删掉时误伤读端                                         | OPEN（刻意保留）                                       |
| R-28 | **任务卡片高度固定** `TASK_CARD_ROW_HEIGHT = 192`，卡内加可展开区块会撑破布局                                                                                                                                                                              | 已知约束；新增内容前先确认高度                                                 | MITIGATED                                              |
| R-29 | **`localStorage` UI 状态在测试间泄漏**（分隔条比例等持久化值会变成下个用例初值）                                                                                                                                                                           | 受影响测试的 `afterEach` 清 `localStorage`                                     | MITIGATED（已有先例）                                  |
| R-31 | **导出文件只能写到白名单目录**：主进程 `assertAllowedPath` 只放行 userData / 桌面 / 文档 / 下载 / 图片，`fs:select-save-path` **不会**把选中的目录注册为 allowed root → 保存到 `D:\其它` 会静默写失败（`saveText` 返回 false）                             | 用户在保存对话框里选了非白名单目录                                             | `saveText` 返回 false                                  | 写失败时提示「请选择桌面 / 文档 / 下载 / 图片目录下的位置」（已在 `savePresetTransferFile` 落地）                                                                                                                                                      | MITIGATED                                                                   |
| R-32 | **叠加层改动必须同步进 overlay 缓存键**：标识符是渲染时叠加的，改它**不会**更新 `preset.updatedAt` → 漏进缓存键的症状是「改了标识符，预览和产出纹丝不动」，且只在重启清缓存后才突然生效                                                                    | 新增任何「不在预设里、但影响渲染」的全局输入                                   | 改完立刻看画布是否重绘                                 | 新增即纳入 `getCompositeOverlayCacheKey`；画布 effect 依赖也要订阅该输入（`PresetCanvasEditor` 已加 `identifier`）                                                                                                                                     | MITIGATED（有单测守签名差异）                                               |
| R-33 | **长耗时 fire-and-forget 任务「点了没反应」**：手动后处理一次要读图 + 逐渠道渲染 + 体积二分压缩，几十秒内界面零变化；叠加三条静默路径（零产出不提示、防重入静默 return、执行前段异常变未处理 rejection），用户无法区分「正在跑」「跑完没产出」「按钮坏了」 | 任何 `void runXxx()` 且无加载态的长任务                                        | 点完盯着界面看 3 秒：有转圈/提示吗？                   | ① 入口立刻给「开始…」提示 + 按钮 `loading`（`runtimeStore.postprocessRunning` 计数，覆盖自动+手动）；② 结果无论产出多少**必报**（零产出给原因）；③ 防重入命中要提示而非 `return`；④ 执行体整体包 try/catch 并返回可上报结果；⑤ 单槽 toast 只发一条汇总 | MITIGATED（store.test.ts「手动后处理入口」5 例守着）                        |
| R-30 | **`test:coverage` 收尾会报一条无害的 `Unhandled Error`**：vitest 清理 `coverage/.tmp` 时被安全删除护栏拦住（`SAFE_DELETE_BULK_CONFIRM_REQUIRED`，命中 239 个文件 > 阈值 50）。**报告已正常生成**，但退出码可能非 0                                         | 本地跑 `npm run test:coverage`                                                 | 输出末尾的 `[safe-delete]` 段                          | **刻意不接进 CI**（会被误报成失败）；CI 只跑 `npm test`                                                                                                                                                                                                | OPEN                                                                        |
| R-34 | **「节点通用输出目录」会盖住全局渠道表**：层级是 节点渠道 → **节点通用** → 全局渠道 → 全局默认。某方向一旦填了通用输出目录，旗下所有渠道都写它，按渠道配好的交付目录**静默失效**（界面看不出被通用值盖住）                                                 | 同一节点既写了通用 `outputDir`，又依赖后处理设置里的全局渠道表                 | 填完通用目录后看各渠道行的占位提示是不是都变成了这个值 | 渠道行的占位必须显示**继承到的实际位置**（`ChannelOutputDirs.resolveInheritedHint` 走父链解析）；要逐渠道不同就别填通用值。**按钮/提示口径不变，别改成「渠道表优先」** —— 那会让已按方向配好的数据行为反转                                             | MITIGATED（`postprocessMedia.test.ts` / `params.test.ts` 各有用例钉住层序） |
| R-35 | **双写会放大写盘量与失败面**：一个渠道配两个位置后文件数 ×2、耗时 ×2，任一位置不可达都会产生 warning；若静默降级写到本地，用户会以为交付进了共享盘                                                                                                         | 某渠道配了 2 个导出位置                                                        | 看产出目录里两个位置是不是都有同名文件                 | 渲染只做一次（多份写盘复用同一份 `dataUrl`）；配了位置但**全都不可用**时跳过整桶并提示，绝不回退本地；只有一个可用时写可用的并报「有 N 个位置不可用」                                                                                                  | MITIGATED（`outputRoots.test.ts` 6 例）                                     |
| R-36 | **命名模板的插入位置会退化成「永远插到开头」**：点变量按钮时输入框已经失焦，此时 `selectionStart` 被浏览器归零；另外受控 input 要等渲染提交后才能 `setSelectionRange`（否则光标停在旧值末尾）                                                              | 直接读 DOM 的 `selectionStart` 实现「在光标处插入」                            | 把光标放中间点一个变量：结果是不是插在了中间？         | 光标位置记在 `useRef`（`onSelect`/`onKeyUp`/`onClick`/`onBlur`/`onChange` 都刷新）；插入后 `requestAnimationFrame` 里再 `focus()` + `setSelectionRange`                                                                                                | MITIGATED（`postprocessNaming.test.ts` 覆盖夹取与光标算式）                 |

| R-37 | **改颜色 Token 必须三处同步**：`tokensContract.test.ts` 用 dict **钉死**每个 Token 的精确 HSL 值，并交叉校验 `tokens.tokens.json`。只改 `styles.css` 会得到 7 条失败，且报错点在测试而非样式（容易误判成"测试坏了"） | 修改 `:root` / `.dark` 里任何颜色的值 | 跑 `npm test -- tokensContract` | 三处一起改：① `src/design-system/styles.css` ② `src/design-system/tokens.tokens.json`（含 sRGB `components` + `hex`）③ `tokensContract.test.ts` 的 `LIGHT_COLOR_VALUES` / `DARK_COLOR_VALUES`。HSL→sRGB 手算易错，写一次性脚本转换后即删 | MITIGATED |
| R-38 | **「等效值」假改造**：HSL 的色相/饱和度在**高亮度段**对最终 sRGB 影响极小 —— `220 20% 98%` 与 `210 20% 98%` 换算后**同为 `#f9fafb`**。只改 HSL 数字而不验算 → 代码 diff 很漂亮、界面零变化，用户会直接质疑"你改了啥" | 调色时只改色相/饱和度、亮度留在 96% 以上 | 改完立刻把新值换算成 hex，与旧值**逐条比对** | 表面层级必须靠**明度差**拉开：相邻表面 ≤ 1.05:1 肉眼不可辨，≥ 1.10:1 才明确可辨；描边需 ≥ 1.3:1。换算脚本见 runbook 第十节 | **MITIGATED**（已写进 runbook 验收步骤） |
| R-39 | **停靠面板占位不随可见性释放**：`WordLibrarySidebar` 在 `!detailAvailable` 时 `return null`（**未卸载** → `useEffect` cleanup 不触发），而副作用只按 `docked` 判定，于是 `--word-library-right-width` 被永久写成 340px。表现为界面右侧一条**莫名其妙的空白/灰边**，浅色主题下尤其显眼 | 侧栏处于 docked 状态但无详情可展示 | `getComputedStyle(documentElement).getPropertyValue('--app-docked-right-width')` 在无详情时**应为 `0px`** | **占位必须跟随可见性**：effect 的判定条件用 `detailAvailable && !compactViewport`，不要只用 `docked`。同类风险：任何「return null 但副作用已提交」的组件 | **MITIGATED**（`WordLibrarySidebar.tsx:139`） |
| R-40 | **弹窗内的「白叠白」隐形分组**：弹窗底是 `surface-raised`（≈白），里面若写 `bg-ds-surface/50`（≈白 + 半透明），分组卡**完全看不见**，且半透明会让弹窗外遮罩透进来。这类写法在浅色主题下视觉上"等于没写"，但代码里看着像设置了背景，极易漏查 | 弹窗 / 抽屉 / 面板内嵌容器用了 `bg-ds-surface/[数字]` | 弹窗内 `grep "bg-ds-surface/[0-9]"` 判断是否落在 raised 底上 | **弹窗内分区一律用下沉色 `bg-ds-surface-subtle`**（四级层级见 runbook 第十节的表）。深浅两侧都必须验算：浅 `#edeff3`↔`#ffffff` = 1.151:1、深 `#282c33`↔`#1f2228` = 1.137:1 | **MITIGATED**（2026-09-19 收口 9 个弹窗） |
| R-41 | **设计文档里的颜色表会静默过期**：`MASTER.md` §4.2 曾把 26 个色值里的 **24 个**写成过期数据（文档 `Canvas #F9FAFB` vs 实现 `#F2F4F7`、文档 `Primary #4468A7` vs 实现 `#2C59BA`）。人肉同步不可靠 —— 改 Token 时没人会想起来翻文档，于是"规范"变成误导后来者的假信息 | 修改 `styles.css` 任何 `--ds-color-*` 值 | 跑 `npm test -- docColorTable`（逐行比对 §4.2 表格与实现换算结果） | 新增 `src/design-system/docColorTable.test.ts`：从 `styles.css` 解析 HSL 通道值 → 换算 hex → 与 `MASTER.md` §4.2 表格**逐行断言**。改 Token 不同步文档 → 测试直接失败。注意该测试用 `fs.readFileSync` 而非 `?raw` 导入 CSS（后者会被 CSS 管线处理，拿不到源文本） | **MITIGATED**（20 例守着） |
| R-42 | **两套并行的圆角命名，值相同但语义不同**：裸 `rounded-lg`=8px 与 Token `rounded-ds-md`=8px **数值相同**，于是"看起来没问题"；但规范 §4.5 要求卡片/面板用 12px，而 `rounded-ds-lg` 才是 12px。混用时同一层级的卡片会呈现 8px / 12px 两种圆角，且从类名上看不出哪个是"对"的 | 给卡片 / 面板 / 输入框加圆角 | 看该元素的圆角类是否有 `-ds-` 前缀 | 语义映射表：**卡片与面板 → `rounded-ds-lg`(12px)；小控件 → `rounded-ds-md`(8px)；模态壳 → `rounded-ds-xl`(16px)**。存量 376 处裸 `rounded-*` 用棘轮快照治理（`compliance.test.ts` 的 `bareRounded`，只减不增），**不要一次性批量替换** —— 回归面太大且收益低 | **MITIGATED**（棘轮 + 逐点修正） |
| R-43 | **同一份参数被渲染逻辑复制到多处**：改前「后处理」的参数入口有 3 个（左栏每节点「参数」按钮 → 第三层弹窗、左栏「参数表格」按钮 → 工作台、右栏 6 段硬编码表单），`DIRECTION_OPTIONS` 逐字复制 2 份，`PostprocessMediaConfig`（全局）与 `PostprocessNodeOverride`（节点）8 个字段重叠。改一个字段要同步改三处，漏一处就是「在 A 改了、在 B 看不到」 | 给后处理加字段 / 改字段语义 / 改方向选项 | `grep -c "DIRECTION_OPTIONS"`；`grep -rn "跟随尺寸" src/` 应只命中 `paramSchema.ts` | 参数**唯一元数据源** = `src/features/postprocess/paramSchema.ts`：一个字段声明一次（`key`/`label`/`control`/`scope`/`group`/`resettable`/`help`），左树只给 `selectedNodeId`，右栏按 `scope` 过滤 + 按 `control` 分派渲染。`paramSchema.test.ts` 守住「键唯一 / 分组都有定义 / 无死分组 / 两种作用域覆盖完整」。**新增参数只改元数据表，不改组件** | **MITIGATED**（15 例契约测试） |
| R-44 | **`git stash` 在本机会毁掉 `.git/refs/`**：命令被 `SIGTERM` 中断时（Bash 工具吃特殊字符的老问题），`.git/refs/` 整个目录消失、`objects/pack/*.pack` 可能被删，git 随即报 `not a git repository`。**2026-09-20 实测踩中两次**（都发生在用 stash 做基线对比时）。工作区文件**不会**丢，只有 git 元数据受损 | 在本仓库执行 `git stash` / `git stash push`，且命令被中断 | 执行后 `git status` 报 `fatal: not a git repository` | **禁止用 `git stash` 做基线对比**。要对比「我的改动 vs HEAD」用只读方式：`git show HEAD:<path> > /tmp/baseline` 落盘，再 `cp` 换入换出（本仓 2026-09-20 已用此法成功对比）。**若已踩中**：① `mkdir -p .git/refs/heads .git/refs/remotes/origin .git/refs/tags` ② 从 `.git/logs/refs/heads/main` 末行取 commit hash 写回 refs ③ 若 `.pack` 缺失则 `git -c http.sslVerify=false fetch origin main` ④ `git fsck` 确认 | **OPEN**（环境级；恢复配方见 runbook） |
| R-45 | **配方卡引擎是「保真移植」，不要顺手"修"它的两个疑似笔误**：`farthestPointSampling.ts` 里 ① `enforce` 的循环变量名为 `n`，遮蔽了外层的 `const n = variables.length`，于是 `[...Array(n).keys()]` 用的是**重掷轮次**而非维度总数（轮次 0 时 sameSlots 恒空 → 只重掷第 0 槽）；② `windowFar` 只赋默认值**从未参与逻辑**（远层用完整 window，实际无界）。两条都像是笔误，但**实测影响采样分布**：把 `n` 改成维度数后，8 维池取 10 条的实测最小差异从 4 掉到 1 | 移植 / 重构 `campaignRecipe.ts` 的 `farthestPointSample` | 与原始文件对拍：同 seed 同输入下 `selections` / `signatures` / `totalAttempts` 必须逐位一致 | **逐字保留原行为**并在代码里用注释标明「这是原始行为，疑似笔误」。同理 `mix64` 的 64 位常量**不能**改写成截断后的短常量（`0x9e3779b97f4a7c15` → `0x9e3779b9` 会改变散列结果，实测 selections 全部不再一致），只能用 `eslint-disable no-loss-of-precision` 原样保留。要改这两处必须先与杰哥确认 | **REVISED（2026-09-21，杰哥拍板「全改」）** —— ①「`n` 屏蔽」**已修**：改为按维度数枚举槽位（见 R-69 —— 它会让 `selection` 被幻影槽位撑到 119 长、塞满 NaN，并把官方验收指标 `computeBatchMinDistance` 虚报成「1.000 完美」）；②「`windowFar` 死参数」**仍按原样保留**（远层用完整窗口是有意为之，本次未动）。<br>⚠️ **原文用来支撑「逐字保留、改了会退化」的那张对照表（「最小差异从 4 掉到 1」）是用 `computeBatchMinDistance` 量的，而该指标当时正被 R-69 污染，数字不可信。** 用真实槽口径复测后，两类改动的收益见 R-68：最小差异 1/13 → 6/13、违约对 9.4% → 0%。<br>另：`mix64` 的 64 位常量**依然不得改写成短常量**，但**必须改成 BigInt 精确 64 位** —— 见 R-72：float64 会吞掉入参（`x = 0` 与 `x = 1000` 同值），让基座坍缩成「每个 8 选项的槽只剩 2 个取值」。BigInt 版才是真正的逐位一致。 |
| R-46 | **弹窗不要 import 生成模块的辅助函数**：`GallerySopBatchModal` 的测试用 `vi.mock('./storeSopGeneration', () => generateMocks)` **整体替换**该模块。若弹窗新增对该模块导出函数（如 `isCampaignRecipeSop`）的调用，而 mock 未提供 → 运行时报 `xxx is not a function`，**一次弄挂该文件全部 22 个生成相关用例**（非生成用例照过，极易误判成"环境问题"） | 给 `GallerySopBatchModal` 增加「从 storeSopGeneration 导入的新函数调用」 | 该文件测试从 36 通过变成 14 通过 | **弹窗内的分流判定用内联字段判断**（`Boolean(sop.campaignRecipe) \|\| sop.executionMode === 'campaign-recipe'`），不要为省一行去 import 辅助函数。**必须 import 时，同步在测试 mock 里补上**。另：基线对比时务必确认「基线本身是不是绿的」—— 本次 22 个失败一度被误认为环境损坏 | **MITIGATED**（mock 已补 3 个生成函数 + 2 例分流测试） |
| R-47 | **新增可编辑字段必须同步 `itemDirty` 的比较列表**：`SopManagementCenter` 的 `itemDirty` 是手写枚举比较（逐字段 `!==` + 两处 `JSON.stringify`），**不是深比较**。2026-09-20 加 `campaignRecipe` 编辑器时漏比该字段 → 编辑维度后草稿**不标记为脏** → 自动保存 effect 不触发、「保存修改」按钮恒 `disabled`。界面**看不出任何异常**，表现为「改了但存不下去」，且刷新后改动消失 | 给 `SopLibraryItem` 增加可编辑字段（除 `updatedAt` / `favorite` 这类由 action 自管的） | 编辑新字段后：① `itemDirty` 仍为 `false` ② 「保存修改」按钮 `disabled` ③ 无自动保存 toast | 在 `itemDirty` 的布尔链里**补上该字段比较**（对象/数组用 `JSON.stringify(x ?? null)`，与既有 `variableMeta` / `seriesConfig` 写法一致）。同类问题：**新增 store 忘配持久化白名单 = 完全存不住而 UI 不报**；**新增排序键忘改 SQL 分页 = 第一页顺序错乱** —— 三者都是「静默失效」，新增字段时按清单逐项过一遍 | **MITIGATED**（`SopManagementCenter.test.tsx` 新增「配方卡 content 为空仍可保存」守卫用例） |
| R-48 | **新增 UI 模块必须登记 `catalog.ts`，否则 `catalog.test.ts` 直接失败**：`src/design-system/catalog.ts` 的 `legacyComponentCoverage` 与「全仓扫描出的 UI 模块清单」做**全等比较**（`expect(registered).toEqual(discovered)`）。新增一个 `src/**/*.tsx` 而忘了登记 → 测试报 `expected [93] to deeply equal [94]` 并列出缺失模块名。这是**刻意设计的棘轮**，不是 bug | 在 `src/` 下新建任何 `.tsx`（组件 / 面板 / 浮层） | `npx vitest run src/design-system/catalog.test.ts` 报数量不等并点名模块 | 在 `catalog.ts` 的 `legacyComponentCoverage` 里补一条：`{ module, responsibility（中文一句话）, decision: 'compose' \| 'retain' \| 'migrate', targets: [实际消费的共享组件名] }`。`decision` 含义：`compose` = 用共享组件拼装；`targets` 要**填真实 import 的组件**，另有用例校验其是否真的 import 了设计系统 | **OPEN**（结构性棘轮，每次新增 UI 都会遇到） |

| R-49 | **文本解析的维度名不能做大小写归一**：`campaignRecipeImport.ts` 文本分支最初用 `normalizeKey`（小写化）处理池名 → `S1` 与 `s1` 视为同一维度，但同时「裸标识符分支」与「`键: 值` 分支」都会命中，导致同一个池被建两次（`s1` + `S1`）。表现为维度数虚高、`missingPools` 误报 | 在文本解析里对维度名/池名做 `toLowerCase()` 之类的归一，或同时保留两条分支而不去重 | 解析一段含 `S1:` 的文本，看 `dimensions` 里是否出现大小写不同的同名项；`missingPools` 是否把已给的池报成缺失 | **池名保留原始大小写**（SLOT 名是大小写敏感的标识符）；去重交给 `Map` 的键；「裸标识符」分支只接受正则 `^[A-Za-z][A-Za-z0-9_]*$`。**匹配骨架占位符时用 `normalizeKey` 做宽松比较**（占位符可能带空格），但**写入维度时用原名** —— 两个方向的处理方式相反，别抄错 | **MITIGATED**（`campaignRecipeImport.test.ts` 断言池名 `['M','S1','S2','S8']` 保序保大小写） |
| R-50 | **单花括号 `{M}` 与双花括号 `{{S1}}` 必须"先双后单"扫描**：引擎原本只认 `{{...}}`（项目标准），而外部真实资产用的是单花括号 `{M}`。若两个正则同时无差别扫描，`{{S1}}` 会同时被单括号规则匹配到内层的 `{S1}` → 同一槽位产生**两个同名维度**、组合空间虚高 | 给 `renderRecipeBody` / `extractPlaceholders` 增加单花括号支持 | 用含 `{{X}}` 的骨架跑 `extractPlaceholders`，看返回数组里有没有重复的 `X` | 扫描顺序：**先 `matchAll` 双花括号并记录**，再把双花括号替换成哨兵字符（`\u0000`）后在剩余文本上扫单花括号。渲染时单花括号替换要**保留未知名占位符原样**（`value === undefined ? marker : value`），不做删除 —— 静默删除会让用户永远发现不了槽位名写错 | **MITIGATED**（`extractPlaceholders` 用例 + 真实资产 13 维度无重复） |

| R-51 | **同一个「可否保存」门槛被复制到三处，漏改一处就静默失败**：`content` 为空的配方卡是合法状态（骨架在 `campaignRecipe` 里）。前一轮放宽门槛时改了 `itemDraftValid` 与自动保存 effect，**唯独漏了 `saveItemDraftNow`**。后果：配方卡草稿永远不会被「保存修改 / 切换前保存」写入 → `runAfterDraftConfirmation` 拿 `false` → 弹「放弃未保存的修改？」→ 用户点确认后改动丢失。**2026-09-20 实测：编辑维度后无论如何都存不下** | 新增一种「某个字段可以为空」的 SOP 类型，或调整保存门槛 | 编辑该类型草稿后点「保存修改」：`onSaveItem` 是否被调用？是否弹「放弃未保存的修改？」 | 门槛**收敛为单一表达式**并三处共用（`itemDraftValid` / 自动保存 effect / `saveItemDraftNow`）。新增类型时**全文搜索 `content.trim()`**，逐个确认是否要加类型分支；`SopManagementCenter.test.tsx` 已加回归用例（回退后测试必失败） | **MITIGATED**（3 例回归测试，含反向验证） |
| R-52 | **草稿同步 effect 会静默换卡，导致「用配方卡却走了 AI」**：`SopManagementCenter` 里 `useEffect([filteredItems, selectedItemId])` 在选中项不在**过滤后**列表时，会 `setSelectedItemId(filteredItems[0])` + `setItemDraft(filteredItems[0])`。搜索框只匹配 `name/description/content`（**不匹配 `campaignRecipe`**），所以一旦搜索词残留，刚建好的配方卡就被换成列表第一条（往往是普通 SOP）→ 随后批量弹窗按**普通 SOP** 分流 → 调 AI 大模型生成。界面看不出异常，用户只看到「配方卡也去调 AI 了」 | 搜索框有残留文字 / 分组过滤，且当前选中的 SOP 被过滤掉 | 选中配方卡后在搜索框输入只命中普通 SOP 的词：配方卡编辑器是否消失、选中项是否被换掉 | effect 里加**全量兜底**：`if (selectedItemId && items.some(i => i.id === selectedItemId)) return` —— 选中项仍存在于全量列表（只是被过滤）时保留选中与草稿不动。`SopManagementCenter.test.tsx` 已加回归用例。**同类隐患**：任何「找不到就 fallback 到第一条」的 effect 都要问一句「找不到是因为真没了，还是只是被过滤了」 | **MITIGATED**（回归用例 + 反向验证） |
| R-53 | **分流判定与生成引擎的「取配置口径」不一致**：`generateCampaignRecipePromptsFromStore` 用 `sop.campaignRecipe ?? parseCampaignRecipeConfigFromContent(sop.content)`（认识「content 直接放 JSON」的手工资产），但弹窗分流只判 `Boolean(campaignRecipe) \|\| executionMode === 'campaign-recipe'` → 同一张卡「引擎能认出、分流认不出」→ 被判成普通 SOP 走 AI，引擎的兜底永远没机会执行 | 修改任一处的配方卡识别条件 / 给引擎加新的配置来源 | 造一张只有 JSON `content`、没有 `campaignRecipe` 字段的卡，看弹窗是否调了 AI 生成函数 | **两处口径必须同步**：给引擎加配置来源时，同步在弹窗分流里补上同规则的内联探测（不要从 `storeSopGeneration` import 辅助函数，会踩 R-46 的 mock 坑）。`GallerySopBatchModal.test.tsx` 已加「JSON 正文配方卡」回归用例 | **MITIGATED**（回归用例 + 反向验证） |
| R-54 | **本地引擎跑出来的 run 快照会带上一个「本次没用到的」文本模型名，界面显示成「文本模型 gemini-…」→ 用户据此误判成走了 AI**。三处叠加：① `generateForSources` **无条件**写 `activePromptGenerationModelRef.current = getSopPromptGenerationModelFromStore()`（该函数只是读 `profile.model \|\| settings.model`，**不发请求**）；② `buildPromptRunSnapshot` 的取值链最后有 `previous?.promptGenerationModel` **粘性回退** —— 历史 run 一旦被写进模型名，光改 ref 洗不掉；③ 界面把该字段裸渲染成「· 文本模型 xxx」，没有本地/远端区分 | 任何本地算法分支（配方卡 / 变量提示词）走完一次生成 | 用配方卡生成后看提示词集头部：是否出现「文本模型 xxx」。DB 里查该 run 的 `promptGenerationModel` 是否有值 | ① 记录模型名前先用**与分流同一口径**的 `isLocalGenerationSopForSop` 判定，本地分支写 `''`；② `buildPromptRunSnapshot` 里对本地分支**直接返回 `undefined`**，挡掉 `previous` 粘性回退；③ 界面在无模型名但当前 SOP 是本地引擎时显示「· 本地引擎（不调用 AI）」。**判定口径已收口到模块级 `isLocalGenerationSopForSop`**（与 R-53 同源，别再各写一份） | **MITIGATED**（回归用例断言落盘快照不含模型名） |
| R-55 | _（预留：init effect 中止在途生成后未结算状态 —— 经实测**证伪**，取消分支由 `generateForSources` 自身收尾，不单独登记）_ | — | — | — | CLOSED |
| R-56 | **`generateForSources` 开头会 abort 上一轮，被 abort 的那一轮会留下「孤儿 `generating` 快照」，界面永久停在「生成中 · 0 条提示词」**：`generateForSources` 第一件事是 `generationAbortRef.current?.abort(...)`；而每轮的起点会 `persistPromptRun(..., 'generating')`，其快照 id = 当轮的 `progressiveSnapshotId`。一旦同帧二次进入，前一轮就带着 `status: 'generating'`、`promptCount: 0`、`taskIds: []` 落盘，且它的 id 已 ≠ `activeRunIdRef.current` → **后续所有收尾分支都只更新新一轮，这个孤儿快照永远不会被覆盖**。触发源是异步 `running`（来自 `status`）挡不住同步重入：自动启动 effect 与用户点击都能调它。**这是从豆泡 fork 继承的存量缺陷（`git log -S "已开始新的提示词生成"` 指向 rebrand 提交），2026-09-20 用配方卡时暴露**——本地引擎瞬间出词，「0 条」特别刺眼 | 同帧内二次进入 `generateForSources`（自动启动 / 快速点击 / 宿主回写 `autoStart`） | 提示词集头部卡在「生成中 · 0 条提示词」；DB 查 `sopBatchSnapshots`：`status='generating'` 且 `promptCount=0` 且 `taskIds=[]`。**判据**：早期（9/18，尚无配方卡）的普通 SOP 运行**也是 `promptCount: 0`** → 说明与配方卡无关，是存量问题 | 用 **ref 做同步重入闸**（`generateInFlightRef`），比依赖 `status` 的时序可靠：`if (generateInFlightRef.current) return`，外层 `try/finally` 复位。同时把函数体拆成 `runGenerateForSources` 保持可读。**推论**：任何「开头 abort 上一轮 + 先写一个非终态快照」的长任务都要自查这件事，终态快照必须能覆盖所有已创建的 run id | **MITIGATED**（回归用例断言全程只调用一次生成） |
| R-57 | **`generateForSources` 的重入闸是「静默 return」，把用户点击吞掉、并把上一轮的 `generating` 状态永久留在界面上**：R-56 的修法是 `if (generateInFlightRef.current) return` —— 挡住了破坏，但**挡的过程完全没有反馈**：不报错、不 toast、不落终止态快照。用户看到的是「点了生成，界面还停在上一轮的『生成中 · 0 条提示词』」，判断成「生成坏了 / 根本没接上引擎」。2026-09-20 上午报障截图正是这个形态（快照 `updated_at` 停在 10:25:52，之后 15 分钟无写入，`status` 仍是 `generating`） | 上一轮生成尚未结束（或已挂死）时再次点击生成 / 自动启动 effect 二次进入 | 点「生成」后 3 秒内看 `sopBatchSnapshots` 的 `updated_at` 是否推进；界面是否停在 `generating` 而不变化 | ① 闸门分支改为**显式反馈**：`setStatusMessage(...)` + `showToast('上一轮提示词生成尚未结束，请稍候', 'info')`；② **挂载时收敛库里残留的非终态快照** —— 在途生成只存在内存 ref 里，弹窗挂载即证明无在途，把 `status='generating'` 就地收敛成 `'ready'` 并写回（`orphanRunsToFlush` 登记 + 应用时/兜底 flush），保证库里不留 `generating` 孤儿。<br>**通用防线**：任何「ref 重入闸 + 非终态落盘」的组合，闸门分支必须能收敛到终止态 | **MITIGATED**（2 例回归测试：闸门 toast + 挂载收敛，均过反向验证） |
| R-58 | **`SopBatchSnapshot['sop']` 的类型与构造只保留 `id/name/description/content`，把本地引擎判定所需的字段裁掉了**：`buildPromptRunSnapshot` 写快照时 `sop` 是**显式字面量**，只带这四个字段。而读回侧（`applyPromptRun` 的残留净化、`buildPromptRunSnapshot` 自身的模型名挡板）都靠 `isLocalGenerationSopForSop(run.sop)` 判定 —— 它需要 `campaignRecipe` 或 `executionMode`；两者都没有时只能退回「`content` 以 `{` 开头」的内联探测，而配方卡的骨架在 `campaignRecipe.body`、`content` 往往是空串或普通骨架文本 → **判定恒为 false**。后果是 **R-54 的整套修复从落地起就是失效的**：模型名挡板不生效、残留永远洗不掉，配方卡一直显示「文本模型 gemini-…」 | 任何把本地引擎 SOP 落盘成快照后再读回的路径（**每次生成、每次开弹窗**） | 用配方卡生成后查 DB：该 run 的 `promptGenerationModel` 是否有值（本不该有）。或看快照 `sop` 的键：是否缺 `campaignRecipe` | ① `SopBatchSnapshot['sop']` 类型补 `campaignRecipe?` / `executionMode?`（`src/types.ts`）；② `buildPromptRunSnapshot` 原样带上这两个字段。**通用防线**：**读回侧的判定依赖哪些字段，写盘侧就必须保留哪些字段** —— 快照裁剪字段时先问「有没有下游逻辑在读它」。<br>**测试陷阱（同源教训）**：mock 的 `putSopBatchSnapshot` 若透传对象引用、不做序列化边界，写盘侧漏字段读回后**依然存在**，测试会假绿（本条的首次回归用例就是这样被反向验证抓出来的） | **MITIGATED**（回归用例断言落盘快照仍带 `campaignRecipe`，过反向验证） |
| R-59 | **`start.bat` 只做 `where node.exe` 的存在性检查、不验版本，PATH 里 v22 排在 v24 前面时必然启动失败，且失败原因被大字报表吞掉**：本机同时存在 v22（`C:\Users\tt\.workbuddy\binaries\node\versions\22.22.2-2\`，WorkBuddy 托管）与 v24（`C:\Program Files\nodejs\`），**v22 在 PATH 里靠前**。`start.bat` 原逻辑 `where node.exe` → 命中 v22 → `npm run dev` → `vite.config.ts` 的 `MIN_NODE_MAJOR = 24` 守卫抛错。表现为「双击 start.bat，窗口一闪而过 / 只留一句报错」——**用户看到的是「起不来」，看不到「Node 版本不对」** | 任何 PATH 中 v22 优先于 v24 的机器（WorkBuddy 托管环境默认如此） | 直接跑 `node --version`（拿到 v22）+ `node node_modules/vite/bin/vite.js` → 报 `[糖包] Node 版本过低`。**注意 `vite --version` 不加载配置、不会报错**，只有真正 `dev`/`build` 才命中守卫 | `start.bat` 改为**按版本挑 Node**：① **遍历 `where node.exe` 的每一行**、逐个用 `node.exe --version` 验主版本 ≥ 24（**不要**去拼 `%ProgramFiles%\nodejs` 候选路径 —— `for %%D in ("%VAR%\x")` 括号内不做变量展开，且 `%ProgramFiles%` 在继承环境里可能为空，实测展开成 `\nodejs`）；② 命中则用**绝对路径**调 npm，并把 Node 目录提到 `PATH` 最前（防派生的 vite/electron 子进程回落 v22 —— 实测 `npm test` 阶段就栽在这）；③ 都不满足才报错退出，且错误文案里带上「当前 PATH 的版本」与「把 v24 提到 PATH 前面」的指引。<br>**通用防线**：启动脚本对运行时版本的检查必须**比对版本号**，不能只判断「命令存在」；且检查逻辑要与构建期守卫（`vite.config.ts`）的阈值保持同一个数字 | **MITIGATED**（`start.bat` 已改为版本探测 + 绝对路径 + PATH 前置；沿用 `MIN_NODE_MAJOR = 24` 阈值。改这段前先读 `docs/tangbao-ops-runbook.md` §18） |
| R-61 | **`.bat` 被「编码 / 行尾」静默破坏，症状与「脚本逻辑写错」无法区分**：cmd.exe 按**当前活动代码页**（本机 936）读取**整个**批处理文件，且要求 **CRLF** 行尾。① 文件以 UTF-8（无 BOM）落盘 → 中文被按 GBK 解成乱码，乱码字节含引号/括号时**碎片会被当命令执行**，满屏 `'pansion' 不是内部或外部命令`；② 文件**只有 LF** → cmd 把多行**拼成一行**，症状几乎一样但更难认；③ 反过来，若用 GBK 落盘却又 `chcp 65001`，切页**之后**的行照样乱码。另有一层：`.gitattributes` 的 `* text=auto eol=lf` 会在 `git add` 时**把 CRLF 翻成 LF** → 本地明明能跑，别人 clone 下来就坏 | 任何手改 / 工具生成的 `.bat`、`.cmd`；尤其是「本地修好了，一提交就坏」的场景 | `git add` 时的 `warning: in the project copy of 'start.bat', CRLF will be replaced by LF` **就是它**，别当噪音忽略。自查：`xxd start.bat \| head` 看行尾是否 `0d0a`、中文处是否 GBK 字节；`grep -c " 3f"` 数 `?` 残留 | 三条硬约束同时满足：**GBK(936) 落盘 + CRLF 行尾 + 不加 `chcp 65001`**；`.gitattributes` 里对 `.bat`/`.cmd` 用 **`-text`**（二进制，不做任何转换 —— 不要写 `text eol=crlf`）。为免手改 GBK 文件反复踩坑，改用 **UTF-8 单一真相源 + 构建脚本**：`scripts/start.bat.utf8-source.txt` → `scripts/build-start-bat.mjs`（iconv-lite 转 GBK+CRLF，并自检 GBK 表达不了的字符）。<br>**转换脚本只能对 UTF-8 源跑一次**：把已是 GBK 的文件按 utf8 读会得到一堆 `?`（0x3f），中文**永久丢失** | **MITIGATED**（源/产物分离 + `.gitattributes` 加 `*.bat -text`；配方见 `docs/tangbao-ops-runbook.md` §18） |
| R-62 | **主进程 IPC 路径白名单与「导出到业务盘」的诉求直接冲突，且失败原因被吞成布尔值**：`getAllowedRoots()`（`electron/ipc-handlers.ts:241-253`）只放行 userData / 桌面 / 文档 / 下载 / 图片 + `localSavePath` + `sessionAllowedRoots`；`D:\…`、`E:\…`、`C:\Users\Public\…` 一律被 `assertAllowedPath` 拒绝。**两处不对称**：① 只有走 `fs:select-directory` 对话框选的目录才会 `addAllowedRoot`（`ipc-handlers.ts:1104`），**在输入框手敲同样路径永不被授权**；② `sessionAllowedRoots` 是内存 `Set`（`ipc-handlers.ts:34`），**重启即清空** → 昨天能出图、今天不能。链路末端 `outputRoots.ts:47` 弹「请检查路径是否可达」，**文案与真因不符**（路径明明可达，是不在白名单里），把人往错方向引 | ① 任何把输出目录配到 D 盘/E 盘/自定义目录的用户；② 手输路径（不用「选择…」按钮）；③ 跨重启后复用上次的目录 | 复刻 `assertAllowedPath` 喂路径：`D:\投放大图` → `Path is outside allowed application directories`；端到端跑 `resolveBucketOutputRoots` → `roots=[]` + 截图那句提示。<br>**自查一句话**：在资源管理器里打得开的目录，糖包却报「不可达」→ 就是本条 | **A（必做）**：把「用户显式配置的输出目录」纳入白名单 —— 从 `postprocessMedia` 读 `outputDir` + `mediaOutputDirs` 一并 `addAllowedRoot`。这不是拆白名单，而是与既有 `localSavePath`（`ipc-handlers.ts:251`）**完全一致**的处理：用户仍不能写任意路径，只是「他自己配过的目录」被承认。<br>**B（必做）**：`fs:ensure-dir` 不要再 `catch → return false`；把 `assertAllowedPath` 的错误传到渲染侧，文案区分「不在允许范围」vs「不可写」。<br>**C**：输出目录控件加实时可用性校验，别等跑完任务才从 toast 知道。<br>**D**：`sessionAllowedRoots` 持久化到 `local-settings.json`，启动读回，消除「重启失效」。 | **OPEN → 已缓解**（2026-09-20：A/B 两条已实现，见 `docs/BACKLOG.md` TB-049「已实现」节；仍待实机验证一次） |

> **2026-09-20 修订（重要）**：原先写的修法 A 是「从 `postprocessMedia` 读配置一并 `addAllowedRoot`」，
> **实际实现不是这样，而且更好**：授权函数 `authorizeCompositeOutputDirectory` **早就存在**，
> 只是调用点在 `ensureDir` **之后**（`taskPostprocess.ts` 的 `for (const root of outputRoots) await api.authorize…`）
> —— `ensureDir` 一失败，`resolveBucketOutputRoots` 已经返回空数组，**那行永远等不到**。
>
> **所以这是个顺序 bug，不是缺功能。** 修法 = 把授权挪到 `ensureDir` 之前，
> 由渲染进程主动把用户填的路径提交授权（不需要主进程去理解 `postprocessMedia` 的存储结构，避免耦合）。
> **`getAllowedRoots()` 未改动**，仍是原样的安全边界。
>
> 顺带修掉「真因被吞」：`fs:ensure-dir` 失败时返回错误消息**字符串**而不是 `false`（B 条）。
>
> **「重启失效」这条已不成立**：现在是每次产出前重新授权（`resolveOutputRootCached` 的缓存
> 只在单批内有效），所以重启后照常出图 —— 原 D 条（持久化）**不再必要**。
>
> 教训：`R-62` 原文把链路末端的 `outputRoots.ts:47` 当成「文案与真因不符」，
> 但没往上追一步看**授权那行的位置**。**「功能明明有却完全不起作用」时，先看调用顺序，再看实现有无。**
> **自查一句话**：在资源管理器里打得开的目录，糖包却报「不可达」→ 就是本条 | **OPEN → 已缓解**（2026-09-20：A/B 两条已实现，见 `docs/BACKLOG.md` TB-049「已实现」节；仍待实机验证一次） |
> | R-63 | **参数字段集收窄时，旧数据里的节点级值会静默消失**：`PostprocessNodeOverride` 已从 10 字段收到 3 字段（`TB-050` / ADR-0011），被上收全局的有 `namePattern` / `creator` / `autoCompanionClean`（纯净版伴随）/ `distribution`（分发排期）/ `direction` / `selectedMediaIds`。这些字段的**可覆盖语义**一旦从类型里消失，归一化（`normalizePostprocessNodeOverride`）就不再读它们，用户**已在节点上配好的命名模板 / 排期 / 伴随开关会凭空消失**，而界面不报任何错 —— 与 R-47（`itemDirty` 漏字段）/ R-07（漏配白名单）同属「改动静默失效」家族，但这次的失效发生在**升级瞬间**、且**不可逆**（旧值被新一次写盘覆盖） | ① 给 `PostprocessNodeOverride` 删字段；② 任何把「节点级可覆盖」降级为「仅全局」的重构 | 手工构造一份含节点级 `distribution` 的 `projectTreeParams` 落盘数据 → 升级后检查该配置是否还生效。<br>**自查一句话**：删类型字段前先问「用户之前能不能在这里配它」→ 能，就是要迁移 | **已实现（2026-09-20）**：① `storeProjectTreeParams` **`version: 1 → 2`** —— 不 bump 则 zustand 不调 `migrate`，等于没迁；② 迁移时 `collectPromotedNodeFieldValues(rawParams)` 从**原始数据**（归一化前，否则字段已丢）收集旧值，存进参数层 store 自己的 `promotedGlobals` slice（**不跨 store 写**：`migrate` 是同步纯函数）；③ 消费方 `mergePromotedGlobals` 合并，**两个点都要接** —— `PostprocessSettingsModal`（界面）与 `runTaskPostprocess`（运行时落盘），只接界面会出现「看着对、产出错」；④ **只补空缺**：基线已有值不被迁移值覆盖，「空缺」判定按类型分 —— 字符串**空串也算空缺**（`creator: ''` 就是没填过），布尔/对象只有 `undefined` 算空缺（`autoCompanionClean: false` 是有效设置） | **MITIGATED**（`docs/adr/0011-node-override-narrowing.md`；7 个回归用例含边界，逐个反向验证通过）<br>⚠️ **2026-09-21 第二个实例（渠道 `enabled` 字段删除，ADR-0013）**：与节点字段不同，这次的失效点在**媒体表**上 —— 旧数据里 `enabled: false` 的渠道**本来不产出**，字段一删、代码不再看它，它就会**悄悄重新开始产出**（不是「丢了一个开关」，而是「产出了一批本不该有的文件」）。同族但更隐蔽。<br>通用口径已上收 `docs/architecture-constraints.md` §4.2.1：**删字段 = ① `persist.version` bump ② 旧值折成它当时表达的语义再丢**；两件都做才算迁完（只做①是空转，只做②不跑 migrate 是空想） |
> | R-60 | **两处「密钥静默失效」面：Key 还原失败零提示 + 空 Key 不前置拦截**：① `hydrateDesktopApiSecrets`（`src/store.ts`）在 `safeStorage.isEncryptionAvailable() === false` 时**裸 `return`** —— 读不回已存的 Key，界面一个字都不提示，用户只看到「所有 API Key 都空了」，且此时**不能继续执行**（后续 `saveApiSecrets(extractApiSecrets(settings))` 会把空的 secrets 写回去，等于覆盖掉唯一一份真值）；② `openaiCompatibleImageApi.createRequestHeaders` 无条件拼 `Bearer ${profile.apiKey}`，空 Key 时照常发出 → 服务端回 401，用户看到「请求失败/未授权」，根本不知道真正原因是**本地没填 Key**（R-03 的副作用：`stripApiSecrets` 剥离后若还原失败，profile 看起来是配好的，`apiKey` 却是空串） | ① 换机器 / 重装系统 / DPAPI 不可用；② profile 的 Key 为空（还原失败、或用户忘了填）就点生图 | ① 看控制台是否有 `[api-secrets] 系统密钥库不可用`；② 空 Key 时点生图，若报的是 401 / 「请求失败」而不是「未配置 API Key」即为本条 | ① 新增 `notifyApiSecretsUnavailable()`，走 `tangbao:persist-error` 通道（namespace 用**独立的 `apiSecretsUnavailable`**，因为 `apiSecrets` 的文案是「正在自动重试」而这种情况重试无用），`App.tsx` 给「系统密钥库不可用，已保存的 API Key 无法读取，请在设置里重新填写」；② 新增 `assertOpenAICompatibleApiKey(profile)`，在**发请求前**抛出可读文案。<br>⚠️ **闸门位置**：只能放在 OpenAI 官方两条路径（`callImagesApiSingle` / `callResponsesImageApiSingle`），**不能塞进 `createRequestHeaders`** —— 那个函数还被 `submitCustomRequest` / `pollCustomTaskResult` 复用，自定义 provider 的鉴权由用户自己在映射里配，在共用函数里加闸门会误伤（首次实现就踩了这个坑，被 2 个既有测试挡住） | **MITIGATED**（2 例回归：不可用必须上报且不覆盖真值 / 空 Key 必须抛未配置而非 401；均过反向验证） |
> | R-64 | **「取消提示词生成」按了没反应：渐进派发把界面卡死在不可取消的提交 await 上，且模型报错被换成通用文案**。两处独立缺陷、叠加成同一个用户症状：<br>① **取消传不进去**：`dispatchProgressivePrompt` 里 `await submitTaskWithData(...)` —— 提交链路（`putTask` 写库 + 建任务）**不接受 AbortSignal**，但它是 `onBatch` 里的 `await`，而 `onBatch` 又被 `generateSopPromptBatches` 的 `await options.onBatch(...)` 等着。链路一路传导：`generatePromptsFromSopStore` 永不返回 → `runGenerateForSources` 的 catch 走不到 → 取消分支（`generationCancelled`）永远不执行 → `status` 永久停在 `'generating'`，**取消按钮不消失、界面停在「正在取消提示词生成」**。<br>② **真因被吞**：`collect` 侧把模型/接口报错压成「提示词生成中断，可重试缺口。」，服务端原文（如「分组 专用gemini 下模型 gemini-3.1-pro-preview 的可用渠道不存在」）只存在 `SourceRun.error` 里、**界面一个字都不显示** | ① 开着「自动生成」（`progressiveDispatch = autoGenerate \|\| generateImagesForNewPrompts`）时长任务在途时点取消；② 任何模型渠道不可用 / 接口拒绝的失败 | ① 点「取消」后 3 秒内看：取消按钮是否消失、文案是否离开「正在取消提示词生成」。**不消失即为本条**；② 生成失败时界面是否只给「生成中断」而不含服务端原文 | ① 抽 `raceWithCancellation(task, signal)`（`sopPromptBatch.ts`）：自身不响应取消的长任务与信号**竞速**，取消即立刻 reject 取消原因，原任务丢后台自行收尾。用在提交处，并**把 catch 改成先 `throwIfSopPromptGenerationAborted` 再计入失败** —— 否则取消会被当成「提交失败」吞掉，循环继续跑下一轮，同样走不到取消分支。<br>② 抽 `collectSourceFailureReason` / `collectOutcomeFailureReason`，把失败项原文原样带进 `setError`，**不改写措辞**。<br>**通用防线**：任何「在批次循环里 await 一个不接 signal 的副作用」都是取消黑洞 —— 要么让副作用接 signal，要么用竞速把它包起来。**且循环里的 catch 必须先判取消再判失败。** | **MITIGATED**（6 例回归：在途取消必须收口/竞速五态/真因透传，逐个反向验证通过） |
> | R-65 | **重建对象式清洗丢字段 → 主控槽全线静默失效**。`sanitizeCampaignRecipeConfig`（`campaignRecipe.ts`）用 `return { name: dimension.name, options }` 重建维度，**把 `weight` 抹掉**（本意只是剔除命中红线的候选值）。后果分岔两条、**都不报错**：<br>① `pickDominantIndices` 返回 **`undefined`**（不是空数组）→ `resolveEffectiveDominantSlots` 的 `if (!indices) return []` → 界面「主控槽」整片消失（易被当成「本来就没有」）；<br>② `farthestPointSample:395` 的 `options.dominantIndices ?? variables.map((_, i) => i)` **兜底成「所有槽都是主控槽」** → `minDomNear/Far = 2` 从「只对主控槽下狠手」退化成「全槽平等」→ **多样性策略被静默改写**（引擎照常出图、数量照常）。<br>实测铁证：用户真实配方卡 `sop-mu96iez8-4y7cho`（13 维）原始 `weight` 为 `S1=2,S3=2,S4=2,S8=3,M=2`，sanitize 后**全部变成 `undefined`** | ① 任何「重建对象」的函数（sanitize / normalize / map / adapt）没有全量透传字段；② 给 `CampaignRecipeDimension` 新增字段；③ 下游用 `??` 兜底一个「本该有值」的结果 | 拿真实配方卡跑 `sanitizeCampaignRecipeConfig` → 打印 `dimensions.map(d => [d.name, d.weight])`，全 `undefined` 即命中。**自查一句话**：写 `x ?? fallback` 前问 —— `x` 为 `undefined` 是合法输入，还是上游坏了？后者应断言/抛错而非兜底 | ① 改成 `return { ...dimension, options }` —— **清洗只删候选值，不动维度元数据**；② 新增回归用例「清洗保留维度的 weight」：既断中间态 `expect(...).toEqual([3, 2])`，也端到端断 `pickDominantIndices(...) === [0]`（**只断最终产出会漏**，因为兜底掩盖了失效）。<br>**通用防线**：`??` 兜底默认值出现在「本该有值」的位置是静默失效高发点；`weight?: number` 这类可选类型让「被抹掉」与「本来没有」在类型层不可分，只能靠测试钉住 | **MITIGATED**（反向验证：改回旧写法 → `AssertionError: expected [undefined, undefined] to deeply equal [3, 2]`，精确 1 例失败；修复后 40/40 全绿） |
> | R-66 | **「从渲染后文本反推结构」的口径与「渲染」的口径不一致 → 跨批次去重静默失效，提示词重复率飙升**。`deriveUsedSignatures`（`campaignRecipe.ts`）判定「正文引用了哪些维度」时写死 `body.includes('{{' + name + '}}')`，**只认双花括号**；而外部真实资产的骨架用的是**单花括号**（`{M}` / `{S9风格}`，见 R-50 与 `campaignRecipeImport.ts:213`）。后果：`usedInBody` **恒为空集** → 每个维度都走 `parts.push('*')` 通配分支 → **整批历史塌缩成同一条签名** `*|*|*|*|*|*|*|*|*|*|*|*|*`，它与任何真实签名（选项名拼串）都不相等 ⇒ `usedSignatures` 里躺着一条**永不命中的假签名**，跨批次去重等于不存在。用户症状是「连点两次生成，第二批和第一批一模一样」<br>**实测对照（13 维 × 6 候选 × 10 条，只改花括号写法）**：双花括号 → 反推签名 10 个、**批间重复 0/10**；单花括号 → 反推签名 **1 个（假签名）**、**批间重复 10/10（一字不差）**。单批内部质量始终完好（两两最小差异 1.000 = 13/13 槽全不同）⇒ **最远点采样本身没坏，坏的是喂给它的去重输入** | ① 任何「把渲染后的成品文本反推回结构化数据」的函数（反推签名 / 反查来源 / 导入识别）；② 新建或移植一处**新的**占位符扫描时口径分叉；③ 骨架由外部资产导入（单花括号是常态）而非编辑器新建（双花括号） | 拿单花括号骨架的配方卡跑 `deriveUsedSignatures(config, 第一批prompts)` → 返回 `1`（内容形如 `*|*|…`）即命中；正常应等于历史条数。**自查一句话**：写「从文本反推」的逻辑前，先问「它是按哪套占位符写法反推的，和 `renderRecipeBody` 认的是同一套吗？」 | ① 把两条扫描正则提为模块常量 `RECIPE_DOUBLE_BRACE_PATTERN` / `RECIPE_SINGLE_BRACE_PATTERN`，**渲染与反推共用同一份**（口径唯一，防再次分叉）；② 新增 `referencedDimensionNames(body)`（先双后单 + 哨兵，与 `extractPlaceholders` 同序，避免 R-50 的重复识别），`usedInBody` 改用它判定；③ 3 条回归用例（逐条真签名 / 同 seed 批间零重复 / 单双混写与通配不误判）；④ **不能复用 `extractPlaceholders`**：依赖方向是 `campaignRecipeImport → campaignRecipe`，反向引用成环 | **MITIGATED**（反向验证：把 `usedInBody` 改回 `body.includes(\`{{name}}\`)`→ **精确 3 例失败、其余 56 例全绿**；修复后 59/59） |
| R-67 | **组合穷尽时的两条「静默重复」通路（与 R-66 同族，未修）**：<br>①`farthestPointSample`的签名重掷只有`while (used.has(signature) && guard < 40)`，**40 轮耗尽后直接 `selections.push(candidate)`放行重复项** —— 既不抛错也不把该条计入`exhausted`，与代码注释宣称的「组合耗尽时宁可少给也不重复（`exhausted: true`+ console.warn）」**不符**；<br>②`generateCampaignRecipeBatch:899`的文本兜底`existing.has(prompt) → continue`**只过滤不重采样**，被挡掉的条数不会补回来，挡空后抛「**配方卡候选组合已耗尽：请增加维度候选值，或减少生成数量**」—— 而真实资产那张 13 维卡组合空间是 2.97 万亿，**报错文案与真因方向相反**，会把排查带偏（真因常是 R-66 让采样器没避开历史） | ① 候选组合空间小于「历史已用 + 本次请求」；② 弹窗里留着上一批提示词且采样器未避开（R-66 未修时必然发生） | 小池（如 2×2=4 组合、要 6 条）连跑两批，或同 seed 复跑 → 第二批出现与第一批重复的条目即命中①；生成数量少于请求且报「候选组合已耗尽」，但实际组合空间远大于请求即命中②。**自查一句话**：看到「候选组合已耗尽」先算一下组合空间 —— 若远大于请求数，先怀疑去重链路而不是池子小 | ① 重掷耗尽应标记该条为「未去重成功」并由调用方决定（降级为少给 / 上报），不要静默放行；② 文本兜底命中也应回填重采样而不是直接`continue`；③ 报错文案要与真因对齐（区分「池子真的小」与「去重没生效」）。**改动前先看 `campaignRecipe.ts`里`farthestPointSample` 的 JSDoc 对照表** —— 该函数有过「看着像 bug、实测是多样性来源」的先例，动它必须先补对照组 | **OPEN**（2026-09-21 排查 R-66 时发现，未修；真实资产组合空间极大，当前不构成实际影响） |
| R-68 | **⭐ 主控槽下限「恒不满足」把「总差异」约束整个短路掉 → 最远点采样的核心约束从未生效（已实测，未修，需杰哥拍板）**。`farthestPointSample.enforce`的判据是`if (domDiff(cand, point, dom) < minDomFar \|\| hamming(cand, point) < minTotalFar)`—— **或**关系，而`domDiff`的上限就是主控槽个数：真实资产`pickDominantIndices` 只取权重**并列最高**者（`S8=3`），主控槽仅 1 个 ⇒ `domDiff ∈ {0,1}`<`minDomFar(2)`**恒真** ⇒`target` 恒被设置 ⇒ **`hamming`那一侧被短路、永不参与判断** ⇒ 每轮都重掷、打满`maxAttempt(120)`⇒ 循环退出时**候选仍未满足总差异约束，却被原样 push 进结果**（且无`exhausted`、无告警，见 R-67①）。**`minTotalNear`/`minTotalFar` 事实上是死参数。**<br>**实测（13 维 × 6 候选，只统计真实 13 个槽）**：<br>· 现状（主控槽=[S8]）：30 条最小差异 **1/13**、**9.4% 的组合对差异 <6**、重掷 3481<br>· 主控槽改成「用户本意」（全部带 weight 的 5 个：`S1,S3,S4,S8,M`）：最小 **6/13**、**0% 违约**、重掷 **126（快 27 倍）**<br>· **把 `minTotalFar`从 6 提到 10：指标一模一样（1/13、41 对、3481 重掷）—— 直接证明该参数被短路**<br>· 把`minDomNear/Far`降到 1（对单槽即可达）：违约率 9.4% → **0.9%**<br>· 违约率随批量增长：10 条 4.4% → 30 条 9.7% → 50 条 11.4% → 100 条 **12.5%**（即每 8 对里有 1 对「一半以上槽完全相同」）<br>**⚠️ 本条推翻了 R-45 与`farthestPointSample`JSDoc 里那两张对照表的结论**（「改成`min(2, 主控槽数)`是净亏」「全槽最小差异 7.00 vs 3.10」）：那些数字全部由`computeBatchMinDistance`量出，而该指标被 R-69 的 NaN 污染架空（虚报差异），**结论不可用**；用真实槽口径重测，结论**相反**（现状更差） | ① 配方卡的`weight`有多个非并列最高的值（用户认为「这些都是重点」）；② 批量条数较大（>30）；③ 任何让`minDom`> 主控槽个数的参数组合 | 用**真实槽**口径量（**不要用`computeBatchMinDistance`**，它被 R-69 污染）：对 `samples[].selection`只取「下标 < 维度数」的槽两两算差异，统计「最小差异」与「差异 <`minTotalFar`的对数」。**自查一句话**：把`minTotalFar` 调大后指标**一点不变** → 就是本条（约束被短路） | 三条候选改法（收益已实测，按收益排序）：<br>① **`pickDominantIndices`口径**：改成「取全部带`weight`的维度」或「weight ≥ 某阈值」而不是「只取并列最高」—— 收益最大（0 违约 + 快 27 倍），但属**语义变更**，需杰哥确认他写`weight: 2`的本意是不是「也是主控槽」；<br>② **消除短路**：判据按语义分列判断，或让下限取`Math.min(minDom, dom.length)`使其可达；<br>③ **加 best-so-far 择优**：预算耗尽时返回过程中「与窗口最不相似」的那个候选，而不是随机游走的终点（max-min 贪心的标准做法，能兜住任何约束组合下的兜底质量）。<br>⚠️ 改`enforce`前必须先处理 R-69（否则指标本身是假的，改完无法验收） | **MITIGATED（2026-09-21）** —— 三条改法已实现：① `pickDominantIndices`改为「全部带`weight`即主控」；② 下限取`min(minDom, 主控槽数)`消除短路；③`enforce`增加 best-so-far 择优。<br>**实测验收（13 维 × 6 候选，真实槽口径）**：主控槽`[7]` → **`[0,2,3,7,12]`**；30 / 50 / 100 条的最小差异 1/13 → **6/13**；差异 <6 的组合对 9.7% / 11.4% / **12.5%** → **0% / 0% / 0%**；重掷 3481 → **131**。<br>回归用例「主控槽下限会被收敛到可达范围，总差异约束不再被短路」（含「调大 `minTotalFar` 必须真的改变输出」这条最锋利的断言），反向验证精确 **1** 例失败。 |
| R-69 | **⭐`enforce`的`n`变量屏蔽导致「幻影槽位」→`selection` 数组被撑到 119 长并塞满 NaN → 验收指标失真、测试蒙混过关（已实测，未修）**。`for (let n = 0; n < maxAttempt; n++)`遮蔽了外层的`const n = variables.length`（R-45 第①条记录过这个笔误），于是 `[...Array(n).keys()]` 枚举的是**重掷轮次**而非维度下标：轮次 ≥ 维度数时，`candidate[slot]`与`target[slot]`**同为`undefined`** ⇒ `undefined === undefined`为真 ⇒ **幻影槽位被选进`sameSlots`**；随后「选窗口里重合最多的槽」中，幻影槽位对每个窗口点都相等（`count = window.length`，恒为最大）⇒ **幻影槽位永远获胜** ⇒ `candidate[slot] = nextValue(slot, …)`里`sizes[slot]`为`undefined`⇒`rngState.s % undefined`= **NaN** ⇒ 写入下标 13 ⇒ **数组每轮长一格**，最终 13 维的`selection` 被撑到 **119 长、填满 NaN**。<br>**实测**：10 条时 **9/10 条数组异常**、最大长度 119、953 个 NaN；50 条时 31/50 异常、3073 个 NaN；100 条时 55/100 异常、**5300 个 NaN**。<br>**最严重的后果是验收指标被架空**：`computeBatchMinDistance`→`hamming(left, right)` 用 **`left.length`**（被撑长的长度）作上界 ⇒ 差异被虚报成 ~100 ⇒ 官方指标报 **1.000「完美」**，而**真实槽最小差异只有 3/13** ⇒ 现有测试「8 维池取 10 条最小差异 ≥ 0.5」一直在**蒙混过关**（实测：官方 0.500 恰好压线，真实槽只有 **0.250**）。<br>**反向对照**：把循环变量改名（`n`→`round`，即恢复 `n = 维度数`）后 → 数组异常 **0**、NaN **0**，但相似度指标**几乎不变**（12.5% → 12.7%）⇒ 证明它是**纯污染，不是「相似度过高」的原因**；但它**必须修**，否则 R-68 改完无法验收、官方指标永远在报假数 | ① 任何一次采样（`maxAttempt`> 维度数即触发，默认 120 ⇒ 只要维度 < 120 就必然触发）；② 依赖`computeBatchMinDistance`或`hamming`的任何判断 | 采样后立刻检查`samples[].selection.length === 维度数`与`Number.isNaN`零命中 —— 任一不成立即命中。**自查一句话**：拿`selection.length`当差异上界之前，先断言它等于维度数 | ① 循环变量改名（或循环内改用`variables.length`），消除幻影槽位；② **`hamming`加长度守卫**：只比较`min(left.length, right.length)`，或断言两数组等长 —— 长度不等说明上游已坏，应暴露而不是继续算；③ 补回归用例「采样结果每条 `selection`长度等于维度数且无 NaN」。<br>⚠️ **与 R-45 的关系**：R-45 说「逐字保留、要改先与杰哥确认」。本次实测把**保留的代价量化了**（数组污染 + 指标失真），而 R-45 用来支撑「保留」的那张对照表恰恰是被本条污染的指标量的 —— 需一并重测后再定 | **MITIGATED（2026-09-21）** —— 槽位枚举改用`sizes.length`；`hamming` 改为以上界取两数组较小值。<br>**实测验收**：100 条的数组异常 55 → **0**、NaN 5300 → **0**，`computeBatchMinDistance` 不再虚报（不再出现「1.000 完美」而真实只有 3/13 的情况）。回归用例「采样结果形状必须是「每条长度 = 维度数」且无 NaN」＋全槽差异用例里的 shape 断言，反向验证精确 **2** 例失败。 |
| R-70 | **系列模式：组间复用同一 seed ⇒ 每组的「变化维度」取值序列完全相同**。`sampleCampaignRecipeSelections`按组循环，每组调用`farthestPointSample(variableDimensions, size, { ...options })`—— 传的是**同一个`options.seed`（不含 groupIndex）**，而 `baseCandidate(k)`只由`seed + k`决定 ⇒ **每组的出发点序列一模一样**；组间唯一的不重复来自「变化维度子签名进`usedSignatures`后被 L3 签名重掷推走」，那只有 40 轮预算（R-67①），且**不接 L2 的 max-min 约束**。另：每组调用时`selections` 是**该组自己的局部数组**，所以组间**完全没有近层/远层约束**，只有「不完全相同」这一条。<br>**实测（3 组 × 4 张，M 槽取值）**：`3,2,1,0 \| 3,2,1,0 \| 3,2,1,0` —— **三组完全一样的序列**（第 1、5、9 张的 M 槽取值相同）。组间最小差异 **5/13**，比「组内像一套」的组内最小（7~8/13）**还低**；组间平均 10.44 也仅比组内 9.58 高一点点 | 任何系列出图（`outputUnitSize > 1`）且组数 ≥ 2 | 打印各组「变化维度」的取值序列，看是否按组周期性重复；或直接看 `samples`里同一组内位置（如第 1、5、9 张）的取值是否雷同。**自查一句话**：随机性的种子若不随「组」变化，各组就是同一场抽签 | ① **把 groupIndex 混进 seed**（如`${seed}\|group\|${groupIndex}`）—— 一行改动，让每组从不同起点出发；② 组间也应跑一次 **L2 约束**（把已选点作为窗口传入，而不只靠 L3 签名去重）；③ 组内固定项本身的机制是对的（组内确实一致），要保留 | **MITIGATED（2026-09-21）** —— 每组 `seed`混入`groupIndex`。<br>**实测验收**：3 组首张的 M 槽取值由「三组全同（`3,2,1,0` 逐位一致）」变为 **`1,0,3`（互异）**。回归用例「组间：靠后的变化维度也不能逐组雷同」，反向验证精确 **1** 例失败。<br>**机制更正（比本条原文更准确）**：靠后的槽之所以雷同，主因不只是 seed 相同，还有**签名重掷选槽的偏置** —— `dom[guard % dom.length]`的`guard` 从 0 起，浅轮次只碰靠前的槽，**`M`（最后一个维度）永远轮不到重掷**，其取值完全由 `baseCandidate`决定。原始 Python 引擎用的是`rng.choice(dominant)`（真随机），移植时改成了确定性轮转。换 seed 已消除可观测症状；若要彻底对齐原版语义，需把该处选槽也改回 rng。 |
| R-71 | **两个正交的控件维度被套成父子嵌套 ⇒ 同一份数据被复制 N 份（中控台「多棵树」事故）** —— 把「配置维度」做成左树的父层级（维度组 → 作用域树）后，每个 `scopeAware` 的维度各渲染一份**完整的同一棵**方向树。默认只展开当前维度所以初看只有一棵；`expandedSections`是`Set`（允许同时展开多个），手动展开第二个组就出两棵；搜索态下 `searching \|\| expandedSections.has(...)` 让**所有组无条件展开**，直接铺满。根因：维度与作用域是两条**正交**的轴（「改什么」×「改谁」），套成一层嵌套必然复制数据 | 任何「按 A 分组的 B 列表」，且 B 的数据源在每个分组里是**同一个实例** | 数**唯一性**：对每个数据项断言「在界面上只出现一次」（`ConsoleAssetTree.test.tsx`首条即此回归断言）。**只断言「存在」是瞎的** —— 重复渲染照样满足 | 让两条轴**并排**而不是嵌套：树管「谁」（项目树：业务线 / 产品 / 方向，增删改查都在树上），tab 管「什么」（右区那排）。错误版本`5f52e88`，修正 `ffa9879`，详见 `docs/BACKLOG.md` TB-062 第二轮修订 | **MITIGATED**（回归断言：每个集合在树上出现且仅出现一次，1 例） |

| R-72 | **⭐ 低差异基座（`base_cand`）的散列在 JS 里精度丢失 → 每个维度只剩极少数取值（已实测并修复）**。`mix64` 的三个 splitmix64 常量都是 64 位字面量，其中 `0x9e3779b97f4a7c15` ≈ 1.14e19 —— **超出 float64 的整数精度**（该量级的精度间隔是 2048）。于是第一步 `(x + BIG) & 0xffffffff` 里 **x 的贡献被整个吞掉**：实测 `x = 0` 与 `x = 1000` 得到**完全相同**的结果。散列退化的后果是基座取值空间坍缩 —— 用真实资产（14 维 / 8 选项 / 30 条）实测：**`S1人物风格`、`S4点缀元素`、`S5背景氛围`、`S7服装`、`S10道具`、`S13副标题` 各只有 2 个取值**，`S13副标题` 是 15:15 完美对半、`M` 有 27/30 挤在前两个值上。用户观感就是「提示词相似度高」。<br>**为什么它比 R-68 更根**：R-68 修的是「约束没生效」，但**约束生效也救不了坍缩的取值空间** —— 每个槽只有两个值可用时，任意两条必然大量雷同。实测：只修 R-68 后「差异 <6 的对」仍有 1.8%，修掉本条才降到 **0%**。<br>**库里最直观的现场证据**：某次 30 条产出里，**第 1 条与第 11 条的前 80 字完全一致**（14 个槽只差 1 个）。 | ① 任何依赖 `mix64` 的地方（每槽盐值 + 每条的 `base_cand`）；② 维度选项数是 2 的幂（4 / 8 / 16）时更严重 —— `% size` 丢掉高位、只留偏差最大的低位 | 用 `farthestPointSample(dims, 30, { seed, maxAttempt: 0 })` **关掉重掷**后看各槽唯一取值数：8 选项的槽若只有 2 个值即命中（正常应接近 8）。**先查一步**：`(x + 0x9e3779b97f4a7c15) & 0xffffffff` 对 `x = 0` 与 `x = 1000` 是否同值 —— 同值就是本条。**自查一句话**：把 64 位字面量丢进 float64 运算前，先问它有没有超出 2^53 | 改用 **BigInt 精确 64 位**（= Python 原版的任意精度整数语义）：`(BigInt(x >>> 0) + 0x9e3779b97f4a7c15n) & MASK64`，掩码提到模块级。代价可忽略 —— `mix64` 只在「每槽盐值」与「每条的 `base_cand`」处调用（约 count + 维度数次），重掷走 `nextValue` 的 xorshift，不经过它。<br>**⚠️ 与 R-45 不冲突**：R-45 禁止的是把常量**截断成短常量**（会静默改变散列结果），本条做的是**恢复精确 64 位** —— BigInt 版才是真正的「与原始引擎逐位一致」，float64 版反而是走样的那个。<br>⚠️ 这会**改变全部采样输出**（含跨批去重与系列模式），需一次性验收 | **MITIGATED**（BigInt 版实测：真实资产 30 条下「差异 <6 的对」1.8% → **0%**、平均相同槽 2.69 → **1.90/14**、被正文引用的槽全部用满选项、重掷 98 → **31**；回归用例「低差异基座不能退化」用 `maxAttempt: 0` 直测基座分布，反向验证：改回 float64 → `expected 4 to be greater than or equal to 6` 精确 1 例失败） |
| R-73 | **水印库按产品隔离后，「归属」的解析在产出与导入侧仍按 preset id 全局查找 ⇒ 跨产品绑定会静默生效**。隔离做在**界面层**（库只列当前产品的预设、勾选框只有本产品的行）与**入口层**（新建 / 导入落当前产品），但 `taskPostprocess.ts` 找预设仍是 `presets.find(id)`、中控台 Excel 导入（`consoleImport.ts`）写 `watermarkPresetIds` 也不校验归属 —— 只要数据里出现别产品的 preset id，产出照样叠上去，而界面上看不见它（不在本产品的库里，「未分配」区也不会有）。 | ① 中控台 Excel 导入把 A 产品的方向绑上 B 产品的预设；② 手工改库（localStorage 的 `tangbao-composite-v2-workspace-storage` / `app_data_records`）；③ 升级前的跨产品共用残留（迁移会复制副本并改指引用，正常不残留） | 中控台水印分区里，某个方向勾着的水印在它所属产品的库里找不到；或产出文件名 `{preset}` 段的名字与库里的对不上。**自查一句话**：「勾得到」与「产得出」若各有一套集合，就一定分叉 | ① **本轮缓解**：界面层已无法选出跨产品的预设，正常操作路径不会产生这种数据；② **彻底拦**：产出前加一道「预设属于该节点所属产品」的校验（`taskPostprocess` + `consoleImport` 两处）—— 但那会让**现有**跨产品绑定突然不出图，是行为变更，需单独评估后再做（见 ADR-0012 §四） | **ACCEPTED**（知情保留；TB-067） |
| R-74 | **CI 连续多个提交全红而无人察觉 ⇒ 远端门禁形同虚设；且「本地 WIP 恰好修好了已提交代码的错」会让本地门禁「假绿」。**实测（2026-09-21）：`c72656d`（TB-064）起 CI 的 `Typecheck renderer` 一直 failure（连续 4 个提交），真因是 `git show HEAD:src/features/postprocess/PostprocessParamPanel.tsx:56` 仍写着 `import { PresetCover } from '../composite/components/ConsolePresetCard'`，而该组件**已被 TB-064 删除**（HEAD 里根本没有这个文件）。本地 `npm run verify` 全绿只是因为**工作区有一份未提交的 `PostprocessParamPanel.tsx` WIP 恰好删掉了这行** —— 本地编译的是「HEAD + WIP」，CI 编译的是「HEAD」。 | ① 删组件 / 文件时没做「三查」（R-18 已规定，这里没执行到位）；② 工作区有未提交 WIP 时跑门禁；③ 提交后不看 CI 结论 | `git show HEAD:<file> \| grep '<被删模块>'`；或逐个比对「`git show HEAD:<f> \| wc -l` vs 工作区行数」列出被 WIP 覆盖的文件。**自查一句话**：门禁绿的到底是「我要提交的那份代码」，还是「我桌上那份」？ | ① **提交前把被 WIP 覆盖的文件挑出来单独验**（`git show HEAD:<f>` 落盘后单独跑 `tsc`，或 `git worktree add` 一份干净树）；② 推送后**必须**查一次 CI 结论（public 仓库可匿名查：`curl -s --ssl-no-revoke "https://api.github.com/repos/<owner>/<repo>/actions/runs?per_page=2"`）；③ 删文件严格执行 R-18 三查 | **OPEN**（2026-09-21 发现；那行失效 import 目前由另一条写线的未提交 WIP「挡着」，其代码一提交即恢复红） |
| R-75 | **给共享模块加了新导出之后，`vi.mock` 它的测试全挂，报错指向被测组件而不是 mock**：`vi.mock('../stores/runtimeStore', () => ({ useRuntimeStore: ... }))` / `vi.mock('../store', () => storeMocks)` 都是**手工列举**的工厂，模块新增任何导出都不会自动带上 —— 本轮撞了**两次**（`useLatestPostprocessRunForTask` 挂 4 例 `TaskCard`、`showPostprocessIssuesDialog` 挂 1 例）。vitest 抛的是 `No "X" export is defined on the "..." mock`，而堆栈指向**被渲染的组件**（`TaskCard.tsx` 的 hook / 回调行），一眼看去像组件写错了 | 任何「往 `stores/runtimeStore` 或 `store.ts` 加导出」的改动，且仓库里存在 `vi.mock` 它们的测试（当前：`components/TaskCard.test.tsx`、`features/assetLibrary/AssetBatchView.test.tsx`） | `grep -rn "vi.mock('.*\(runtimeStore\|store\)" src` 找出手工 mock；**改完这两个模块先跑这些测试文件**，别等全量 | 往这类模块加导出时，**同一轮**就把 mock 工厂补上，并在 mock 处写一句「必须与模块导出同步」的注释。根治办法是 `vi.mock` 里用 `importOriginal` 展开原模块再覆盖需要的字段（vitest 报错里就给了模板），但会改变既有 mock 的隔离语义，需单独评估 | **MITIGATED**（2026-09-21：两处 mock 已补齐 + 注释；根治方案未采用） |
| R-76 | **「结论型标记」在重新加载的 effect 里只重置了数据、没重置结论 ⇒ 一次误判就永久粘住**。`TaskCard` 加载封面的 effect 清了 `thumbSrc` / 比例 / 尺寸，**漏了 `setThumbLost(false)`** —— 某轮在「缩略图未就绪 + 图片记录查不到」的瞬间点亮「图片已丢失」后，此后**任何**一次 `thumbSrc` 为空（切封面、重新生成、加载中）都会显示「图片已丢失」，而图其实一直都在。用户报的是「素材不定时丢失」。同一个需求的另一份实现 `hooks/useCoverThumbnail.ts` 是**重置**的 —— 属于写漏。 | 任何把「加载失败 / 空态 / 已丢失」这类**结论**存进组件 state 的地方，只要在 effect 里只重置了数据源（`src` / `list` / `id`）而没重置结论；尤见于"两份实现同一个需求"的场景 | 数据明明正常、界面却持续显示错误态；切换数据源或重新挂载后错误态消失（= 说明是 state 粘住而非数据缺失） | **数据与结论在同一个 effect 里一起重置**；同一需求的多个实现（如 `TaskCard` vs `useCoverThumbnail`）改一处后**必须回读另一处** | **MITIGATED**（2026-09-21：`TaskCard` 已补 `setThumbLost(false)`；TB-082） |
| R-77 | **提示单槽 + 「每个任务结算播一次」⇒ 用户点掉后被下一条同款立刻顶回来，主观上就是「关不掉」**。toast 只有一个槽位（`set({ toast })`），批量生成每完成一个任务就 `showToast` 一次（`生成失败：…` / `生成完成…`）⇒ 点 × 后几百毫秒内同一条又回来。用户报「失败提示突然弹出且无法被关闭」，而代码里明明有 × 按钮、也有自动消失 —— 症状与代码对不上时极易查错方向。 | 任何「在循环里逐条播报」的提示（批量生成/重试/后处理/导入）；用户能感知到"我关过它"的场景 | 有 × 按钮、有自动消失，用户仍说"关不掉"；问一句「是不是关掉之后又自己出来了」即可确认 | ① 用户**主动关闭**的文案进 `lib/toastReplayGuard`，短窗口（8s）内同文案不重播（自动消失**不**登记，避免吞掉新提示）；② 新增"每任务一次"的提示前先想这条：能不能汇总成一条，或降到 info/状态条 | **MITIGATED**（2026-09-21：`toastReplayGuard` + store 接线 + 5 例单测；TB-082。**未消除**：逐任务播报的节奏本身还在，只是被抑制窗口挡住） |
| R-78 | **删除类操作「先把记录删掉、再按记录去找文件删」⇒ 文件永远删不掉；而且失败信息没人看，全程静默**。`executeAssetPurge` 的次序是「事务删素材/图记录 → 删图片字节」，而字节删除要按图记录里的 `localPath` 找文件 —— 记录已经没了，这一步**空转**：`batchDeleteImages` 内部 `batchGetImages` 返回空、`deleteRawCacheImages` 无路径可删，主进程 `deleteCacheImageFiles` 连调都不会调。更隐蔽的是它**没有任何反馈**：主进程返回 `{ deleted, failed }`，渲染侧 `deleteRawCacheImages` 直接丢弃；路径不匹配（库根换过）或 `unlink` 失败同样无声。实测（2026-09-21）：132 张素材记录被永久删除，而 `cache-images/` 里**文件一张不少**（`561 = 429 + 132` 精确吻合），用户视角是"素材删了、磁盘没瘦"。 | 任何「按记录里的路径去删真实文件」的删除流程，只要顺序是**先删记录**；同类还包括"删素材连带删导出文件"「删任务连带删图」。 | 记录没了、文件还在（`磁盘文件数 > 记录数`，且差值恰好等于墓碑数）。**自查一句话**：删文件那一步依赖的信息，在它执行时还查得到吗？ | ① **把顺序变成契约**：删除执行器在删记录**之前**回调收集路径（`AssetPurgeExecutorDeps.collectImagePaths`），删完记录再按路径删文件（`deleteImageFiles`）；单测按调用顺序断言，改错顺序即变红。② 删除接口的返回值不要丢 —— 失败要能看见（同日已做：返回 `{ deleted, failed }` + `[cache-image-delete]` 日志 + 3 例单测） | **MITIGATED**（2026-09-21：顺序已改为契约 + 单测守 + 反向验证；同日把「失败静默」那一半也补上；TB-083） |
| R-79 | **同仓多写线时，别人的 `git add` 会把我的未提交改动一并提交走**（我的改动进了历史，但提交信息不是我的）；**反向**同样危险：我要提交的文件里混着别人**未提交**的半成品，整体 `git add` 会把对方没做完的东西提交掉。实例（2026-09-22）：我只在 `src/types.ts` 加了三行（`prompting` / `promptPending` / `promptFailed`），对方的 `c1075c2`（TB-086 第二段）把它一并带进了历史 —— 是我提交前 `git status` 发现 `types.ts` 已经「干净」才察觉；同时 `src/store.ts` 的 `exportData` 区域混着对方另一段未提交改动。 | 同一仓库并行两条以上写线（本仓 2026-09-22 同时有三条：配置包 v8 / 素材库空格预览 / 生图发起即建卡） | **提交前把两份清单对齐**：`git status --porcelain` 与「我这轮实际改过的文件」必须完全一致 —— 少了（被别人的 add 带走）或多了（别人的 WIP）都要停下来查。**自查一句话**：我 add 的这批文件，每一个都是我改的吗？ | ① 提交前逐条核对清单；② 同一文件混着别人改动时**不要整体 `git add`**：`git diff <file> > all.patch` → `grep -n '^@@' all.patch` 看 hunk 边界 → `head -N all.patch > mine.patch` 截到自己最后一个 hunk → **`git apply --cached mine.patch`**（只进索引、不动工作区，对方继续写不受影响）→ 其余文件正常 add；③ **`git apply` 不认 `/tmp` 路径**（git 是 Windows 进程，会把 `/tmp/x` 当相对路径报 No such file），patch 要落到项目相对路径（如 `.git/mine.patch`）；④ 必然的派生：全量 `tsc` / `npm run verify` 会因对方 WIP 变红（本轮红在 `selectDirectory` 未导出），**只能跑定向用例 + 过滤后的 tsc**，别把对方的红当成自己写错了 | **OPEN**（2026-09-22 实测；属写线隔离缺口的派生风险，根治要靠 `git worktree` 物理隔离，见 `docs/work-protocol.md`） |
| R-80 | **ds 基础类会静默吃掉同名 Tailwind 工具类**：`design-system/styles.css` 在 `index.css` 之后加载、特异性同为单类 → **后写的赢**。调用方写 `className="absolute right-4 top-4"` 实际得到的是「仍在文档流 + 被 right/top 平移 16px」——元素把后续兄弟节点挤到它那一行，自己再压上去。2026-09-22 报障「删除预设？弹窗的 × 压住正文」即此因（ConfirmDialog 关闭键；全仓同类漏项 3 处：ConfirmDialog / WorkspaceTabBar 窄屏「打开标签页」/ AssetLibraryWorkspace 移动端导航关闭键；另有 6 处早已手写 `!absolute` 补丁）。**同一坑对任何属性成立**，不限于 position：`min-h-ds-control-lg` 同样被 `.ds-icon-button{min-height:…}` 吃掉。 | 给 ds 组件或 `ds-*` 类元素传 Tailwind 工具类，且该属性 ds 基础类已声明（`position` / `min-height` / `padding` / `background`…） | 界面元素**位置怪但仍渲染**（不消失、不报错）；典型是「把旁边元素挤开又压上去」。构建产物里比两条规则的先后：`.absolute{position:absolute}` @6105 **早于** `.ds-button,.ds-icon-button{…position:relative}` @64924（`dist/assets/index-*.css`）。**自查一句话**：这个工具类真的赢了吗？ | ① 需要覆盖 ds 基础类时工具类**加 `!`**（`!absolute` / `!fixed` / `!min-h-…`，仓库既有约定，6 处先例）；② 已加合规守卫：`compliance.test.ts`「设计系统组件的定位工具类必须加 ! 前缀」（AST 扫组件 + 文本扫 `ds-*` 类）+ `ConfirmDialog.test.tsx` 断言关闭键含 `!absolute`；③ 根治方向（**未做**）：把 ds 基础类挪进 `@layer components` 让工具类天然占优 —— 会翻转现有「ds 赢」的既有行为，需单独评估；配方见 runbook §21 | **MITIGATED**（2026-09-22：3 处补 `!` + 守卫 + 反向验证；TB-090） |
| R-81 | **表单行的「对齐」有两个隐形变量：① 底对齐（`align="flex-end"` / `items-center`）+ 只有部分字段有 helper/error ⇒ 一屏出现两条基线，且**状态一变就跳**（给 宽 输个非整数，多出一行红字，整行往上移一次）；② `<Button>` 里把图标当 children ⇒ 图标与文字分成两行（图标压在上方）。**成因②是 Tailwind preflight 给 `svg` 设了 `display: block`**（`img,svg,video,…{display:block}`）—— 图标在 button 的文字 span 里成了块级元素，必然换行。2026-09-22 实测：尺寸编辑面板（报障）里标签/输入框各差 20 截图 px ≈ 26 CSS px（正好一个标签行 16.25px + 字段内间距 8px）；全仓「图标当 children」共 3 处（资产树「业务线」/ 渠道表「添加渠道」/ 尺寸面板「删除尺寸」），运行中的应用截图可见「+ 独占一行、业务线在第二行」。 | 写/改任何「一行多字段 + 行尾操作按钮」的表单行；给 ds 组件传图标 | ① 标签或控件不在一条线（量测：同一行相邻字段的标签 y 差恰好等于一个标签行高）；② 按钮高度异常（≈两行文字高）或按钮内部 `svg` 是 `span` 的子节点而不是 `button` 的直接子节点（`span > svg` vs `button > svg`）。**自查一句**：这一行里，谁比谁多了一行？ | ① 字段行一律**顶对齐**，helper/error 往下挂；操作区借 `.ds-field` 的标签槽占位（`<div class="ds-field"><span class="ds-field__label invisible" aria-hidden>…</span><控件行/></div>`）—— 同一份 class/token，不写死像素，按钮自动落在控件那条线；② 图标一律走 `leadingIcon`；③ 两条守卫：`compliance.test.ts`「Button 里的图标走 leadingIcon」（AST 扫全仓）+ `ConsolePostprocessSections.test.tsx` 的对齐结构断言 | **MITIGATED**（2026-09-22：4 文件修正 + 2 条守卫 + 反向验证；TB-091。⚠️ 字段行对齐未做渲染验证） |

| R-82 | **⭐ 合并 / 替换一个视图时，「字段」有列可比对、「行级动作」没有 ⇒ 动作最容易静默丢掉**。实例（2026-09-22 TB-093）：把「渠道表 + 尺寸表 + 输出位置表」合成一张表时，字段一个没丢（渠道名 / 详细尺寸 / 参与产出 / 导出位置都安排了列），但**尺寸表整张消失**，它承载的「加尺寸」动作**没有天然落点** —— 第一版实现只安排了「删渠道 / 删位置 / 加位置」三个动作，把「加尺寸」漏了。这类漏**不会报错**：编译过、渲染正常、只是「哪儿都点不到」。是既有用例 `ConsolePostprocessSections.test.tsx` 的「尺寸行的「+」是图标按钮：加尺寸就近加在这个渠道上」直接报「未找到元素：给「头条」加一个尺寸」才抓出来。<br>同类风险：删尺寸 / 删预设这类**删除动作**、导出 / 复制类动作、批量动作 —— 只要被删的那张表是**另一个实体的主界面**（本例尺寸表是尺寸增删改的唯一入口），它的动作就无处安放 | 任何「两张表合成一张」「删掉一张表」「换掉一个主界面」的改动 | 改完先 grep 被删组件里的**动作名**（`aria-label` / 按钮文案）在测试里还有没有断言，逐条确认落到哪了；`npm test` 里报「未找到元素 / 未找到按钮」的即是。**自查一句话**：我删掉的那个界面上，用户原来能点的按钮，现在都点得到吗？ | ① 合并前先列两张表的**行级动作清单**（不是字段清单），逐条写下新落点再动手；② 动作的 `aria-label` **原样保留**（本例「给「头条」加一个尺寸」一字未改）—— 既有守卫才继续有效；③ 动作**就近放**（加尺寸贴着它所属渠道的尺寸组），不要全堆进操作列（操作列塞第四个图标后彼此认不出） | **MITIGATED**（2026-09-22：加尺寸已补回「详细尺寸」格内；既有守卫 + 两条合并形态新用例守着；TB-093） |

| R-83 | **⭐ 把「派生尺寸」当排版约束用 ⇒ 差一像素变成差一行**。文字层的框（`position.width`）是 `measureCompositeTextBox` 算出来的**派生值**，但它两头都对不上真实要画的字：① **永远算不进渲染时才叠加的标识符**（库里 `preset-compliance-04`：框内宽 432 = 23 字 × 18px，叠加 `★` 前缀后正好 24 字 = 432，卡在边界上）；② **旧版文本层的框是手拖的 / 铺满画布**（`preset-compliance-06` 是 1160 宽的框配 19 字文案，自然宽才 ~624）。2026-09-22 两天内因此连出两次障：先把框宽当 `fillText` 的第 4 参（maxWidth）→ canvas **横向压扁**（报障「部分文字被拉伸变形」）；改成逐字折行 → **末字被挤到第二行**（报障「我没设置换行，它自己换行」）。两次都是「拿对不上的数当硬约束，把一像素的误差放大成一行」。 | 渲染或布局时用到 `position.width` 这类**派生尺寸**；或给自动适应结果 / 缓存的量测值当真值用 | 症状**都不是报错**，而是「字的形状 / 行数不对」。判据：**开关标识符会改变行数或字形**就是这条。可查数据：框宽与文案自然宽之差 ≈ 一个标识符宽（或差一个字符宽 = 卡边界） | ① **框宽只用来定位**（对齐锚点 + padding），排版只看文案自己的 `\n`；② `fillText` / `strokeText` **不传 maxWidth**（canvas 的 maxWidth 是横向压缩，不是换行）；③ 溢出按 `align` 向外长（`right` 右边缘不动、往左溢出）；④ 守卫 `compositeRendererV2.test.ts`「⭐ 字比框宽也不断行、不压缩」（假 ctx 记录调用：行数 = 手动换行数、maxWidth 必须 `undefined`、右对齐锚点不动）+「手动敲的换行符照旧生效」；⑤ `measureCompositeTextBox` 头注写明「这是框、不是排版约束」及上述两个原因 | **MITIGATED**（2026-09-22：折行函数删除 + 2 条守卫，反向验证两条都精确命中；TB-094） |

| R-84 | **⭐ 界面里「自己拼一遍继承」的提示 ⇒ 显示少于实际生效，用户照界面判断就会错**。中控台「导出位置」那格的灰字原先自己扫父节点的 `byMedia`，三个错叠在一起：① **只取列表第 1 个**（上一级给同一渠道配了两处双写，界面只念一处）；② **只看直接父节点那一层**（上一级没配、更上层配了 ⇒ 报成「默认输出位置」）；③ **只看 `byMedia`**（父级用通用 `outputDir` 覆盖时同样错报）。而产出侧跟随的是**整份列表**（`applyPostprocessOverride`：表态就整份替换、不表态就保留上层），两处都照写。2026-09-22 报障原话：「上一级有一个渠道有两个导出位置，但跟随的只有一个」。<br>⚠️ 同一坑在弹窗侧还有一份：`PostprocessParamPanel` 取 `sliceUp.config.outputDir`，而按渠道配的目录存在 `mediaOutputDirs[mediaId]` 里 ⇒ **恒定**错报默认位置 | 在界面里写「留空会落到哪 / 生效值是谁 / 继承自谁」这类**推导**，但不是调 `resolveProjectPostprocessSlice` 等既有解析函数，而是自己遍历 `params` / `byMedia` | 症状是「界面显示一套、产出按另一套」，**不报错**。判据：往继承链中间某层配个值，界面提示若**不跟着变**（或只变一半）就是这条。**自查一句话**：这段推导，仓库里是不是已经有一个函数在做？ | ① 生效值只走既有解析函数（`resolveProjectPostprocessSlice` + `resolvePostprocessOutputDirs`），界面层不自己拼继承；② 提示里的**列表要念全**：`formatInheritedOutputDirsHint` 一处 ⇒「留空则 A」、两处 ⇒「留空则继承 2 处：A、B」——数量写在最前，共享盘长路径被输入框截断时那一眼要看到的就是「几处」；③ 守卫：`ConsolePostprocessSections.test.tsx` 三例（上一级两处 / 隔层 / 父级通用输出目录）+ `postprocessMedia.test.ts` 四例文案 | **MITIGATED**（2026-09-22：中控台与弹窗两处同修 + 7 例守卫；反向验证退回旧实现精确挂 3 例；TB-095） |

| R-85 | **⭐ 守卫断言的是「输出」，而两条不同代码路径能产出同一份输出 ⇒ 新分支被删掉、测试依然全绿**。加竖排（`orientation`）时写了一条「一字一行 + 竖排 = 折成一列」的守卫：断言 `fillText` 被调 3 次、文字序列 `['该','活','动']`、x 全相同 —— 但**横排渲染 `该\n活\n动` 也是 3 行 × 每行 1 字**，输出逐字段一样。于是反向验证（把竖排分支短路成 `if (false && …)`）时另外 3 条竖排守卫全挂、**唯独它绿**；而它的「邻座都挂了」很容易被读成「这批守卫是有效的」，差一点就当有效守卫收下。 | 新增一条渲染 / 计算路径，且它**对某些输入与旧路径产出相同结果**（竖排 vs 横排的「一字一行」、重构前后等价的实现、别名 vs 规范路径、兜底与主路径同值） | 反向验证要**逐条看哪些用例挂了**，不能只看「挂了几条」。某条守卫在关掉修复后仍绿 ⇒ 要么它测的不是这段代码，要么真有另一条路径产出同样结果。**自查一句话**：这条断言，是只有新路径才给得出来的吗？ | ① 守卫里加一条**只有新路径才有**的判据（本例：y 步进 = `fontSize + 字距`，横排是 `fontSize × 行高`，数值不同即可分辨）；② 反向验证的结论里写清「挂了**哪几条**」而不是「挂了几条」（TB-097） | **MITIGATED**（2026-09-22：竖排守卫已补 y 步进判据，重跑反向验证 4/4 精确命中；TB-097） |

| R-86 | **⭐ 拖拽移动只重排「落点那一层」的顺序号，源那一层留下空洞 ⇒ 之后新建的节点会与已有节点**撞号**，顺序静默退化成按名称排**。实例（2026-09-22 做中控台拖拽时查代码发现，**非报障**）：`moveCollectionsToPosition` 落库时只对 `targetParentId` 的同级重排 `order`（`store.ts:1224`–`1233`），被拖走节点原来所在的同级**不重排** —— 一层里 A(0)/B(1)/C(2) 拖走 B 后剩 A(0)/C(2)；此时新建节点按 `order = 同级数量` 取值（`store.ts:1059`–`1065`）⇒ 新节点 `order = 2`，与 C **撞号**，排序键 `order - order \|\| name.localeCompare` 在相等时退化为按名称。用户看到的是「刚加的方向没排到最后」，**不报错**。（画廊左侧栏自拖拽上线以来一直如此，两处共用同一个 action。） | 在同一层做过「拖走一个节点」之后，又在该层新建节点；或任何假设「同级 `order` 连续」的派生逻辑 | 新建节点的落位与预期不符（没在末尾）且不报错。判据：查该层 `order` 是否有空洞（`[0, 2]` 这种）。**自查一句话**：这段代码假设同级的 `order` 是连续的吗？ | ① **本轮不动** —— 改动会碰画廊侧栏共用的落库逻辑，属单独评估；② 修法方向：移动后把**源父级**的同级也一并重排 `order`（一次 filter + map）；③ 临时绕开：新建时取 `max(order) + 1` 而不是「同级数量」 | **OPEN**（2026-09-22 发现并登记；TB-098 只做中控台拖拽，未修此处） |
| R-87 | **⭐ 导出的包被自己拒收：包结构版本「导出侧写死」与「导入侧上限」不同步**。`exportData` / `exportDataToPath` 写 `version: 8`（`7decde3`「配置包 v8，TB-086 第一段」引入），而导入校验的上限 `CURRENT_BACKUP_VERSION` 停在 `7`（`backupImport.ts`）⇒ `validateBackupArchive` 抛「备份版本 8 高于当前支持的版本 7」，**导出的每一个包都导不回来**。「发布配置 / 拉取最新」因此形同虚设 —— 配置同步失效的根因不在同步本身，而在包根本进不来；而每次同步都会先做「拉取前备份」，于是失败路径上还多写了一份同样不可用的包。 | 任何时候走「导出 → 导入」：配置同步的发布 / 拉取、备份恢复、换机器搬配置 | 导入报「备份版本 X 高于当前支持的版本 Y」。**自查一句话**：导出侧写死的包版本与 `CURRENT_BACKUP_VERSION` 对得上吗？（这类问题的特征：**两边各自的用例都是绿的**） | ① 三处版本号（`exportData` / `exportDataToPath` / `CURRENT_BACKUP_VERSION`）统一提到 9，并在规范 §七 写成对照表（"包结构版本" ≠ "配置格式版本"）；② `backupImport.test.ts` 新增「接受本版本自己导出的包」用例守住同步；③ TB-100 起有「导出 → 导入」的 round-trip 用例（`store.test.ts` 断言包内 `config.json` 与 manifest 版本） | **CLOSED**（2026-09-22 TB-100 修复；全量 262 文件 / 3096 例绿） |
| R-88 | **⭐ 导入时「本地独有」的口径不一致：树保留、参数与水印却整份替换 ⇒ 自建方向还在、它的参数没了**。项目树走 `mergeImportedAssetLibrary`（同 id 覆盖 + **保留本地独有**），而节点参数走 `useProjectTreeParamsStore.setState({ params: toNodeParams(bundle) })`（**整表替换**）、水印库走 `mergeCompositeV2PersistedState` 的 `{ ...currentState, ...persisted }`（**整份替换**）。已有资料的同事拉一份配置后：树上自建的方向还在（界面看着都对），但那几个方向的**渠道选择 / 输出位置 / 水印引用被清空** ⇒ 退回「继承全局」，**产出结果悄悄变了且不报错**；自建的水印与 LOGO 则直接消失。 | 本机已有自建的方向或水印，再导入 / 拉取一份别人（或自己另一台机器）的配置 | **产出结果与导入前不一致，而界面上的树是"对"的**。判据：对比某个自建方向导入前后的参数，或看水印库数量有没有变少。**自查一句话**：这条数据的「本地独有」是保留还是替换？跟同链路的其它数据一致吗？ | ① **TB-101 已修**：统一成「同 id 以包为准 + 本地独有保留」（由 `localOnly` 开关决定去留），水印库新增按 id 合并的 `mergeCompositeV2Library`；② 用户可**逐模块**选覆盖范围，不必接受整份替换；③ 新增 `POSTPROCESS_FIELD_GROUP` 归属表（编译期闸门），防"分组口径"再走散 | **CLOSED**（2026-09-22 TB-101；全量 262 文件 / 3099 例绿） |
| R-89 | **⭐ 导出全挂：主进程 ZIP 条目白名单漏了 v9 的 `config.json`，而失败文案把真因盖住了**。`electron/streaming-zip.ts` 的 `validArchivePath` 是**白名单**式校验，只放行 `images/` / `thumbnails/` / `composite-assets/` 三个目录；TB-100 把配置本体改成包内**根级**的 `config.json` 后没同步放行 ⇒ 导出时写第一个条目就被拒（`无效 ZIP 路径：config.json`），**发布配置 / 导出数据 / 导出备份一起失败**。更糟的是真因被吞了两层：`exportDataToPath` 的 catch 只把 `error.message` 丢进 toast、返回值里**没有 error 字段**，于是 `publishConfigToSyncDir` 只能编一句「写不进 X（确认这个目录可写）」—— 而该目录**明明可写**（同一次会话里 `writeJsonText` 实测能写进去），排查被彻底带偏。 | 任何带配置的导出（默认导出就带配置）；v9 之后必现 | 用户照着「确认这个目录可写」去查权限，白查；配置同步与备份整体不可用。**判据**：界面说「写不进 <目录>」，而你能往该目录写文件 | ① `ROOT_ARCHIVE_FILES` 放行根级信封文件；② `streaming-zip.test.ts` 用**渲染侧的 `TREE_CONFIG_ENTRY`** 写成对用例 —— 谁只改一边（常量或白名单）测试就红；③ `exportDataToPath` 回传 message，发布/导出照实报。**教训**：白名单式校验 + 新增条目 = 必须配成对守卫；错误文案自问一句「用户照这句话去查，能查到吗」 | **CLOSED**（2026-09-22；反向验证：拿掉白名单 → 守卫用例红，报错与线上真因一字不差） |
| R-90 | **⭐ 一份配置被「自动」与「手动」两条触发路径共用 ⇒ 只针对一侧的需求会污染另一侧**。`savedTargetCollectionIds`（记住的产出目标）原先的语义是「自动与手动都照它跑」（`taskPostprocess.ts` 注释原文）。杰哥要的是「手动跑时能跨产品投到还没参与自动产出的方向」—— 若只把弹窗放开（放开后目标必然含未启用方向），**自动跑会连带把那些方向也当成目标**，再被 `PP-SCOPE-001` 逐条跳过：用户看到的是「自动后处理开始刷一堆跳过提示」，而他要改的只是手动那一次。反方向同理：启用范围（`selectedCollectionIds`）本是**自动**的路由开关，拿它拦手动就让「手动跑」这个动作失去意义（2026-09-21 的 `PP-SCOPE-002` 报障是同一形状）。 | 给某个字段 / 开关加「只针对手动」或「只针对自动」的需求时；或新增第二处判「要不要产出」的门 | **不报错，但另一条路的产出结果变了**。判据：把该字段的**全部读点**列出来，有没有一个读点处在另一条触发路径上？**自查一句话**：这个字段现在有几个人在读，各属于哪条路？ | ① **按 `source` 分流，而不是共享语义**：`savedTargets = source === 'manual' ? … : []`，两处门都写成 `input.source !== 'manual' && …`；② **两侧各一条守卫**（`taskPostprocess.test.ts`：「自动跑不读记住的目标」+「自动跑仍逐个过启用范围」）—— 只守一侧会让另一侧的回退无人察觉；③ 后续新增判定照这个格局走，见 `docs/architecture-constraints.md` §4.4.1 | **MITIGATED**（2026-09-22 TB-089 修订：分流 + 两侧守卫；反向验证轮 1 / 轮 2 各自精确命中 1 例） |
| R-91 | **⭐ 「内存缓存窗口」被当成可见性判据 ⇒ 缓存外的数据静默消失，而库里其实一条没少**。桌面端启动 `hydrate()` 只把**最新 200 条**灌进 `assetsById`（`assetLibraryRepository.hydrate` 的 `limit: 200`），其余素材只活在 SQL 分页里；而 `resolveEffectiveAssets`（`query.ts:199`）旧写法是 `if (!live \|\| live.status !== asset.status) return false` —— **没缓存就剔除**。再叠两处：① `catalogPage` 是**组件局部 state**，切界面重挂载即归零，`AssetLibraryWorkspace.tsx:483` 于是整屏退回「只画内存那 200 条」；② `.catch(() => setCatalogPage(null))` 一次查询失败就静默清空、且 deps 未变不会再跑（只能重启）。实测本机 479 张 active 素材里 **279 张（58%）在窗口外** ⇒ 切一次界面少一半卡片，不报错、不留占位。 | 库内素材数 > 启动 hydrate 窗口（200）；或目录查询失败一次 | 顶部「全部素材 N 张」− 卡片视图速览条「X 张素材」的差值；切换一次界面看这个数是否跳 | 已缓解（TB-106）：`!live` 时以数据库分页结果为准保留；查询失败**不清分页快照**（有快照就留着 + 出「刷新失败 · 重试」，没快照才显示失败态）；新增 `catalogAwaitingFirstPage`，用「加载中」代替「换一份数据源」；重试令牌进 effect 依赖。**根治**是把分页快照提到 `features/assetLibrary/store.ts`（跨重挂载存活），未做 | MITIGATED（2026-09-22 TB-106） |
| R-92 | **⭐ flex 列布局里「没有高度下限」的子项会被压扁 ⇒ 标签栏只剩一条，而一个错都不报**。SOP 管理中心的 `.sop-center-dialog` 是 `flex flex-col`，高度由 `max-height: min(86vh, 860px)` 兜住；内容一多（SOP 列表几百条）浏览器就**按比例压缩所有可收缩的子项**。`.sop-center-header` 有 `min-height: 3.25rem`，因此保住了自己；而 `.sop-center-tabs`（design-system 的 `Tabs`）**只有 background + padding-inline、没有任何高度下限** ⇒ 实测被压到 **7px**，三个标签只剩文字上半截（2026-09-23 报障「SOP 管理弹窗标签栏显示异常」；dev 与生产构建表现一致）。**同类子项还有**工具条 / 表头 / 面包屑 —— 凡是「按内容排布、自己不定高」的横向条，放进高度受限的 flex 列里都有这个风险 | 往「高度受限的 flex 列」里放不设下限的横向条；内容区一旦超出可用高度就会触发 | 量 `getBoundingClientRect().height`（正常约 40px，实测 7px）；或看截图里文字只剩上半截。**自查一句话**：这一列里，谁被压缩了？（逐个量兄弟元素高度，谁比预期矮就是它） | ① 不该收缩的子项一律 `flex: none`（「至少多高」的语义也可用 `min-height`，但 `flex: none` 更直接）；② 守卫 `sopCenterSizing.test.ts`「标签栏不被压缩：必须不参与 flex 收缩」；③ 反向验证：撤掉 `flex: none` → **恰好 1 条红**，其余 3 条照过 | **MITIGATED**（2026-09-23：`features/strategy/styles.css` 加 `flex: none` + 1 条守卫；CDP 实测 7px → 41px） |
| R-93 | **⭐ 「读 CSS 文本 + 正则断言」的守卫会被注释里的同名文字骗过 ⇒ 撤掉修复测试仍全绿**。给 R-92 加守卫时，注释里写了「这里用 `flex: none` 从根上钉住…」，而断言是 `toMatch(/flex:\s*none\|flex-shrink:\s*0/)` —— `declarations()` 原来只按 `{`…`}` 切片、**不剥注释**，于是正则命中的是**注释里那五个字**。反向验证（撤掉 `flex: none`）时测试**依旧全绿**，差一点就当「守卫有效」收下 —— 与 R-85（两条路径产出同一输出）同族，但成因是断言匹配到了注释里的自然语言 | 用「读源码文本 + 正则」写守卫，且被断言的那组声明里带注释（本仓注释里常引用属性名 / 属性值，属常态） | 反向验证里**撤掉修复但测试不红**。**自查一句话**：这条正则，可能匹配到注释吗？ | ① `declarations()` 切片后先剥注释再返回（`.replace(/\/\*[\s\S]*?\*\//g, '')`）；② 反向验证必须真做，且**逐条看挂了哪几条**（见 R-85）；③ 注释里尽量不复述属性值 | **MITIGATED**（2026-09-23：`sopCenterSizing.test.ts` 的 `declarations()` 已剥注释；重跑反向验证 1 红 3 绿，恢复后 4/4 绿） |
| R-94 | **⭐ 低维模板上「总槽差异下限不可达」看着像缺陷，其实等价于「有限预算内的局部 max-min 优化」—— 按「让下限可达」去修，多样性反而更差**。`farthestPointSample` 的两个总槽下限（`minTotalNear` 默认 7 / `minTotalFar` 默认 6）是**为 13~14 维真实资产挑的常数**，而一个维度最多只能差异 1 ⇒ 模板维度数 < 7 时 `hamming < minTotal` **恒真**、判据永不满足 ⇒ 每条采样都打满 120 轮重掷（4~6 维 30 条实测 **3481 次 = 1 + 29×120**），收尾靠 best-so-far 择优。`minDomNear` 在 R-68 已按 `dom.length` 收敛、`minTotalNear` 没收敛 ⇒ 看起来像「同一类坑只修了一半」。**2026-09-23 实测否掉了这个判断**：4 维模板（30/8/6/5）× 30 条 × 8 个种子、同一 seed 同输入对照 —— 现状 **20.0/30 个不同歌名包、重复对子 12.3**；下限收敛到 `ceil(n/2)` → 18.4 / 16.0；收敛到 `ceil(n×3/4)` → 18.8 / 14.0。**「恒真 + 跑满预算 + 取过程最优」实质是在做「最大化与窗口的最小差异」，多样性反而更好**（收敛后只要「达到下限」就退出，即「够用就行」）。 | 看到 `minTotalNear`/`minTotalFar` 打满预算，或想给它加 clamp / 按维度数收敛时 | 症状：重掷次数恰为「条数 × 120」。判据：**别只看 `attempts`，要对照两个实现「同一输入下不同内容槽取值数」与「重复对子数」**。**自查一句话**：我给这个下限加收敛，是让结果更好了，还是只是让 `attempts` 数字更小了？ | ① **不改**（2026-09-23 的改动已回退，`campaignRecipe.ts` 保持原样，并在下限旁留了指向本条目的警告注释）；② 性能不是理由：打满预算实测 **2.4ms / 批（30 条）**，最坏形状（4 维 × 400 候选）1.4ms；高维资产下限可达、本来就不打满；③ 若将来真要动，**零回归门槛 = 13/14 维真实资产逐位不变**，判据只能是「下限 > 期望差异 `Σ(1 - 1/池大小)` 才收敛」，绝不能无条件 `min(下限, n/2)`；④ 「出图太相似」的正解在**模板侧**（版式写死在正文、没有版式维度），与引擎无关 | **OPEN**（2026-09-23 实测；改动已回退，登记为「禁止再修」） |

> **2026-09-22 第二轮修订（同日 22:40，TB-106 续）**
>
> ① **「还没量到宽度」的连带坑已修**：`AssetBatchView.tsx` 的测量 effect 依赖里只有 `measure`
> （`useCallback([])` 恒定）⇒ **一生只跑一次**；首帧若落在空态（两个 ref 都是 `null`，函数直接 return），
> `ResizeObserver` **从此永不建立** ⇒ `layoutWidth` 恒为 0 ⇒ 卡片按 **1px** 渲染（「在 DOM 里但看不见」），
> 而且不报错、不留占位。已把 `hasMeasurableContent` 加进依赖（空 → 非空必补一次测量），
> 并让 `cardWidth` 在 `layoutWidth === 0` 时保持 0、由 `visibleItems` 拦住不渲染
> （宁可空这一帧，也不要 1px 幻影卡片）。
> 守卫用例：`AssetBatchView.test.tsx`「never renders 1px phantom cards while the layout width is unknown」；
> **反向验证精确命中 1 条**（把 `cardWidth` 改回 `Math.max(1, …)` → 恰好那条红）。
> ⚠️ 写这类用例**必须 `try/finally` 卸载 renderer**：本文件后续用例共用 `useAssetLibraryStore`，
> 失败时漏掉 `unmount()` 会把「查看来源任务」那 3 条滚动用例一起带红，产生 3 条迷惑性连带失败
> （本次实测踩到）。
>
> ② **查询失败自动补试**：目录查询失败后自动补试**一次**（600ms 退避，成功后计数归零），
> 再失败才停在提示条 / 失败态等人工作 —— 瞬时 IPC 抖动不该让用户自己去找「重试」按钮。
>
> ③ **「内存陈旧时会把回收站里的图剔掉」经复核不成立，撤销该条警报**：
> `resolveEffectiveAssets` 里 `live.status !== asset.status → 剔除` 只在「内存与库对同一素材状态不一致」时
> 才有影响。本应用**单实例**，所有状态变更都经过 store（同时写内存与库），而直写库的批量路径
> （导入 / 恢复 / 迁移）后面都跟 `hydrate()` 重灌内存 ⇒ **内存只可能比库新，不可能更旧**，
> 「库说 trashed、内存还说 active」这条路径**不可达**。判据保留（用户刚回收时立即从活跃网格消失是预期行为）。
>
> ④ **未做，留待拍板**：启动 `hydrate()` 的 **200 条窗口**要不要全量化（`hydrateFull` 会拉长启动时间）；
> 把分页快照提到 `features/assetLibrary/store.ts` 可彻底消掉「加载中」那一帧，但该文件挂着另一条写线的
> 未提交改动，本轮**一个字节都没碰**（R-09 / R-79）。

---

## 登记规则

1. **踩到新坑当场登记**，不要只写进日志 —— 日志会被压缩，本文件是长期资产。
2. 每条必须能回答：**什么条件下会发生** / **怎么发现** / **怎么避免或兜住**。
3. 根因消除后把状态改为 `CLOSED`，**不要删除**（保留"曾经有过这个坑"的信息）。
4. **一个事实一个家**：配方写进 `docs/tangbao-ops-runbook.md`，本文件只登记风险与对策，不复述配方。
