# 糖包配置规范

> **本文是「中控台配置长什么样」的唯一真相源。** 其它地方（ADR-0014、架构约束第十章、
> 代码注释）提到配置结构时只留指针，细节一律以本文为准。
>
> - **状态**：**已落地（v9）** —— 代码、示例、schema 与本规范一致；这笔改动的账见 §八
> - **日期**：2026-09-22
> - **相关**：[ADR-0014 配置包以项目树为骨架](adr/0014-tree-rooted-config-bundle.md)、
>   [ADR-0010 参数中心的层级口径](adr/0010-param-center-scope-correction.md)、
>   [ADR-0013 参与产出跟随方向](adr/0013-participation-follows-direction.md)
> - **可替换的完整示例**：[`examples/tangbao.config.example.json`](examples/tangbao.config.example.json)
> - **机器可读版本**：[`examples/tangbao.config.schema.json`](examples/tangbao.config.schema.json)

---

## 零、30 秒版

配置 = **一个 ZIP 信封 + 一份 config.json 本体**。

```
tangbao-config-20260922-153000.zip
├─ config.json          ← 配置本体，人可读、可手改、可单独替换
├─ manifest.json        ← 打包元信息（谁导的、包多大、含不含图片）
└─ assets/              ← 配置引用到的图片（水印 LOGO、样例背景），按内容哈希命名
   └─ <sha256>.png
```

config.json 里只有 **三块**：全局默认（`defaults`）、项目树（`tree`）、未归属水印
（`unassignedWatermarks`）。

**三条铁律**

| 铁律                       | 一句话                                                                                                     |
| -------------------------- | ---------------------------------------------------------------------------------------------------------- |
| **树是根**                 | 除了「全局默认」，所有配置都必须挂在一个树节点上。新增任何配置项，先回答「它挂在哪一层」，不许平铺进包。   |
| **没写 ≠ 空**              | 字段**没写** = 跟着上一层走；**写了个空** = 我就要空。两者的区别是整套继承的地基，逐字段的空值语义见 §四。 |
| **不放密钥、不放安全边界** | API Key、IPC 白名单、CSP 之类一律不进配置包 —— 它会被放到内网共享盘，等于谁都能拿到。详见 §九。            |

---

## 一、整体分层

### 1.1 信封（ZIP）

| 条目            | 必需 | 说明                                                                            |
| --------------- | ---- | ------------------------------------------------------------------------------- |
| `config.json`   | 是   | 配置本体。见 §1.2。                                                             |
| `manifest.json` | 是   | 打包元信息。谁在什么时候导的、含不含任务/图片、包内文件清单与校验和。           |
| `assets/`       | 否   | 配置引用到的图片资源（水印 LOGO、样例背景）。按内容哈希命名，重复引用天然去重。 |
| `images/`       | 否   | 素材与任务的原图。只有「完整包」才有；只发配置时不带。                          |

**为什么要信封**：水印预设里引用着 LOGO 图片，纯 JSON 装不下二进制。ZIP 只解决"装得下"，
配置本身仍然是一份可以直接打开、直接替换的 JSON。

### 1.2 本体（config.json）

```
config.json
├─ $schema                  声明；编辑器据此校验与悬浮说明
├─ format                   文件头（版本、导出信息）
├─ defaults                 全局默认层 ← 树上没表态的项都取这里
├─ watermarkLibrary         水印库的库级配置（LOGO 列表与顺序、标识符、水印全局适配）
├─ tree                     项目树（第一层是产品线）
└─ unassignedWatermarks     没归属到任何产品的水印预设
```

### 1.3 八个模块

配置按**内容**分八个模块。模块用来讲解与排查，**在文件里不额外分组** ——
字段按模块顺序排列，一眼扫下来是连续的。

| #   | 模块           | 回答的问题                     | 谁能覆盖                     |
| --- | -------------- | ------------------------------ | ---------------------------- |
| ①   | **渠道与尺寸** | 投哪些渠道、每个渠道要几个尺寸 | 仅全局（字典式，节点只引用） |
| ②   | **输出位置**   | 产物写到哪                     | 全局 → 节点 → 节点内按渠道   |
| ③   | **命名**       | 文件叫什么                     | 仅全局                       |
| ④   | **水印**       | 加什么水印                     | 全局 → 节点 → 节点内按渠道   |
| ⑤   | **产出选择**   | 哪些方向允许跑、这次产出到哪些 | 仅全局                       |
| ⑥   | **画面**       | 怎么适配尺寸、横竖版           | 仅全局                       |
| ⑦   | **分发**       | 产出后怎么按天排期             | **排期跟方向**（铺几天 / 跳过周末），其余全局 |

| ⑧ | **水印库** | LOGO 列表与顺序、标识符、水印全局适配 | **仅全局**（库级一份，不跟节点走） |

> 为什么只有三个模块能被节点覆盖：**规格与开关收归全局，只有「这个方向交付到哪、
> 带什么水印、投哪些渠道」是方向自己的事**。给每个模块都开一层节点覆盖，会让
> 「到底哪个值生效」需要递归推理 —— 这正是 ADR-0010 / ADR-0013 收敛掉的东西。

---

## 二、命名规范

| 对象        | 规则                                                                            | 例子                                     |
| ----------- | ------------------------------------------------------------------------------- | ---------------------------------------- |
| 字段名      | 小驼峰（camelCase），不用下划线、不用短横线                                     | `watermarkPresetIds`、`maxSizeKb`        |
| 布尔字段    | 肯定式动词/形容词，**不用 `is`/`has` 前缀**（与现有代码一致，避免全仓改名）     | `enabled`、`pinned`、`randomize`         |
| 复数数组    | 用复数名词                                                                      | `sizes`、`channels`、`children`          |
| 目录字段    | 以 `Dir` 结尾                                                                   | `outputDir`、`targetDir`                 |
| 路径字段    | 以 `Path` 结尾（指具体文件）                                                    | `sampleBackgroundPath`                   |
| id 列表字段 | 以 `Ids` 结尾，且**名字里带上是「什么东西」的 id**                              | `watermarkPresetIds`、`selectedMediaIds` |
| 渠道 id     | 内置：`gdt`/`baidu`/`vendor`/`toutiao`；自建：`custom-<8位随机>`；保留：`clean` | `gdt`、`custom-a1b2c3d4`                 |
| 尺寸 id     | `<渠道id>-<宽>x<高>`                                                            | `gdt-1280x720`                           |
| 节点 id     | 以业务含义命名，不用序号                                                        | `dir-moon`（而不是 `n-17`）              |

### 术语对照（文件里的名字 ↔ 界面上的说法）

| 文件里                 | 界面上叫   | 说明                                                                                      |
| ---------------------- | ---------- | ----------------------------------------------------------------------------------------- |
| `media` / `Media`      | **渠道**   | 同一个东西的两个名字：代码从早期就叫 `PostprocessMedia`，一直没改。**别再加第三套叫法。** |
| `clean`（保留渠道 id） | 纯净版     | 无水印、不压缩、沿用生成尺寸                                                              |
| `collection` / node    | 项目树节点 | 产品线 → 产品 → 方向 → 扩展层                                                             |
| `preset`               | 水印预设   | 缩写只在 `compositeV2*` 模块内部用                                                        |

> ⚠️ **命名模板里的 token 是例外**：`{media}` 已经落在所有用户已配好的模板里，
> 改名等于让全部模板失效。所以 token 永久叫 `{media}`，即使界面上叫"渠道"。

---

## 三、字段规范

图例：**必填** = 缺了整包拒收；**选填** = 缺了走默认值。

### 3.1 `format` —— 文件头

| 字段           | 类型    | 必填 | 默认 | 取值范围 / 说明                                                              |
| -------------- | ------- | ---- | ---- | ---------------------------------------------------------------------------- |
| `kind`         | string  | ✅   | —    | 固定 `"tangbao-config"`。用来一眼认出这是配置本体。                          |
| `version`      | integer | ✅   | —    | 固定 `9`。版本不认识时整包拒收，**不做猜测性解析**。                         |
| `exportedAt`   | string  | ✅   | —    | ISO 8601 时间。排查「这份包什么时候发的」用。                                |
| `appVersion`   | string  | —    | `""` | 导出这份配置的糖包版本号，如 `"0.3.2"`。                                     |
| `skippedNodes` | integer | —    | `0`  | 导出时被跳过的回收站节点数。≥0。**必须报数**，否则"少了几十个方向"查不出来。 |

### 3.2 `defaults` —— 全局默认层

**它不对应树上的任何节点**，是「谁都没表态时用这个」。字段按模块分组如下。

#### ① 渠道与尺寸

| 字段                         | 类型        | 必填 | 默认        | 取值范围 / 说明                                                                                   |
| ---------------------------- | ----------- | ---- | ----------- | ------------------------------------------------------------------------------------------------- |
| `channels`                   | `Channel[]` | ✅   | 内置 4 渠道 | 全局唯一的字典。**允许空数组**（用户把渠道全删了）—— 空数组表示"没有渠道可用"，与"没写"是两回事。 |
| `Channel.id`                 | string      | ✅   | —           | 渠道内唯一。见 §二。                                                                              |
| `Channel.name`               | string      | ✅   | —           | 进 `{media}` 命名段。                                                                             |
| `Channel.sizes`              | `Size[]`    | ✅   | —           | 可空数组。                                                                                        |
| `Size.id`                    | string      | ✅   | —           | 约定 `<渠道id>-<宽>x<高>`。                                                                       |
| `Size.width` / `Size.height` | integer     | ✅   | —           | ≥1（像素）。                                                                                      |
| `Size.maxSizeKb`             | integer     | ✅   | `0`         | ≥0。**0 = 不压缩**。负数无意义，归一化时按 0 处理（而不是丢掉整条尺寸）。                         |
| `Size.enabled`               | boolean     | ✅   | `true`      | 这个尺寸参不参与产出。                                                                            |

> **渠道本身没有 `enabled` 字段**。ADR-0013 删掉了它 —— 它与「参与产出」对产出的影响完全等价，
> 两个开关说同一件事只会制造「勾了却不产出」的静默失效。**「这个渠道暂时不投」= 不勾参与产出。**

#### ② 输出位置

| 字段              | 类型                       | 必填 | 默认 | 取值范围 / 说明                                                                                                                                                                                                                                                    |
| ----------------- | -------------------------- | ---- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `outputDir`       | string                     | —    | `""` | 绝对路径。**空串 = 用应用默认位置**（本地保存目录下的 `postprocess`）。                                                                                                                                                                                            |
| `mediaOutputDirs` | `Record<渠道id, string[]>` | —    | `{}` | 每个渠道 1~2 个目录。**空数组或缺键 = 该渠道用 `outputDir`**（默认位置永远是兜底，不会被渠道配置弄丢）。<br>写 2 个 = **双写**（同一份产物两处各写一份，文件名相同）。<br>**上限 2 是刻意的**：导出位置是写盘放大（文件数 × 位置数），放开后一次误配就能写满磁盘。 |

#### ③ 命名

| 字段          | 类型   | 必填 | 默认                                                | 取值范围 / 说明                                                                   |
| ------------- | ------ | ---- | --------------------------------------------------- | --------------------------------------------------------------------------------- |
| `namePattern` | string | —    | `{date}-{product}-{direction}-{media}-{size}-{seq}` | 见下方 token 表。**必须含 `{seq}`**，否则同批次产物互相覆盖。空白模板回落默认值。 |
| `creator`     | string | —    | `""`                                                | 进 `{creator}` 段。空串 = 这一段被删掉。                                          |

**可用 token**：`{date}` `{line}` `{product}` `{direction}` `{creator}` `{media}` `{size}`
`{seq}` `{preset}`。

两条排版规则：① **取值为空的段整段删除**（不会出现 `A--B`）；② **未知 token 原样保留**，
便于在文件名里直接看出模板写错了（界面上另有提前提示，两者配合，不静默吞掉）。

#### ④ 水印

| 字段                 | 类型       | 必填 | 默认 | 取值范围 / 说明                                                                                                                                            |
| -------------------- | ---------- | ---- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `watermarkPresetIds` | `string[]` | —    | `[]` | 引用哪些水印预设。**`[]` = 不加水印**（显式）。数组顺序即产出顺序。<br>配 N 个预设 → 每个渠道尺寸各出 N 套，**产出量按预设数倍增**（先估磁盘再改这个值）。 |

#### ⑤ 产出选择

这三个字段最容易混，务必分清：

| 字段                       | 类型       | 必填 | 默认        | 回答的问题               | 说明                                                                                                 |
| -------------------------- | ---------- | ---- | ----------- | ------------------------ | ---------------------------------------------------------------------------------------------------- |
| `selectedCollectionIds`    | `string[]` | —    | `[]`        | **无归属的图投到哪**     | 仅兜底：图片**没有归属方向**（手工拖入 / 旧数据）时才用它。                                          |
| `selectedMediaIds`         | `string[]` | —    | `["clean"]` | **默认投哪些渠道**       | 这里的渠道才产出。不含 `clean` 就没有纯净版。                                                        |
| `savedTargetCollectionIds` | `string[]` | —    | `[]`        | **手动跑这次投哪些**     | 「记住配置」存的产出目标，顺序即产出顺序。**只作用于手动触发**；**`[]` = 没记住** → 每张图按自己的归属方向产出（原行为）。 |

> **产出目标按触发来源分流**（`taskPostprocess.ts`，判定按 `source` 分开做）：
>
> - **自动触发**：去向 = **图片归属方向**；**不读** `savedTargetCollectionIds`。
>   整个后台跑不跑由设置里的「生成完自动跑后处理」决定（`AppSettings.autoPostprocess`，**默认关**）；
>   某个方向要不要变体由它自己的「自动后处理」开关决定（默认开）。
> - **手动触发**（素材库点「跑后处理」）：读 `savedTargetCollectionIds`，可以跨产品 / 跨方向选择；
>   既不受总开关限制、也不受方向级开关限制。
>
> ⚠️ **2026-09-23 撤掉了「启用范围」这一层**：`selectedCollectionIds` 原先还被当作
> 「哪些方向参与自动后处理」的白名单（提示码 `PP-SCOPE-001`，空 = 一个都不跑）。
> 撤销理由：它与方向级开关说同一件事、判定却各写一套，用户在两处之间迷路；
> 而批量出图的人本来就要所有方向都参与。现在它只在「图片没有归属」时兜底。

#### ⑥ 画面

| 字段                 | 类型           | 必填 | 默认        | 取值范围 / 说明                                                                                                                                                                                                                                                                                               |
| -------------------- | -------------- | ---- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `direction`          | string \| null | —    | `null`      | `landscape` / `portrait` / `square` / `null`。**`null` = 按源图尺寸自动判定**。                                                                                                                                                                                                                               |
| `fitMode`            | string         | —    | `crop-fill` | `crop-fill` = 等比放大填满 + 裁边；`contain-blur` = 完整画面居中 + 四周补模糊底；`stretch` = 填满但比例被改变。<br>**全局一套，不参与节点继承**（做成方向级会让"为什么这张变形"要递归推理）。<br>未知值**必须回落默认值**，不能原样传下去 —— 渲染器遇到不认识的模式会中途抛错，整张产出作废且坏值会一直留着。 |
| `autoCompanionClean` | boolean        | —    | `true`      | 勾了任一渠道时，额外多产一份无水印原图。                                                                                                                                                                                                                                                                      |

#### ⑦ 分发

产出写盘后按天铺开：把产出文件夹**同级**改名成「排期日开头 + 命名模板其余段」的文件夹。

| 字段           | 类型    | 必填 | 默认    | 取值范围 / 说明                                                                |
| -------------- | ------- | ---- | ------- | ------------------------------------------------------------------------------ |
| `enabled`      | boolean | —    | `false` | 是否启用。**默认关闭**：它会在磁盘上移动文件，绝不能靠默认值把用户的产物搬走。 |
| `days`         | integer | —    | `1`     | ≥1。小于 1 的取值一律按 1 处理。                                               |
| `randomize`    | boolean | —    | `false` | 打乱后再平均分配，避免排期出现肉眼可见的规律。                                 |
| `skipWeekends` | boolean | —    | `false` | 只按工作日排期。                                                               |
| `renameMode`   | string  | —    | `date`  | `date` 替换文件名里的日期段；`sequence` 按排期文件夹名 + 序号重排。            |
| `modifyMd5`    | boolean | —    | `false` | 追加随机字节改变 md5，规避投放平台的「重复素材」判定。                         |
| `targetDir`    | string  | —    | `""`    | 分发根目录。空串 = 就在产出位置整理（同级改名，不换位置、不产生副本）。        |

> **没有「起始日期」字段（2026-09-23 移除）**：排期起算日由程序取**产出当天**
> （与命名模板 `{date}` 同源），用户只决定「铺几天 + 跳不跳周末」。
> 手填日期的后果是双向的：用户不知道该填哪天；填成产出当天则会算出
> 「目标目录 == 产出目录」⇒ 每个文件与**自己**撞名 ⇒ 整目录自我复制一份 `-2`。
>
> **没有「搬运方式（复制 / 移动）」字段（2026-09-23 移除，TB-117）**：搬运恒定是移动。
> 这套目录结构下复制不成立 —— 起算日 = 产出当天时第 1 天就是原地（无副本可言）；
> 第 2 天若复制，第 1 天的文件夹里会留着所有天的文件，排期整个错乱。
>
> **目录结构（TB-117 起）**：产出文件夹**同级**出现排期文件夹，不再在产出文件夹里套一层纯日期子文件夹。
> `D:\导出\20260922-高颜值-头条\a.jpg` 铺 3 天 →
> `D:\导出\20260923-高颜值-头条\a.jpg`、`…\20260924-高颜值-头条\b.jpg`、`…\20260925-高颜值-头条\c.jpg`。
> 排期日与文件当前位置重合（第 1 天）时**不搬、不复制**；模板里没有 `{date}` 时排期日前置。
> `targetDir` 填成输出根目录本身与留空同义（都是同级改名）—— 不会像旧实现那样被算回产出文件夹自身。

### 3.3 `tree` —— 项目树

**同级按 `order` 升序，`order` 相同则按 `name` 排**。所以同级 `order` 不要重复。

| 字段               | 类型                | 必填 | 默认    | 取值范围 / 说明                                                                                                                                                 |
| ------------------ | ------------------- | ---- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `id`               | string              | ✅   | —       | 被「产出目标 / 水印归属 / 无归属兜底」引用。改它等于改动所有引用。                                                                                               |
| `name`             | string              | ✅   | —       | 进 `{line}` / `{product}` / `{direction}` 命名段。                                                                                                              |
| `kind`             | string              | —    | —       | `line` / `product` / `direction` / `extra`（第 4 层及以后）。**刻意不强制**：自建节点没有内置前缀，反推迟早出错；算不出就省略，恢复时按 `parentId` 挂回去即可。 |
| `order`            | integer             | ✅   | —       | 同级排序位。                                                                                                                                                    |
| `color`            | string              | —    | —       | 节点色标。                                                                                                                                                      |
| `pinned`           | boolean             | —    | `false` | 是否置顶。                                                                                                                                                      |
| `updatedAt`        | integer             | —    | —       | 参数最后改动时间（毫秒）。纯记录，不参与判定。                                                                                                                  |
| `overrides`        | `NodeOverrides`     | —    | —       | 本节点的覆盖值。**整个对象缺席 = 全部继承**。                                                                                                                   |
| `watermarkPresets` | `WatermarkPreset[]` | —    | —       | 挂在这个节点下的水印库。**判据是"有没有水印挂在它下面"，不看层级标记** —— 所以自建节点也能当库主。其余层级省略即可。                                            |
| `children`         | `Node[]`            | —    | `[]`    | 子节点。                                                                                                                                                        |

#### `overrides` —— 节点覆盖

**这一节能覆盖什么，是刻意限定的**：只有「这个方向交付到哪、带什么水印、投哪些渠道」
是方向自己的事；规格类的（尺寸、命名模板）一律收归全局。

⚠️ **分发是例外**：排期（`days` / `skipWeekends`）2026-09-23 起回到节点层
（见 [ADR-0017](adr/0017-distribution-schedule-follows-direction.md)），
只有「怎么搬」的那几个字段（启用 / 搬运方式 / 重命名 / 打乱 / 改 md5 / 目标目录）留全局。

| 字段                 | 类型                    | 取值范围 / 说明                                                                                                    |
| -------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `enabled`            | boolean                 | `false` = 归属此方向的图片不产出变体。**缺省 = 参与**。                                                            |
| `outputDir`          | string                  | 空串 = 用默认输出位置（等价于没写）。                                                                              |
| `watermarkPresetIds` | `string[]`              | **`[]` = 显式「这个方向不加水印」**；字段缺席 = 继承上一层。                                                       |
| `selectedMediaIds`   | `string[]`              | **`[]` = 这个方向一个渠道都不投**；字段缺席 = 继承上一层。数组顺序即产出顺序。                                     |
| `byMedia`            | `Record<渠道id, {...}>` | 按渠道再细分，**只对本节点生效，不往父节点继续找**。只开放两项：`outputDirs`（1~2 个目录）、`watermarkPresetIds`。 |

> ⚠️ `byMedia` 里**没有** `namePattern` / `selectedMediaIds` / `distribution` / `direction`：
> 它们要么是全局规格，要么是「这个节点要不要跑」的开关。按渠道分只会让
> 「到底哪个值生效」需要递归推理。**类型上保持窄，比事后靠约定约束可靠。**

### 3.4 `watermarkLibrary` —— 水印库的库级配置

水印库有两半：**库级配置**（这里）和**预设本体**（§3.6，按 `productId` 挂到树上）。库级这半
不属于任何节点，全局一份。

| 字段                   | 类型                  | 默认         | 取值范围 / 说明                                                                                     |
| ---------------------- | --------------------- | ------------ | --------------------------------------------------------------------------------------------------- |
| `logos`                | `ProjectLogo[]`       | `[]`         | LOGO 列表，每项 `{ id, name, assetId }`。**图片按 `assetId` 落库并随包打包**（见 §十 10.4）。       |
| `logoOrder`            | `string[]`            | `[]`         | LOGO 的展示顺序（元素是 `logos[].id`）。                                                            |
| `libraryPath`          | string                | `""`         | LOGO 库文件夹。**本机路径**，换机器不保证可用（见 §十 10.3）。                                      |
| `globalFitMode`        | string                | `crop-fill`  | 水印画布的适配方式：`crop-fill` / `contain-blur` / `stretch`。**与后处理的 `fitMode` 不是同一个。** |
| `identifier`           | `{ text, placement }` | 由归一化兜底 | 水印标识符（署名）。`placement` = `prefix` / `suffix` / `both`；`text` 前后的空格**有意保留**。     |
| `backgroundFolders`    | `string[]`            | —            | 背景图文件夹。**本机路径**。                                                                        |
| `recursiveBackgrounds` | boolean               | —            | 背景图是否递归扫子目录。                                                                            |

> ⚠️ `identifier` 与预设**不是一回事**：预设回答"整套水印长什么样"，标识符回答"往文案上贴的署名"。
> 预设里没有任何可用文字水印时，它会退化成左下角的一个文字层（样式见 `IDENTIFIER_FALLBACK_STYLE`）。

### 3.5 `unassignedWatermarks` —— 未归属的水印

| 项     | 类型                | 说明                                                                                                                                                                    |
| ------ | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 数组项 | `WatermarkPreset[]` | `productId` 缺省（老数据）或指向一个**已被删除**的产品时，按产品分组的循环会漏掉它们。**必须单独兜住** —— 漏掉 = 静默丢配置，用户只会觉得"水印少了几套"却查不出为什么。 |

### 3.6 `WatermarkPreset` —— 水印预设

| 字段                   | 类型             | 必填 | 说明                                                                                                          |
| ---------------------- | ---------------- | ---- | ------------------------------------------------------------------------------------------------------------- |
| `id`                   | string           | ✅   | 被 `watermarkPresetIds` 引用。                                                                                |
| `name`                 | string           | ✅   | 进 `{preset}` 命名段。                                                                                        |
| `productId`            | string           | —    | 归属产品（节点 id）。**缺省或空串 = 未分配** → 不进任何产品的库，界面上单独成区，可一键指派。                 |
| `baseCanvas`           | `{width,height}` | ✅   | 画布基准，层里的比例值都以它为基准。                                                                          |
| `sampleBackgroundPath` | string           | —    | 预览用样例背景。空串 = 无。                                                                                   |
| `layers`               | `Layer[]`        | ✅   | 图层数组，自下而上渲染。**层是水印编辑器的事**，完整结构见 `src/features/composite/lib/compositeV2Types.ts`。 |
| `updatedAt`            | integer          | ✅   | 最后修改时间（毫秒）。                                                                                        |

层的三种类型：`text`（文字）、`image`（图片）、`logo`（品牌标识）。每层必带
`id` / `name` / `type` / `visible` / `opacity` / `position`。
两个**正向判定**字段（反向字段会让老数据整批出问题）：

- `orientation`：`"vertical"` 才竖排；缺省或任何其它值都按横排。
- `withIdentifier`：**缺省 = 带**署名；只有显式 `false` 才是不带。

---

## 四、继承与覆盖

### 4.1 继承链

```
defaults（全局默认）
  └ 产品线        overrides 覆盖上一层
      └ 产品      overrides 覆盖上一层
          └ 方向  overrides 覆盖上一层  ← 产出时实际取用的值
```

**逐级向上找，谁先表态就听谁的。** 只有 §3.3 表里列出的四项参与继承，
其余字段只存在于 `defaults`。

### 4.2 「没写」与「空」—— 逐字段对照（**最容易出错的一节**）

| 字段                       | 字段缺席（没写）       | 写了空值（`[]` / `""` / `null`）           |
| -------------------------- | ---------------------- | ------------------------------------------ |
| `watermarkPresetIds`       | 继承上一层             | `[]` = **这个方向不加水印**（显式覆盖）    |
| `selectedMediaIds`         | 继承上一层             | `[]` = **一个渠道都不投**                  |
| `outputDir`                | 继承上一层             | `""` = 用默认输出位置（≈ 没写）            |
| `mediaOutputDirs[渠道]`    | 继承（用 `outputDir`） | `[]` = 该渠道用 `outputDir`                |
| `savedTargetCollectionIds` | 用默认值 `[]`          | `[]` = 没记住 → 按图片归属方向产出         |
| `channels`                 | 用内置渠道表           | `[]` = **真的没有渠道**（不复活内置表）    |
| `namePattern`              | 用默认模板             | `""` / 空白 = 回落默认模板（不是空文件名） |
| `fitMode`                  | 默认 `crop-fill`       | 未知值 = 回落默认值（**不原样传下去**）    |

**判断"有没有配"时不能用 `length`** —— 空数组是有效值。这与 `undefined`（没表态）是两件事。

### 4.3 恢复顺序（不可交换）

```
① 立树  →  ② 灌每个节点的参数  →  ③ 放水印库  →  ④ 挂渠道与输出位置
```

顺序反了会出现「引用了不存在的水印 / 渠道」。**而引用关系是字符串，写的时候不校验，
出问题要到产出那一刻才炸** —— 这类错误极难自查。

「同 id 节点覆盖 + 本地独有的保留」也是刻意的：彻底替换会把使用者自建的方向、
连同挂在上面的素材一起变成孤儿，而换来的只是"树长得更一致"。

---

## 五、完整示例

可直接替换的完整示例见 [`examples/tangbao.config.example.json`](examples/tangbao.config.example.json)
（已通过 `examples/tangbao.config.schema.json` 校验）。骨架如下 —— 注释只在本节出现，
**正式文件是纯 JSON，不带注释**：

```jsonc
{
  "$schema": "./tangbao.config.schema.json",

  "format": {
    "kind": "tangbao-config",
    "version": 9,
    "exportedAt": "2026-09-22T15:30:00.000Z",
    "appVersion": "0.3.2",
    "skippedNodes": 6, // 回收站里跳过了 6 个节点
  },

  "defaults": {
    "channels": [/* 4 个渠道 / 15 个尺寸，完整内容见示例文件 */],

    "outputDir": "", // 用应用默认位置
    "mediaOutputDirs": { "gdt": ["D:\\交付\\广点通"] }, // 广点通单独指定，其余走 outputDir

    "namePattern": "{date}-{product}-{direction}-{media}-{size}-{seq}",
    "creator": "阿伟",

    "watermarkPresetIds": [], // 全局默认不加水印，由各方向自己表态

    "selectedMediaIds": ["clean"], // 默认只产纯净版
    "selectedCollectionIds": ["line-home", "prod-vacuum", "dir-moon"],
    "savedTargetCollectionIds": ["dir-moon", "dir-sun"],

    "direction": null, // 按源图尺寸自动判定
    "fitMode": "crop-fill",
    "autoCompanionClean": true,

    "distribution": { "enabled": false, "days": 1, "mode": "copy" /* … */ },
  },

  "watermarkLibrary": {
    "logos": [{ "id": "logo-…", "name": "品牌LOGO.png", "assetId": "…" }], // 图片按 assetId 落库并打包
    "logoOrder": ["logo-…"],
    "libraryPath": "", // 本机路径，换机器不保证可用
    "globalFitMode": "crop-fill", // 与后处理的 fitMode 不是同一个
    "identifier": { "text": " · @糖包", "placement": "suffix" },
  },

  "tree": [
    {
      "id": "line-home",
      "name": "智能家居",
      "kind": "line",
      "order": 0,
      "color": "#7F77DD",
      "children": [
        {
          "id": "prod-vacuum",
          "name": "扫地机器人",
          "kind": "product",
          "order": 0,
          "watermarkPresets": [/* 这个产品的水印库（含 LOGO 层） */],
          "children": [
            {
              "id": "dir-moon",
              "name": "月亮",
              "kind": "direction",
              "order": 0,
              "overrides": {
                "enabled": true,
                "outputDir": "D:\\交付\\扫地机\\月亮",
                "watermarkPresetIds": ["wm-vacuum-01"],
                "selectedMediaIds": ["gdt", "baidu"],
                "byMedia": { "baidu": { "outputDirs": ["D:\\交付\\百度\\扫地机月亮"] } },
              },
              "children": [],
            },
            {
              "id": "dir-sun",
              "name": "太阳",
              "kind": "direction",
              "order": 1,
              "overrides": {
                "watermarkPresetIds": [], // 显式：这个方向不加水印
                "selectedMediaIds": ["clean", "toutiao"],
              },
              "children": [],
            },
          ],
        },
        { "id": "prod-purifier", "name": "空气净化器", "kind": "product", "order": 1, "children": [/* … */] },
      ],
    },
  ],

  "unassignedWatermarks": [],
}
```

---

## 六、校验

| 层         | 手段                                                                                                                                |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| **编辑期** | 文件带 `$schema` → 编辑器按 [`examples/tangbao.config.schema.json`](examples/tangbao.config.schema.json) 实时校验，字段说明悬浮可见 |
| **导入前** | 结构体检：不是对象 / 版本不认识 / 缺 `tree` / 缺 `defaults` / 缺 `channels` → **整包拒收**，不拿半个包去覆盖用户配置                |
| **归一化** | 坏条目**逐条丢弃**（缺 id、宽高非正），而不是整份配置回退 —— **保住用户其余的编辑**                                                 |
| **静默项** | 兜底字段必须补齐默认值（`unassignedWatermarks` → `[]`、`skippedNodes` → `0`），缺字段不能读成 `undefined`                           |

---

## 七、版本与兼容

- `format.version` 是**整数**，当前 `9`。结构变化才 +1；**纯新增可选字段不 bump**
  （缺字段 → 回落旧行为，没有需要折算的旧语义）。
- 版本不认识 → **整包拒收并说明原因**，不做猜测性解析。
- ⚠️ **两个版本号别混**（2026-09-22 踩过，R-87）：

  | 版本号                                    | 是什么                     | 谁和谁必须同步                                                  |
  | ----------------------------------------- | -------------------------- | --------------------------------------------------------------- |
  | `manifest.json` 的 `version`              | **包结构**版本             | 导出侧（`exportData` / `exportDataToPath` 各写一处）↔ 导入侧的 `CURRENT_BACKUP_VERSION` |
  | `config.json` 的 `format.version`         | **配置格式**版本（当前 9） | 本文 ↔ `TREE_CONFIG_VERSION`                                     |

  那次不一致是「导出写 8、导入上限停在 7」⇒ **导出的每个包都被自己拒收**，配置同步形同虚设；
  而两边各自的用例都是绿的 —— 因为**没有任何「导出 → 导入」的 round-trip 用例**。
  现在 `backupImport.test.ts` 有一条专门守这个同步。
- 拉取 = **单向覆盖**（发布方那份是权威）：同 id 节点一律以包为准，本地独有的方向保留。
  **拉取前先备份本机配置，备份失败就中止** —— 不做没有退路的覆盖。
- 包内**默认不含 API Key**。

---

## 八、v8 → v9 改动账

> **已全部落地（2026-09-22，TB-100）**。这张表留的是「改了哪些、为什么」—— 复核与回退时按它对。
> **不受向后兼容约束**（糖包尚未交付他人使用），故不做过渡期双写。

| #   | 改动                                                                                                                                                   | 落点                                                                                                     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| 1   | 配置从包内 `treeConfig` 字段拆成独立的一份 `config.json`                                                                                               | `src/store.ts`（导出/导入）、`src/lib/configSync.ts`                                                     |
| 2   | `version`+`exportedAt` 收进 `format`，新增 `kind`/`appVersion`/`skippedNodes`                                                                          | `src/lib/treeConfigBundle.ts`                                                                            |
| 3   | `root` → `defaults`、`nodes` → `tree`                                                                                                                  | 同上                                                                                                     |
| 4   | 节点 `postprocess` → `overrides`；`watermarks` → `watermarkPresets`                                                                                    | 同上 + `src/features/projectTree/types.ts`                                                               |
| 5   | `version` 8 → 9，`validateTreeConfigBundle` 的版本判定同步                                                                                             | 同上                                                                                                     |
| 6   | **删掉过渡期双写**：`compositeState` / `postprocessMediaState` / `assetCollections` 三个重复字段，以及 v7 及更早的老恢复路径                           | `src/store.ts`（导出侧注解 + 导入侧 `restoreTreeConfigBundle` 分支）                                     |
| 7   | 配一份 `$schema`，导入前可选做一次 schema 校验                                                                                                         | `src/lib/treeConfigBundle.ts` + `docs/examples/`                                                         |
| 8   | 映射层加**穷尽守卫**（用 `Record<keyof X, …>` 让漏字段编译报错），挡住「加字段忘改映射」                                                               | `src/lib/treeConfigBundle.ts` + 测试                                                                     |
| 9   | 把散在三处的结构描述改为指向本文                                                                                                                       | `docs/adr/0014-*.md` §一、`docs/architecture-constraints.md` §十、`src/lib/treeConfigBundle.ts` 头注释   |
| 10  | `docs/README.md` 登记本文                                                                                                                              | `docs/README.md`                                                                                         |
| 11  | ⚠️ **补掉「本机选图型水印素材跨机器会丢」的缺口**：导出前把 `layer.asset.kind === 'path'` 也迁成 `stored`（现在只迁 `dataUrl` / `project`，见 §十·C3） | `src/features/composite/lib/compositeAssetMigration.ts`、`src/features/composite/lib/compositeAssets.ts` |

| 12 | **新增 `watermarkLibrary` 节**（§3.4）：把水印库的**库级**字段（LOGO 列表与顺序、标识符、水印全局适配、背景文件夹）从 `compositeState` 那条路搬进 `config.json` —— 这是**删掉双写的前提**，否则这些字段无处落脚 | `src/lib/treeConfigBundle.ts` + `src/store.ts` |

**验收标准（可测）**

1. 导出的 ZIP 内确实有 `config.json`，且通过 schema 校验；导出的 `treeConfig` 字段不再存在。
2. 导出 → 导入 round-trip 后**逐节点字段值一致**（断言字段值，不是"恢复了几条"）。
3. v8 包（含 `treeConfig`）导入时**明确报「版本不认识」并拒收**，不静默什么都不恢复。
4. `format.skippedNodes` 等于回收站节点数；`unassignedWatermarks` 含缺归属的水印。
5. 全量 `npm run verify` 全绿。
6. **跨机器还原**：在一台干净机器上导入后，水印里「从本机磁盘选的图」**不再断图**
   （第 11 项落地后），且 `composite-assets/` 里能查到它对应的条目。

---

## 九、不该放进配置的东西

配置包会被放到**内网共享盘**，能看到那个盘的人都能打开它。所以：

| 类别                       | 例子                                              | 为什么                                                                                          |
| -------------------------- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| **密钥与凭据**             | API Key、代理口令、OAuth token                    | 放共享盘 = 谁都能拿到。API 配置住在设置里，**不进配置包**。                                     |
| **安全边界**               | IPC 路径白名单根、CSP、可信渲染进程校验           | 做成"用户可配置"等于把边界交给用户 = 没有边界。正确做法是从**用户已配置的输出目录反推**受信根。 |
| **本机绝对路径之外的环境** | 机器名、盘符映射、用户目录                        | 换台机器就没意义，只会让导入方多出一堆指不到的条目。                                            |
| **素材索引**               | `generatedAssets`                                 | 树属于配置，**素材索引不属于** —— 发过去只会让别人的素材库里多出指不到的条目。                  |
| **内部实现细节**           | UI 时序（防抖 400ms、hover 延迟 150ms）、动画时长 | 纯粹是噪声，用户不会改，改坏只会制造新的静默点。                                                |

---

## 十、跨机器一致性（「A 机发布 → 全新 B 机拉取」能还原到什么程度）

> 结论先行：**不能保证完全一致。** 但「中控台里改得动的那套配置」能一致。
> 本节的作用是把「哪些地方一定不一样、为什么」写死，免得每次都要重新推一遍。

### 10.1 能一致的部分

项目树（层级 / 名称 / 顺序 / 颜色 / 置顶）、每个方向的参数（参与渠道 / 显式空水印 / 输出位置 /
按渠道覆盖）、水印库（预设、归属 `productId`、LOGO、图层）、渠道与尺寸字典、命名模板与创作者、
画面方向与适配、分发配置、产出目标与无归属兜底 —— 这些都在 `config.json` 里，**逐字段一致**。

**如果你「水印只用水印库里的」**（即从不碰水印编辑器里的「替换图片素材」从本机磁盘选图）：
预设本体、`productId` 归属、每个图层的几何（`position` / `fontSize` / `lineHeight` /
`letterSpacing` / `padding` / `rotation` / `opacity` / `orientation` / `align`）、`baseCanvas`、
标识符配置、`globalFitMode` 全部逐字段一致 ⇒ **B 机上水印布局不会乱**。
唯一会变的是**字体观感**（见 10.4 后半），以及样例背景图（`sampleBackgroundPath` 是本机路径，
只影响编辑器预览，不影响产出）。

### 10.2 一定不一致的（设计如此，不是 bug）

| #   | 差异                      | 为什么                                                                                                                                                                                                                            |
| --- | ------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1  | **API 密钥为空**          | 配置同步**不传** `includeSecrets`（`configSync.ts` 的 `publishConfigToSyncDir`），而 `sanitizeSettingsForBackup` 会把 `apiKey` / `profiles[].apiKey` / `agentProfiles[].apiKey` 全清空。放共享盘 = 谁都能拿到。**B 机需手工填。** |
| A2  | **回收站里的节点**        | 不导出，只在 `format.skippedNodes` 里报个数。回收站里的东西在别人机器上没有意义。                                                                                                                                                 |
| A3  | **工作区标签 / 会话草稿** | 在包里（`workspaceState`），但导入侧**不恢复** —— `replaceWorkspace` 要求 `importTasks === true` 且包里确实有任务，而配置包是 `importTasks: false`。                                                                              |
| A4  | **纯 UI 偏好**            | 上次停在中控台哪个 tab（`controlConsoleSection`）、树的折叠状态（localStorage）**都不进包** —— 它们是「我这台机器上想怎么看」。                                                                                                   |

### 10.3 值一致、但目标机器上「用不了」的（本机绝对路径）

`outputDir` / `mediaOutputDirs` / 节点 `overrides.outputDir` / `byMedia.outputDirs` /
`distribution.targetDir` / 设置里的 `customBackupPath` / 水印库的 `logoLibraryPath` 与
`backgroundFolders` —— 都是**绝对路径**。

包搬过去后，配置里照旧写着 `D:\交付\…`，但 B 机没这个盘就是没这个盘：产出会失败或写到别处。
**只有 UNC 共享路径（`\\server\share\…`）才真正跨机器可用。**

### 10.4 会真丢的 / 会变的：水印素材与字体 ⚠️

#### A. 本机选图型的水印素材（会真丢）

水印图层里引用的图片有 5 种形态（`CompositeV2ImageAssetRef`）：`stored` / `dataUrl` /
`project` / `path` / `internal`。

- 导出前的 `migrateLegacyCompositeAssets` 只迁 `dataUrl` 与 `project` 两种 → `stored`；
- `collectCompositeAssetIds` **只收集 `stored`**（`compositeAssets.ts:76-87`）；
- 而 `{ kind: 'path', path }` 由「**从本机磁盘选图**」直接写入（`PresetCanvasEditor.tsx:376`、
  `PresetLayerPanel.tsx:121`），既不迁移也不收集。

⇒ **这类图片不进包，B 机上断图。** 这是当前实现的一个真实缺口，已列入 §八 第 11 项。

**怎么自查**：判断标准不是「用不用库」，而是**那张图是怎么进到图层里的** ——

| 图片的来源                                       | 落成什么形态                  | 跨机器    |
| ------------------------------------------------ | ----------------------------- | --------- |
| LOGO 库选 LOGO（`PresetManagementTab.tsx:1001`） | `stored`                      | ✅ 不丢   |
| 素材库送入 / 预设导入                            | `dataUrl` → 导出前迁 `stored` | ✅ 不丢   |
| **编辑器里的「替换图片素材」→ 从本机磁盘选**     | `path`                        | ❌ **丢** |

**为什么 LOGO 库不用额外处理**（2026-09-22 杰哥问「把 LOGO 库固定持久化是不是就行」）：

LOGO **一导入就立刻落库** —— `importLogoFiles`（`PresetManagementTab.tsx:363-393`）先
`storeCompositeBlobs(blobs)` 拿到 `assetId`，再 `addProjectLogos({ id, name, assetId })`；
而 `collectCompositeAssetIds` 会收集**所有**带 `assetId` 的 `projectLogos` ⇒ **每一张 LOGO 都进包**。
从库插图到水印也是写成 `{ kind: 'stored', assetId }`（`PresetManagementTab.tsx:997-1005` 的 `onPickAsset`），
拿不到 `assetId` 时**直接不插入**。

⇒ **LOGO 库天然跨机器安全，不需要任何"额外固定"。** 真正要处理的是上表第三行那类
（编辑器里从本机磁盘直选的图），与"用不用库"无关。

#### B. 字体（不会乱位置，但会变字形）⚠️

- `fontFamily` 在水印编辑器里是一个**自由文本输入框**，不是可选列表 —— 它只是个**字体名**，
  **字体文件不随包走**：`public/` 下没有任何字体文件，全仓也没有 `@font-face`。
- 渲染时把它直接拼给 canvas：`ctx.font = \`${layer.fontWeight} ${layer.fontSize}px ${layer.fontFamily}\``
（`compositeRendererV2.ts:307`、`compositeTextLayout.ts:89`）。
- ⇒ B 机没装同名字体时，canvas 回退到系统默认字体：**字形、字重合成、单字宽度都会变**。
- **但布局位置不会乱**：文字是按 `position` 给出的**框**对齐的（`ctx.textAlign = layer.align`，
  贴框的左 / 右 / 中边），框的几何全在包里 ⇒ 居中还是居中、贴右边还是贴右边；字号是像素值、
  行距与字距也是数值 ⇒ 行 / 列位置不变。变的是**文字自己长什么样、有多宽**。
- 真正的风险是**溢出**：某行文字在 A 机上正好排满框、B 机字体更宽时，可能压出框外。

⇒ 要完全一致，B 机必须装有与 A 机**同名同版本**的字体（建议在发布说明里附上所用字体清单）。

### 10.5 时间戳与合并语义

- 节点 `createdAt` / `updatedAt` 在包里没存，恢复时用 `now` 补齐（`toAssetCollections`）→ 数值不同，用户看不见。
- 设置走 `mergeImportedSettings`：**干净的 B 机**（只有默认 profile）→ `hasOnlyDefaultProfiles` 为真 →
  **整份采用**；**已经加过 API profile 的机器** → 走合并分支，只追加 `profiles` / `customProviders`，
  其余字段保持本机值（`...current`）→ **不一致，且这是刻意的**。

### 10.6 时机与来源

- 发布是**手动**点「发布配置」→ 拿到的是**那一刻**的快照；发布之后改的配置不在包里。
- 拉取取的是配置目录里**字典序最大的那份**（`listSyncDirConfigs` 排序后取第一个），
  **不区分是谁发布的** —— 目录里有别人发过，你拉到的可能不是自己那份。

### 10.7 要「完全一致」必须满足的前提

1. B 机装**同一个大版本**的糖包（`format.version` 不认识就整包拒收）。
2. 发布前确认配置目录里**只有你一份**（或确认最新那份就是你发的）。
3. 发布完成后**不再改**配置。
4. 所有路径类配置改用 **UNC 共享路径**，或每台机器建同样的目录；且配置目录要在主进程白名单里
   持久化放行（R-31 / R-62，`local-settings.json` 的 `configSyncPath`）。
5. 水印素材不要用「从本机磁盘选图」—— 或等 §八 第 11 项落地。
6. **B 机装好与 A 机同名同版本的字体** —— 文字水印唯一的观感差异来源（见 10.4·B）。
7. API 密钥手工补。
8. 接受回收站、UI 偏好、时间戳这三类不参与还原。

---

## 十一、导入与拉取：覆盖范围

> 「已有资料的同事拉一份别人的配置」是最容易出事的一步 —— **他本机的东西会不会被清掉**。
> 这一节把口径写死。

### 11.1 一句话语义

**勾了才用包里的，没勾的这一块完全不动。**

勾选粒度停在**模块级**（5 项）：

| 项                   | 含什么                                         | 拉取配置时的默认 |
| -------------------- | ---------------------------------------------- | ---------------- |
| 项目树与各方向的参数 | 树、每个节点的覆盖、产出目标与无归属兜底     | 勾               |
| 水印库               | 预设 + LOGO 列表与顺序、标识符、水印全局适配   | 勾               |
| 渠道与尺寸           | 渠道字典、默认投哪些渠道、按渠道的导出位置     | 勾               |
| 全局产出配置         | 输出位置、命名模板、画面适配、分发             | 勾               |
| 应用设置             | 主题与本机偏好                                 | **不勾**         |

**为什么不能再细到字段级**：这几块互相引用 —— 方向参数引用节点 id、水印按产品 id 归属、
渠道选择引用渠道 id。自由组合会勾出「引用了不存在的东西」，那是比"多覆盖了一点"更难排查的静默失效。
（应用设置默认不勾：拉一份**工作配置**，不该顺手把别人的主题与偏好也换掉。）

### 11.2 「本地自建」是另一个问题

「勾哪些模块」回答的是**包里的要不要进来**；「本地自建的方向 / 水印怎么办」回答的是
**包里没有的要不要删**。两者合成一个控件会让人算不清自己选了什么，所以单独一个开关：

- **保留（默认）**：同 id 以包为准，本地独有的原样留着；
- **删除**：本地独有的**移进回收站**（不是彻底删 —— 拉配置是可反悔的操作，而回收站里的节点
  也不会被下次导出带出去）。

### 11.3 产出配置按字段分组拼（不整份替换）

`PostprocessMediaConfig` 是一个大对象，「渠道与尺寸」与「全局产出配置」**住在里面**，
所以整份替换会让只想换渠道的人连命名模板一起被换掉。恢复时按字段分组拼，分组表是
`POSTPROCESS_FIELD_GROUP`（`Record<keyof PostprocessMediaConfig, …>`，**加字段忘了归类就编译报错**）。

判据是**引用关系**，不是字段名：

| 组            | 含哪些字段                                              | 为什么归这一组                       |
| ------------- | ------------------------------------------------------- | ------------------------------------ |
| `channels`    | 渠道字典、`mediaOutputDirs`、`selectedMediaIds`         | 都引用**渠道 id**                    |
| `watermarks`  | `watermarkPresetIds`                                    | 引用**水印预设 id**，要跟水印库同进同退 |
| `tree`        | `selectedCollectionIds`、`savedTargetCollectionIds`     | 引用**节点 id**                      |
| `postprocess` | 输出位置、命名、画面、分发                              | 不引用任何东西，可以独立覆盖          |

合并本身是纯函数 `applyPostprocessScope`（在 `src/lib/treeConfigBundle.ts`，无副作用、好测）。

### 11.4 「更新的数据」怎么办

同 id 撞车**一律以包为准**，不看谁更新 —— 发布方那份是标准配置，这正是「拉取」的意义。
回退靠**拉取前的自动备份**（备份失败就中止，不做没有退路的覆盖）。

**不按 `updatedAt` 比大小**：两台机器的时钟不可靠。ADR-0014 已经因此定了「不靠机器时钟挑最新」
（发布文件名用时间戳而不是靠时钟比较），这里沿用同一条口径。

### 11.5 导入前的预览

拉取面板展开时会先读一次包里的 `config.json`（**只读这一个条目，很轻**），告诉用户：
「这份包含 N 个节点、M 套水印；你本机还有 K 项是这份里没有的」。

读不到就**不显示、不阻断** —— 预览是锦上添花，"我自建的方向参数怎么没了"才是它要拦的问题。
